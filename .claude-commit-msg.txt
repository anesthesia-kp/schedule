docs: correct the build-50 paperwork — it shipped while the docs still said "awaiting push"

Build 50 / 26 was pushed as 8c43847 and verified live (versions.json read
{"index":26,"admin":50} cache-busted). HANDOFF.md, NEXT-CHAT-START-PROMPT.md
and TODO.md were all still describing it as filed-and-awaiting-push, and the
start prompt additionally listed the live builds as 49 / 25 in its opening
section while its own status table said 50 / 26 — internally contradictory,
which is worse for a fresh session than being plainly out of date.

CORRECTED
  · all three files now state 50 / 26 LIVE, with the commit ref
  · the start prompt's "What this is" section no longer contradicts its own
    status table
  · every build number in both documents is now framed as a snapshot that must
    be re-verified against a cache-busted versions.json, with that instruction
    placed at the TOP rather than buried in a later section

ADDED — two harness traps that cost real time on 16 Aug, now written down
  · a fixture's versions.json must match the `var BUILD` of the bytes under
    test, or the new stale-build gate reloads the page mid-run and wipes the
    seeded fakes. It reads exactly like a page bug. Both harnesses now derive
    the number from the file.
  · the fake auth starts SIGNED OUT; hiding #authGate is not enough, and
    without window.__signInNow() every grid renders header-only. This produced
    8 failures against a page that was completely fine.
  · and the general form: a red test caused by the harness is indistinguishable
    from a red test caused by the code. Run the suite against the PREVIOUS
    build before believing a new failure.

ADDED — the honesty record in the switching-sites section
  This is the third instance of answering from stale in-session memory instead
  of from disk: 269-already-live, the stale battery claim, and now build 50
  being live while the docs said otherwise. The owner caught the last one, not
  the process. Recorded as such, because the pattern is the point.

HANDOFF.md also gained an index of all 34 owner rulings, grouped by area, with
the four that overruled Claude flagged — script-verified that every ruling in
DECISIONS.md appears exactly once and that the index invents none.

No code change. No test change. No Firebase console step.
