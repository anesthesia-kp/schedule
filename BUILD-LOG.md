# BUILD LOG — Daily Schedule

**Why this file exists.** On 16 Aug the owner asked: *"do i really have all the commit
summaries?"* The answer was **no**. Several commits carry a test file's opening lines
instead of the summary written for them, and one auction commit says only "hosuekeeping".

Nothing substantive was lost — `DECISIONS.md` holds every ruling and `anesthesia-kp.github.io/HANDOFF.md` holds the
per-build detail, and both are committed — but the *commit history on its own* no longer
tells the story. So the story lives here too, in a file that is committed and cannot be
lost to a mis-paste.

**Every build gets a row here, written at the same time as the code.** A poor commit
message then costs nothing.

The build number below is authoritative: it was read out of `var BUILD` in that commit's
own bytes, NOT taken from the commit's subject line — two subjects are wrong, and they are
flagged. Where the full prose survives it is in the named commit, or in
`~/Documents/GitHub/_archive/schedule/commit-messages/`.

| build | commit | date | what shipped |
|---|---|---|---|
| **151** (admin) + **staff 51** | pushed 31 Aug, verified served twice | 31 Aug 2026 | **§142 — THE SETUP CHECKLIST (admin) and D-7, THE CHANGE-FEED CAP (staff).** Admin: while MOST of the catalog has no staffing rules (half is the line; an empty catalog counts), Today's coverage chip comes down and a setup checklist stands in its place — how many shifts are never asked for, how many have no start/end times, who has no FTE, each line linking to the page that fixes it; a configured site never sees the card and its chip behaves exactly as before. Staff: the personal change feed is ordered and capped ON THE SERVER — `where(u)` + `orderBy(at desc)` + `limit(150)`, ordered by `at` never `atServer` (absent before 143; orderBy EXCLUDES documents missing its field), with a fallback to the old unordered read until the composite index exists (his console step, in TODO §1: collection `items`, `u` ASC + `at` DESC). Gates: `build151-test` 16/16 with honesty 12 red on the pushed 150; `staff51-test` 12/12 with honesty 5 red on the pushed 50; full schedule battery 108/108 suites EXECUTED in-cloud (browser suites RUN) on md5-verified bytes; auction battery + isolation guard after. Same wave, `tests` repo: `PUSH-ALL.command` (§142 — auction repo included at his instruction, behind its own typed PUSH). |
| **150** (staff unchanged) | `f5b8138`, pushed 31 Aug | 30 Aug 2026 | **§137 A + §139 — THE LINK E-MAIL.** One message per doctor: their private address as a `webcal://` link AND a copy-paste address (mail apps do not reliably make webcal tappable — the paste path always works), then the instructions §139 demands: iPhone in three steps; **Android honestly** — done once at calendar.google.com on a computer, appears on the phone, and Google refreshes on its own schedule (hours, not minutes); Outlook one line; a do-not-forward privacy warning; the calendar's §138 name. Sent to the Google sign-in address plus the secondary when one is set (the auction's rule). **The BULK send is behind §54's release gate** — refused with a dialog naming his ruling while shut; a per-person **E-mail** button on each row works anytime for testing and SAYS it is a test. Every send is awaited with per-recipient failure handling (the auction's hard-won pattern): a person whose send failed is never recorded as e-mailed; the tally and failures are toasted and audited (`feed.mail`); each row shows when its person was e-mailed (`dailysched/feedMail`). Sends are counted in the **schedule's own** `dailysched/mailStats` — never the auction's meter, which is a `vacations/*` write the isolation guard forbids (§2); the EmailJS allowance itself is shared and the bulk dialog says so. **The mailer loads LAZILY at send time** — a static script tag would fail to load in every browser suite's local server and turn their no-page-errors assertions red; production pays only when sending. Same service and template as the auction; **⚠ the SUBJECT lives in the EmailJS template** — before the first real send, send yourself one and read it; if it is auction-ish, a second template is two minutes in the EmailJS console and one constant changes. The fake learned `increment()` (sentinel resolved against the stored number, like 143's `serverTimestamp`). `build150-test.mjs` **34/34** driven in a browser: both addresses named and sent in order, the body's link/instructions/privacy asserted from the actual message, no-address and no-link refused plainly, gate-shut bulk refused with nothing sent, gate-open bulk sends exactly the right people and NAMES the skipped, a failed send leaves no "e-mailed" record, native-dialog counter zero. Honesty on the PUSHED 149 (`origin/main`, md5 `8375dba0…`): **27 of 34 red, exit 1, no crash**. Registered in `sched/run-all.mjs` the same turn. |
| **149** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **DECISIONS §140 — THE FEEDS KEEP THEMSELVES FRESH, AND EVERY EVENT SAYS WHEN IT WAS BUILT** (owner, comparing QGenda: *"unpark the auto-refresh… can we add that time stamp?"*). The 26 Aug parking is lifted. Every write path that changes a PUBLISHED month already names its people — `emitChanges` (§6) — so the auto-refresh hangs off it: people are collected for 2.5s (an engine run writing thirty days is ONE rebuild), then their feeds rebuilt, only those with a link, in the browser that made the change; quiet by design (an audit entry, `feed.auto`, and a "Last auto-refresh: time · N feeds" line on the Feeds page — never a toast). Announced-vacation deliveries are DIFFED per linked person, so an auction write that moves nobody writes nothing. **The read cost fell an order of magnitude:** `loadFeedMonths()` reads the ~150-day horizon ONCE per rebuild (was ~155 document reads PER PERSON — 5,000+ for a department-wide rebuild; now ~155 total), and *Rebuild every feed* and the 148 bulk-create share the same load (FB-3 input). **The timestamp:** every event — shifts, vacation, the draft marker — carries `Auto-sync from KP Anesthesia Schedule · <Mon D, YYYY, H:MM AM/PM>` (Pacific) in its notes, the QGenda habit he asked for by name. The Feeds page stops claiming feeds do not refresh themselves. `emitChanges` exposed on `window` as the suite's seam. `build149-test.mjs` **20/20** driven in a browser: two changed people → ONE feed written (the linked one, never the unlinked), the note on ALL its events, eight changes in a burst → one write, an unmoved vacation delivery → nothing, a moved one → at most the linked person, the status line filled. Honesty on the PUSHED 148 (`origin/main`, md5 `db7771f5…`): **14 of 20 red, exit 1, no crash**. Registered in `sched/run-all.mjs` the same turn. |
| **148** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **DECISIONS §137 + owner, same afternoon: *"why create each docs 1 at a time?"* — THE CALENDAR FEED IS OPERABLE.** The Calendar Feeds page had only a per-person *Create*, and each then wanted a *Build* — seventy clicks for 35 people; the caution belonged to REPLACE (it breaks a live subscription), not to CREATE. **One button** — *Create links for everyone without one* — asks through the page's own dialog (147's `schedConfirm`, naming how many and whom), mints the missing tokens in ONE write, builds every feed in the same go and reports the tally; Replace, Remove and Rebuild still ask, now in-page (the section's last three native `confirm()` gone). **Every calendar names itself on the phone** — `KP Anesthesia Schedule` (his words, §138), and `TEST · ` in front while the release gate is shut, so a testing feed is never mistaken for the real one (his worry, verbatim, in §137); `buildIcs` takes `released` as an argument so the suite drives both names. A calendar app reads the name when the person subscribes and Apple's does not rename an existing subscription later — the dialog says so (a correction, dated 30 Aug, of the first draft's "phones pick it up on refresh"). **The release gate (§54) is a stored switch**, `dailysched/feedConfig {released}` (admin-write / verified-read under the published rules — no rules change), flipped from the page behind a dialog that says what happens and what does not (*nothing is sent to anyone*); the flip rebuilds every feed. The red "not for doctors yet" prose is a **live status card**: endpoint deployed (30 Aug), §136 rules published, gate shut/open. Stale comments in the feed section brought current. One more small listener (FB-3 input). Nothing auction-side; the isolation guard holds. `build148-test.mjs` **33/33** driven end to end in a browser with the native-dialog counter at zero: the collapsed section is opened as an admin would (reachability, §3 rule 9), the bulk dialog lists exactly the two without a link, one write mints two 32-hex tokens, three feeds are written named `TEST · KP Anesthesia Schedule`, pressing again does nothing, opening the gate writes `feedConfig.released=true` and rebuilds all three without the prefix, Replace still asks and Escape writes nothing. **The suite caught one real bug before filing:** the gate dialog dropped its "N feeds are rebuilt" line when opening (a `+` bound to the closing branch only). Honesty on 147 (`/tmp/admin-147.html` = `90af11e`, md5 `ceef446e04a05fc9f914aabc66023b34`): **29 of 33 red, exit 1, no crash** — the two controls 147 never had report as failures, not throws (§3 rule 11). Registered in `sched/run-all.mjs` the same turn. |
| staff **50** (admin unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 §7b — THE FULL SCHEDULE ON A PHONE IS ONE DAY PER ROW, under §134** (*"the same code… just format correctly for phones… like the vacation site"*). The people × days table was contained since 132 but on a 390 px screen it was still a workaround: 1500 px of sideways scrolling under a sticky name column. Under 600 px the SAME month now draws as a day list — a heading per day with today marked, then each person who holds anything that day with the same chips the table draws, me first, *Nothing scheduled* for an empty day, and on a draft month one *Not published* pill per day rather than one per person. It is built in the same `renderAll` loop from the same `cellHtml` calls (`byDay` gathers the cells as the table rows are written) and a style rule chooses the shape (`@media (max-width:600px)` hides `#allGridFade`, shows `.daylist`); on a wide screen the table is untouched and the list is hidden. No second page, no second data path. **§135 (*"no text where you need to swipe"*): measured on every tab at 390 px, the last thing that scrolled sideways was the tab strip itself — 725 px of tabs, "Changes" off the edge; it wraps to three per row now, and the suite walks every panel asserting nothing scrolls sideways and no text sits past the right edge.** `staff50-test.mjs` 22/22: at 390 px the list shows, the table hides and the document is no wider than the screen; the 3rd (the clock) is today, lists me first (OFF) then BO on Call 24 and NOT CD who holds nothing; the 4th says Nothing scheduled; April (draft) shows 30 single pills; at 1280 px the table has every cell and the list is hidden; the source proves one loop and a style rule. Honesty on 49 (`/tmp/staff-49.html` md5 `78b3540d31df0b516394f4c88d82c651`): **16 red, exit 1**. A phone screenshot was sent to the owner. `build132-test` re-anchored (its phone-width scroll-box assertions now apply where the table shows). |
| **147** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 §7b — THE THREE HIGHEST-STAKES QUESTIONS ARE ASKED IN THE PAGE, under §134 (*"continue with what you can"*).** Apply, Publish and *Add the standard request types* asked through the browser's native `confirm()`, which cannot be styled, cannot show a LIST and looks like an alert from somewhere else. `schedConfirm` (title, html, ok, cancel) opens an in-page dialog (`#schedDlg`, the site's own modal card) and resolves `true`/`false`; Escape and a click on the backdrop answer no; the OK button takes focus and says what it does (*Publish*, *Apply the plan*, *Add them*); a second question while one is open answers the first no. **Publish** lists the standing violations by day, person and rule (up to ten, then "…and N more on the Rules page"); **Apply** shows the plan summary, the over-ceiling days as a list, and the month's state; **Add** lists every type it will add. Every other `confirm()` on the page — autoPopulate, Clear Month, the deletes — is unchanged. `build147-test.mjs` 29/29 driven end to end with the native-dialog counter at zero: each dialog opens with its list, Escape / backdrop / Go back write nothing, OK writes; one at a time. Honesty on 146 (`/tmp/admin-146.html` md5 `b151862e41552f913ad011bfaf55f8fb`): **18 red, exit 1**. Six older suites re-anchored to press the page's OK button instead of auto-accepting a native confirm (build120, 121, 122, 127, 128, 129) — and build129's "asks nothing" now also checks the page dialog did not open, so it cannot pass vacuously. |
| **146** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 §7b — THE SIDEBAR REGROUPED BY WHAT AN ADMIN IS DOING, under §132 (*"go as you think best"*).** Twenty entries in five groups, where two were "everything else": Stats and Coverage READ the schedule but sat under *Setup* beside Shift Families; Engine and Reports sat under *Admin* beside the Audit Log. Now, in the order a month is worked: **Schedule** (Today · Schedule Grid · Engine · Print) · **Approvals** (Requests · Swaps · Open Shifts) · **Reporting** (Reports · Stats · Coverage) · **Setup** (Shift Catalog · Shift Families · Shift Eligibility · Quotas · Rules · Request Types · Groups · Users) · **Records** (Audit Log · Calendar Feeds) · Other Systems. Markup only — every panel id, handler and page is unchanged; the Engine keeps its own entry (§123 ②, his sentence; "Admin between People and Setup" was Claude's arrangement, retired). `build146-test.mjs` 14/14 in a browser: the five headings in order and each group's members; **every one of the 20 panels has exactly one link, no link is dead, no panel is linked twice, and clicking each link opens its panel** — the inert-build question (§3 rule 9) asked of a navigation change; a before/after screenshot was sent to the owner, who kept the grouping and asked for plainer headings — the verb headings of the first draft became these nouns (§133) before the push. Honesty on 145 (`/tmp/admin-145.html` md5 `258dacd77bcff006dc4fbedf9da1e544`): **7 red, exit 1**. `build120-test` and `build53-test` re-anchored (they pinned the People/Admin/Setup headings; the invariant they stood for — the Engine has its own entry, no Testing group — is what they assert now). **Reversible in one markup change if he wants the old grouping back.** |
| **145** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 VISUAL DEFECTS V-7, V-12, V-13, V-20 AND TWO §7b PROPOSALS, under §131/§132.** **V-7:** the Families page is also the legend, and three families had landed on the same yellow because every new family defaulted to `PALETTE[0]`. A new family now takes the first colour no family holds (`famFreeColor`), and recolouring onto a colour another family holds asks first, naming that family (`famColorTaken`). **V-12 + "one collapse control":** the Reports settings fold was a `<details open>` — the one fold on the site that was not the vacation admin's ▸ Show / ▾ Hide bar (§122), and it opened EXPANDED against §104 — under a yellow "open by default" banner; the call-baseline fold was a `<details>` too. Both are the site's own bar now (`reportsettings`, `baseline` in `SECT_STATIC`), shut at boot, labelled at boot; `.lockbar.open` is a quiet status line in the site's greys. The pages stay UNLOCKED per §60 — only the volume changed. **V-13:** the overnight-call explanation named three shifts from a sample dataset (RCH-ICU B, Eye Call, Call 12 AM) as facts about this group; `renderReports` now writes it from THIS catalog — a real midnight-crossing shift not on the list, a real call-family shift that runs in daytime — and says "no rule of thumb applies" when the catalog has neither; the reset button names only starting-list shifts that exist here and hides when none do. **V-20:** Compact squares were 9 px pale tints with no legend; 12 px with a hairline edge, and `renderGridLegend` draws one swatch per family with a shift, plus vacation and day off, shown at Compact density only. **"One place for holidays" (§7b):** holidays STAY on the Shift Catalog page beside the staffing rules that read them — the owner built them there (§102/§104) — and are one link from the Rules page (`gotoHolidays` opens the section). `build145-test.mjs` 30/30 in a browser. Honesty on 144 (`/tmp/admin-144.html` md5 `a593627d503aa74ba2c48ea8c83285ca`): **21 red, exit 1**. |
| **144** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 B-8, B-9, N-20 AND VISUAL DEFECTS V-5, V-6, V-8, V-10, V-15, V-18, under §131.** **B-8:** the Add panel says the Vacation Auction still needs its OWN FTE for the person before its next phase can begin — an inherent coupling of "shared roster, separate FTE" it never mentioned. **B-9:** `addSchedUser` is six writes with the LIVE roster first; a failure at step two used to read "the change did not save" while the person already sat on the roster with no name. Each step that lands is remembered (`step(label,fn)`); a failure names what landed and what did not, and audits the partial; `saveAllSchedUsers` likewise. **N-20:** `_flushRender` cleared a key's dirty flag BEFORE calling its renderer, so a render refused mid-edit (`renderUsersGrid`, `renderBaseline`) was discarded, not deferred, and recovery hung on a focus-out timer; a renderer now returns `false` to refuse and the key stays dirty (`_dirtyProbe`). **V-5:** the cell editor's only exits were Done and Remove everything — both sound like commits; ✕ and Escape close it, changing nothing. **V-6:** View and Density are each ONE flex item (`.tbgroup`), so a narrow toolbar wraps a whole switch, never "Compact" alone beside the name filter. **V-8:** Eligibility opened on the first family by design (67) and nothing said so; a count line on the board reads "Showing N of M shifts — filters are on". **V-10:** the Stats box scrolls inside itself (the total row painted outside it past 70vh) and both Stats and Coverage cards shrink to their tables (`.fitcard`). **V-15:** the sign-in button disables and reads "Signing in…" the moment it is pressed, re-enabled if access is refused. **V-18:** the Today board's "latest change" chip carries "N min ago" (`agoText`). `build144-test.mjs` 35/35 in a browser (V-15 asserted before the fake signs in). Honesty on 143 (`/tmp/admin-143.html` md5 `4abd72064eb760319afc6c269b2275b8`): **19 red, exit 1**. `build64-test` re-anchored (the mid-edit guards return `false` now). |
| **143** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 REQUESTS, SWAPS AND THE CHANGE FEED — R-6, R-9, R-10, R-11, R-12, D-9, D-10, D-13, under §131.** **R-6:** approving a day-off in a month still in the old storage format flipped the request to approved, `mutateCell` refused, and the audit recorded "marked them off" — a change that never happened; `decideReq` now asks `legacyAmong` first, exactly as the swap path has since 59, and refuses with a toast. **R-9:** a withdrawn swap was labelled "❌ denied". **R-10:** a swap denial could never carry a reason (the staff page displayed one; nothing wrote one) — the requests queue's inline optional box, copied (`_swapWhy`, `swapWhyOf`), written on deny, shown in Decided, named in the audit. **R-11:** an unconverted month found INSIDE the swap transaction returned before it reached the item, so `r.s` was null and Approve said nothing; the refusal is read first now (the build-59 pre-check still fires first on the ordinary path — this is the race case). **R-12:** changing a request type's kind away from off/absence resets a stale "weekend" span nobody could see. **D-9:** returning a month to draft tells everyone in it, as publish and clear do. **D-10:** publish counted shifts only, so someone whose whole month is approved absence got no entry; `monthPeople` counts days off too and the entry names them. **D-13:** every feed entry carries `atServer: serverTimestamp()` beside the admin's `at`. **The fake learned `serverTimestamp()`** (resolved to a Timestamp shape at write time). `build143-test.mjs` 30/30 in a browser. Honesty on 142 (`/tmp/admin-142.html` md5 `b61fd607157636196bfe3edc652dd343`, rebuilt from 141 + the 142 patch): **15 red, exit 1**. `build123-test` re-anchored (`decideSwapTx` takes a third argument). |
| **142** (staff unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 TIER 3 ENGINE-AND-RULES TIDY-UPS — E-5, E-9, E-10, E-12, E-13, E-14, N-15, N-16, under §131 (*"move ahead with all of those that you can fold into this session"*).** **E-5:** the "checked against this day only" disclosure named minRest, maxPerPeriod and cooldown and forgot the built-in post-call check. **E-9:** "Not more than" embedded the ordinal in its wording and the warn-at-change diff dedups on the wording, so adding a call BEFORE the last over-cap one renumbered it and one addition read as two new violations; a stable `key` (tag + period) is what the diff dedups on now. **E-10:** a quota slot a rule stops was listed twice — pass 2 gains pass 1's guard. **E-12:** a Fairness violation for someone with no tagged shift was dated the 1st and drew a red corner dot on an empty cell; pool violations carry `pool:true`, count in the chip, and are never drawn on a cell. **E-13:** the Shifts-view chip says the marks live on the People view. **E-14 / N-16:** two comments that described code that no longer existed (the ceiling fallback; the pre-63 300-entry trim). **N-15:** the eligibility warning named "the Shift Eligibility grid", retired in 89. `build142-test.mjs` 21/21 in a browser (ruleDiff, checkMonth, fillDay and the grid driven). Honesty on 141 (`/tmp/admin-141.html` md5 `af1c5f6ee7f85255704f1de1d1270e5d`, from `f561a43`): **10 red, exit 1**. `build61-test` re-anchored (it pinned "not ticked as eligible"). |
| staff **49** (admin unchanged) | filed 30 Aug, not pushed | 30 Aug 2026 | **RA-6 STAFF TIDY-UPS, TWELVE AT ONCE, under §131 — D-12, D-13, D-14, S-5/B-13, B-14, S-11, S-13, S-14/B-10, R-7, R-8, R-13/S-15, and V-7's staff half.** **D-12:** sign-out left the previous person's change-feed listener running behind the sign-in screen; `unsubscribeSchedule` detaches it. **D-13:** "unread" compared the doctor's clock with the admin's; `chgAt` reads `atServer` (admin 143) when present, and *Mark all as read* stores the newest entry's own time — no two clocks are compared. **D-14:** feed words were frozen at write time, so a renamed shift showed its old name for ever; `changeText` is ported and the words are rebuilt from the stored facts, the frozen text only a fallback. **S-5 / B-13 / B-14:** two boot listeners on auction documents — `loginEmails` (denied to an anonymous session, then duplicated after sign-in) and `adminSettings` (never read; every auction-admin toggle delivered to every schedule session for nothing) — gone. **S-11:** every read failure was silent; `subscribeSchedule` passes an error path to one banner (`#readErr`) that names what did not load and offers a reload; the silent no-ops — accept/decline/withdraw on something already decided — say so. **S-13:** the commit buttons measured 21–30 px; 44 px on touch screens and windows under 700 px. **S-14 / B-10:** the staff logger appended to the pre-63 array the admin no longer trims — this page was its only writer and at the 1 MB ceiling logging would fail silently for ever; it writes `auditLog/entries/{id}` under the admin's id scheme. **R-7:** a typed work request over shifts the person may do none of is refused before the write, naming them. **R-8:** the page reads `dailysched/tags` and offers a work/avoid type only when its tag names a shift that exists — the admin's own `rtReady` test. **R-13 / S-15:** one write in flight per submit button. **V-7 (staff):** the legend is drawn from the catalog's families in the chips' own tints, not saturated CSS colours. `staff49-test.mjs` 36/36 in a browser. Honesty on 48 (`/tmp/staff-48.html` md5 `f136e70fb5dccbbfde3c54346c99e595`, from `f561a43`): **28 red, exit 1**. `staff46-test` re-anchored (the logger's target moved). **Not taken:** S-4's duplicate month subscription — the SDK serves a second listener on the same document from its cache, so the read is not doubled, and excluding the current month from Quick View would couple two independent subscriptions for no cost saving. |
| **141** (staff unchanged) | `f561a43` | 29 Aug 2026 | **RA-6 D-6 CLOSED — A DRAFT MONTH NEVER REACHES A DOCTOR'S CALENDAR AS IF FINAL.** `readMonth` has reported `published` since 136 and `buildFeedFor` ignored it: the feed horizon is 120 days forward, so a month still under construction was ALWAYS inside it, and every edit until publish day silently re-arranged the doctor's phone. A draft month now contributes NO shifts (`null` in the months map, never `{}`), and `buildIcs` paints one all-day, `TRANSP:TRANSPARENT` marker across the draft's days inside the horizon — *"Schedule not yet published — check back"*, `UID` keyed to the month so a publish replaces it cleanly on the next build. The calendar twin of Stage 3's *Not published* pill: a blank must say why, because a blank reads as a day off. The feed document records `drafts:[…]`; the audit entry and the toast name the months. **N-7's write half** (feeds writable by any physician) is closed by the Stage 5 rules, and the Feeds page's release-blocker text now names the real precondition — the Stage 5 rules published — instead of "lock the token list", which RA-6 B-6 showed fixed neither the read nor the write. `build141-test.mjs` 17/17 in a browser: a published shift is in the feed, the draft month's shift is not, the marker starts on its 1st and is all-day and transparent, the feed document carries the draft, the toast says so, and publishing the month swaps the marker for the shifts. Honesty on 140 (`/tmp/admin-140.html` md5 `d39485e945e45aea7d113a14b2a2df08`): **10 red, exit 1** — the baseline's feed carries `AR-2026-09-03-D`, the draft shift. `build110-test` re-anchored (it pinned the old blocker sentence). Batteries: see the closing report — this is the last build of the session. |
| staff **48** (admin unchanged) | `f561a43` | 29 Aug 2026 | **RA-6 S-3, THE CLIENT HALF — THE STAFF PAGE SUBSCRIBES TO ITS OWN SLICE.** The page renders exactly two slices of the request and swap collections — my requests (`user===me`) and the swaps I am party to (from/to me, or a 3-way listing me); every renderer already filtered to them. Yet it subscribed to the WHOLE of both collections, so every colleague's requests — and the notes typed into them (*"surgery on the 14th"*, *"IVF appointment"*) — sat in every browser's memory, one console line away. `subscribeSchedule()` now asks for the slices: `query(reqItemsCol, where('user','==',me))`, and three swap queries merged into one map — `from == me`, `to == me`, `participants array-contains me` — because Firestore has no OR; each slice keeps its own map so a deletion in one cannot resurrect through another. All single-field equality/array-contains filters, so no composite index is needed and nothing has to be created in the console. The Stage 5 rules gate the read to a signed-in account; this is the other half. Legacy arrays (nothing has written them since 30) are read as before. **The fake learned `array-contains` and now THROWS on an operator it does not know** — it used to return `true`, so an assertion over an unknown operator could never fail (rule 10). `staff48-test.mjs` 18/18 in a browser: the queries the browser sent carry the filters; the items it holds are exactly `r_ar` and the three swaps I am in; BO's and CD's notes never arrive and appear nowhere on the page; my own request renders with its note; the badge counts the two that need me; Accept works on the sliced data; a swap newly addressed to me arrives through its slice. Honesty on 47 (`/tmp/staff-47.html` md5 `c7db32d32766e77a216108d07b7ac341`): **11 red, exit 1** — the baseline's queries are `[[]]`, unfiltered. `staff47-test` re-anchored (its "ten listeners" was a count pin; twelve now); `build133-test`'s malformed swap is now addressed to AR so it still reaches the staff slice (an item nobody is party to is simply never delivered — not the case S-10 guards). **Schedule battery on the 140/48 tree: 94 of 94 suites EXECUTED in the cloud, one red — build133's seed, re-anchored and green on its own re-run.** Auction battery not re-run: it reads the schedule ADMIN page, unchanged since its 55/55 run on 140. |
| **140** / staff **47** | `f561a43` | 29 Aug 2026 | **THE PAGE HALF OF STAGE 5 (§129) — NO SCHEDULE READ BEFORE SIGN-IN, ON EITHER PAGE.** The Stage 5 `firestore.rules` (filed in the auction repo the same afternoon, its `vacations` block byte-identical) make every `dailysched` read need a verified Google sign-in. A Firestore listener attached before the token carries one is refused once and never retried — so a page that subscribed at boot would go blank the moment the rules were published. **Staff:** all sixteen schedule listeners were attached at module scope, before anyone signed in, and `setMonth`/`syncQuickViewMonths` attached the month's two more. They now live in `subscribeSchedule()` (idempotent, ten listeners), called from `finishSignIn` together with `setMonth(curYM)`; `setMonth` and Quick View do nothing while `me` is null; both sign-out paths call `unsubscribeSchedule()` so a second account on the same tab re-attaches with its own token. Verified before changing anything: the sign-in screen waits on exactly three AUCTION documents (`userList`, `usernames`, `adminSettings`) and nothing under `dailysched`. **And the claim re-apply** (`applyOpen`) used to CREATE a copy of the admin's decision under a dated id — a physician creating a decided document, which the rules now refuse; the claim is written back as pending with the decided history riding inside it (`prev`, newest first, capped at ten, never nested). **Admin:** the boot `setMonth(ymKey(new Date()))` ran before `authReady` — the one schedule read this page made without an admin's token; it sits inside `authReady.then` now. Both pages are correct under EITHER rules file, which is why they go live FIRST (§2's order, reversed on purpose and written into `TODO.md` as his two steps). `staff47-test.mjs` 22/22 in a browser against the fake, which gained `window.__listens` (every attach, in order): zero `dailysched` listeners before sign-in on both pages, all of them after; the re-apply writes one pending document and no archive. Honesty on 46/139 (`/tmp/staff-46.html` md5 `f0f5f7cdf76284e8f3903318dbd07bb7`, `/tmp/admin-139.html` md5 `5df31786fc6fa4693354bbb2e3258f90`): **11 red, exit 1** — sixteen listeners before sign-in on staff, two on admin, `claim_o1_AR_200` created. `staff30-test` re-anchored to the invariant (the denied decision survives the re-apply, in either form). Batteries: see the 139 row — one run covers both. |
| **139** (staff unchanged) | `f561a43` | 29 Aug 2026 | **RA-6 B-2 (WITH N-20, N-21) AND B-7 CLOSED — THE LAST SCHEDULE CODE IN RA-6 (§128).** **B-2** — "Save all changes" read every value from the SCREEN, and the Users grid refuses to re-render while a cursor is in it ([64]) — for as long as the cursor is there, not 250 ms. A person added on the auction site during that window had no row; every box of theirs read blank; blank meant `deleteField()`: name, Google login and KP e-mail gone from the LIVE auction roster under a green "Saved all N users". Two guards now. (1) Any roster member without a row on screen, or a stale grid, REFUSES the whole save and names who is missing. (2) Only fields that CHANGED against the loaded data are written — an untouched field is not a write, a blank that was never there is not a delete, a blank over an existing value still is; an empty diff says "No changes to save"; the login-map sync runs only when a login address actually changed. The in-batch duplicate-login check reads EVERY row's address, changed or not (`seenLem`), because the diff map no longer holds them all. **N-20/N-21** — the mid-edit bail set the header count BEFORE bailing, so the one on-screen signal read "All Users (38)" over 37 rows; the count now moves with the rows, the bail sets `_usersStale` and shows a notice ("roster updated while you were editing — click outside the grid to refresh before saving"), and the [64] focus-leave re-render clears it, so the deferred update is never lost. **B-7** — `removeSchedUser` wrote four auction documents (`userList`, `usernames`, `loginEmails`, `emails`) and stopped, while the auction's own Remove also deletes bids, best-bids, bid times, bid phase and locks, walks every week's approvals and denials, and KEEPS a completed-phase winner's FTE. Copying that into this page would double fairness-critical logic and widen the one write surface §1 keeps narrow — so the schedule's per-row button (relabelled **🧹 Clear (this site)**) clears THIS site's data only (`_schedOwnDataOps`: group membership, category, per-shift overrides, this site's FTE, the calendar-feed link), leaves the roster alone, and points at the Vacation admin site → Users; a new **🧹 Clean up departed users** sweep clears the same data for anyone no longer on the roster (an auction-side removal — now the only roster removal — never cleaned this site). The unlock dialog says so. **The isolation guard's sanctioned set is FOUR handlers now** — `removeSchedUser` left it, pinned at zero auction writes, fixture C re-aimed at `saveAllSchedUsers`; on 138 the guard goes red three ways. `build139-test.mjs` 34/34 in a browser against the fake: DX joins the roster mid-edit → no row, count stays 3, notice shows, Save-all writes NOTHING and names DX; leave the grid → DX has a row; edit one name → exactly one `usernames` write holding only AR, no `deleteField()` anywhere; blank BO's FTE → one `fteMap` delete and nothing else; BO taking AR's unchanged address → refused; clear BO → zero auction writes, group/category/override cleared, BO still on the roster; the sweep → every key names ZZ. Honesty on 138 (`/tmp/admin-138.html` md5 `3eaa164189da854fc5df3c1ef8fe8fd2`): **22 red, exit 1** — the baseline's own output shows `DX: delete` on `usernames`, `loginEmails` and `emails` under "Saved all 4 users", and five auction documents written by Remove. Three older suites re-anchored to invariants (build64's guard regex, build79's deleteField premise and its 2,600-char window that had stopped reaching the line, build89's cleanup window now spanning the helper) — rule 12, never a stub. **Schedule battery on the final 140/47 tree: 93 of 93 suites EXECUTED in the cloud (browser suites running), one red on the first pass (staff30's archive pin, re-anchored), re-run clean — see the session's closing report for the frozen-tree numbers. Auction battery on unchanged auction bytes: 55 of 55, 2,019 assertions, exit 0 (regression gate; auction baseline absent by design; the rules-emulator suite skips loudly here — it runs on the Mac, and it is the gate for the rules file).** All six inline scripts across both pages `node --check` clean. |
| **138** (staff unchanged) | `abc3c18` | 29 Aug 2026 | **§127 — ONLY ANNOUNCED VACATION REACHES THE SCHEDULE.** Owner, reviewing the day's builds: *“schedule can only populate vacations approved once admin has sent the e-mail results.”* Build 134 had given the admin every DECIDED week — completed phases whether or not announced, every Phase-4 round, and the auction's live approvals document — as Claude's decision for review; overruled the same day. The admin's `vacationDays()` is now the RELEASED view, `vacationDaysFrom(vPhases,null,true)`, the same words the staff page reads; the `vacations/approvals` listener, its state and its reference are gone, so nothing decided-but-unannounced reaches the grid, the engine, the reports or the feeds, and B-4's schedule-only-admin case is retired with it (the phases document is readable by every registered account). The E-15 memo keys on the phases document alone; the denial banner keeps its one remaining case. `build138-test.mjs` 9/9; honesty on 137: 5 red. `build134-test.mjs` re-anchored by ruling. |
| staff **46** (admin unchanged) | `abc3c18` | 29 Aug 2026 | **RA-6 S-1, S-8 AND N-14 CLOSED.** **S-1** — *“may I request this shift?”* was answered from `dailysched/eligibility`, a flat grid the admin retired at build 89 and nothing writes: frozen data, wrong in both directions (a shift the admin had since allowed stayed unrequestable; one since withdrawn stayed offered). Eligibility lives per shift in the catalog — allow / block / required groups + mode — so the staff page now carries the admin's checker (`shiftElig`, `eligReqKeys`, `eligMode`, `eligListed`, `categoryOf`, `inGroup`, `eligFor`, byte-identical and pinned by the suite) and reads `groupMembers` and `categories` instead of the retired grid; a live change to either re-fills the dropdown. **S-8** — Quick View subscribed to “the months the next seven days fall in” once, at first render, and never asked again, so a tab left open past a month end showed published, assigned days as blank; once a minute it asks again and re-subscribes only when the answer changes (driven with the clock pinned to 23:59 on the 25th and run past midnight: 1 April's D10 appears on its own). **N-14** — accepting a swap was audited, withdrawing it was not; nor was submitting a request, withdrawing one, or claiming an open shift. All four are audited now, in plain words (*requested a day off for … — “dentist”*), and only when the write happened: `txUpdateItem` reports whether it wrote, so withdrawing an already-decided request logs nothing. `staff46-test.mjs` 24/24; honesty on staff 45: 19 red. |
| **137** (staff unchanged) | `abc3c18` | 29 Aug 2026 | **RA-6 D-11, E-15 AND E-6 CLOSED.** **D-11** — the printed day/week sheet, the one artefact that leaves the application, carried no draft marking; a sheet printed on a Friday spanning into a draft month was indistinguishable from a final one. `printGather` now records which gathered months are drafts (a non-enumerable `__draft` on the map, so the older suites' plain maps still print unmarked), each such sheet says **DRAFT — NOT PUBLISHED** in its heading inside a dashed frame that survives a black-and-white printer, the page counts them, and the print cache keys on the state so publishing while the sheet is open clears the mark. **E-15** — `vacationDays()` rebuilt the whole map on every call, and the engine asks twice per candidate per shift per day when a *Not on vacation* rule is on: measured at 1.5 s of blocked main thread per Preview, growing with every completed auction phase. It is built once per delivery of the two auction documents — the listeners hand over fresh objects, so their identity is an exact cache key; asserted by identity. **E-6** — a *Fairness balance* rule could never fire at the moment of change: `ruleDiff` handed every rule a one-person pool, and fairness needs two, so it returned “nothing to check” in the cell editor, on approval, on swap approval and in Assign-by-hand while the Rules page showed it red. A rule marked `pool:true` is now checked against everyone it covers and only the new violations naming the person being changed are reported. **Claude's decision, for review:** the engine keeps its count-ordering and skips pool rules (`{noPool:true}`) — it already hands each shift to whoever has had it least, and rebuilding the fairness model per candidate would reopen E-15 — and the Engine card and the rule's blurb now SAY so instead of claiming it follows every rule. `build137-test.mjs` 17/17; honesty on 136: 11 red. |
| **136** (staff unchanged) | `abc3c18` | 29 Aug 2026 | **RA-6 D-15, D-4 / N-10 AND D-5 CLOSED — THE CHANGE FEED TELLS THE TRUTH.** **D-15** — `mutateCell` passed its before/after picture out of the transaction through three page-level variables, so two edits started in the same moment (Add, then the Off box without waiting — reachable from the modal) emitted two identical *“marked off”* entries and lost the added shift; the audit proved it live and so does the suite on the baseline. The picture is local to the call now. **D-4 / N-10** — *“Remove everything for X on this day”* wrote around the transactional writer with `mergeFields` + `deleteField`: no feed entry (the neighbouring *remove one shift* button produced one) and no protection against two admins editing the same day. It is the fourth cell operation, `clearCell`, inside the same transaction, emitting *removed Call 24* like any other removal; clearing an already-empty cell writes nothing. **D-5** — Auto-populate YEAR duplicated the engine's write loop and the copy dropped the feed — 728 day-documents, 727 assignments, 0 entries. `readMonth` now also reports `published`, and the year runner emits month by month, published months only, draft months silent, the days that landed before a refused write still announced. `build136-test.mjs` 18/18; honesty on 135: 9 red — the baseline writes exactly the two identical entries the audit found. |
| **135** (staff unchanged) | `abc3c18` | 29 Aug 2026 | **RA-6 E-3 AND E-4 / N-4 CLOSED.** **E-3** — the *Minimum rest* check sorted a person's shifts by start time and compared only neighbouring pairs, so a long shift stopped being considered the moment a shorter one started after it: *Long day* 08:00–22:00, *Short* 09:00–10:00, *Evening* 20:00–23:00 reported the two-hour Long/Evening overlap as clear — no conflict row, no grid mark, no warning. Each shift is now compared with every later one that could still be too close (sorted by start, the inner loop stops at the first that is far enough away), and the widened loop raises no false positive on three clear shifts. **E-4 / N-4** — three of the four callers of `assignmentWarnings` (request approval, swap approval, Assign-by-hand) got the shift's capacity ceiling for the **last day of the previous month**: the date came from the cell editor's global, `null` on those paths, and `Number(null)` is day 0. A weekend shift that legitimately takes four warned *“already staffed by 1 — the most it asks for is 1”* and a real over-fill on a weekday-capped shift went unwarned. The date is built from the function's own `ym` and `dd`, which also settles N-4 (a two-month Assign-by-hand range followed the month on screen). `build135-test.mjs` 14/14; honesty on 134: 6 red, with the audit's exact wrong sentence on the baseline. |
| **134** / staff **45** | `abc3c18` | 29 Aug 2026 | **RA-6 B-3 AND B-4 CLOSED — THE SCHEDULE-ONLY HALF OF STAGE 4.** **B-3** — the admin page built its approved-vacation map from the auction's completed phases plus the LIVE approvals document and never read a Phase-4 ROUND archive; the auction clears the live document at every round boundary and only *Complete Phase 4* merges the rounds, so from Round 2 until Phase 4 completed — most of the year — every Round-1 winner's week was invisible to the grid, the engine, the year filler, the reports and the feeds, and *“is on approved vacation this day”* stopped firing. The staff copy had the fix since build 36; only it was under test, which is how they diverged. ONE `vacationDaysFrom(vp, live, released)` now sits on both pages, byte-identical and pinned so; the staff view is released-only (C-2 / RA-H12 kept), the admin view every decided phase and round plus the live document. **B-4** — the live approvals document is gated on the AUCTION's admin list, deliberately independent of this site's; a Daily Schedule admin not on it was refused in the browser console only while the whole site treated everyone as available. The listener wrapper now hands the error on, and a banner under the page title says what is missing, what still shows, and whom to ask; a denied phases document gets the stronger *no vacation is shown anywhere* wording. `ra-fixes-36-76-test.mjs` re-anchored to the shared function and now executes the ADMIN copy too. `build134-test.mjs` 28/28; honesty on 133/44: 22 red — the baseline admin says a Round-1 winner is not on vacation. |
| staff **44** (admin unchanged) | `abc3c18` | 29 Aug 2026 | **RA-6 STAGE 3 CLOSED — N-9, D-3, D-2, N-17, V-17, V-21: A BLANK DAY SAYS WHY IT IS BLANK.** Three visibly different states on the staff page: **Not published** (amber, dashed) on a draft month, **Couldn't load** (red) when the month's listener fails, and a dot in the grids or *Nothing scheduled* in Quick View only on a month that really loaded and really has nothing there. **D-3** — Quick View answers per DAY from the day's own month, so the days in a draft next month (every month's last week — exactly where the audit caught AR's real Call 24 painted as blank) carry the pill instead of nothing; each day is tinted by its month. **D-2** — the Full Schedule panel has its own notice, not only Home. **V-17** — the legend names the states; a draft month is tinted amber, a failed one red. **N-9** is the tomorrow problem, sequenced ahead of any rules change as the audit asked: `watchMonth` FAILS CLOSED — nothing is handed out as published until both the month document and the days have been seen, and a denied listener reports `state:'error'` instead of leaving the draft flag at its initial value for ever, so tightening the rules can never open a draft by silencing one listener. **N-17** — the days-first race paints *Loading…*, never an empty month. The listener wrapper gained an optional error callback; the fake gained `__denyListenPath` and `__holdListenPath`. `build121-test.mjs` re-anchored from a pinned string to the invariant (`.published` is read in `watchMonth` and nowhere else). `staff44-test.mjs` 27/27; honesty on staff 43: 18 red, `leaked=true` — the audit's finding executed. |
| **133** / staff **43** | `6fa8fbb` | 29 Aug 2026 | **RA-6 R-4 CLOSED — THE APPROVAL QUEUE STOPS WRITING A SHIFT THE ADMIN DID NOT CHOOSE; AND R-5 / N-11 / S-10 CLOSED — ONE BROKEN SWAP ITEM STOPS BLANKING THREE PANELS ON TWO PAGES.** **R-4** is the one medium that put the *wrong shift* into the schedule and audited it as correct. A typed **work** request over a tag holding several shifts (*Oakland call = [Call 16, Call 24]*) gets a `<select>` on its queue row — but `renderRequests` rebuilds `#reqPending.innerHTML` **wholesale** on every snapshot and `rtPicker` emitted no `selected`, while `markDirty('requests')` fires from **three** listeners. So a colleague submitting an unrelated day-off request rebuilt the row underneath the admin and reset the picker to the **first** shift in the list. The audit executed exactly that: picked *Call 24*, read back *Call 16*, approved, and **Call 16 was assigned and audited as a correct approval**, with nothing on screen saying anything had changed. The choice now lives in module state keyed by request id (`_reqPick`), is written back into the rebuilt select as a real `selected`, and is what `decideReq` reads — through `reqPickOf(id)`, which asks the **live control first** (still the truth while the row stands) and falls back to module state (the truth once the row has been thrown away). The half-typed **deny reason** is kept the same way, by the same pair of handlers, because it is the same defect. The first option is now a **prompt**, not a shift: an untouched picker means *“not chosen yet”*, and Approve **refuses** with a plain toast — *“Pick which shift to give them first — nothing was approved.”* — instead of guessing the first one. The confirmation dialog **names the shift** it is about to write, and so does the audit entry. Deliberately **not** *“refuse to re-render while a decision is in progress”*: **N-20** shows a refused render *discards* its update rather than deferring it, so that shape would have silently dropped the colleague's new request to save the admin's selection. **R-5 / N-11 / S-10 are one root cause and get one fix.** A 3-way item is `participants:[a,b,c]` with `legs:[l,l,l]`, and three renderers destructured it without ever asking. One malformed item — writable **today** by any registered physician (R-3) — threw inside the `.map` **before** the `innerHTML` assignment, so the admin **Swaps** panel froze on stale content while the badge went on counting *(3)*; on **Today** the same throw escaped through `recombineAudit`'s **direct** call to `renderDash`, outside `_flushRender`'s `try/catch`, aborting the audit listener mid-flight and leaving the coverage figure stuck on **“—”**; and on the **staff** page it threw inside the badge count and the list filter, taking the swap-partner dropdowns, the request shift picker, Quick View and Open Shifts down with it, permanently and with nothing explaining it. One shared **`isSwapItemShape(x)`**, asked in one place per page and used by `renderSwaps` (all three panels), `renderDash` and the staff `renderMySwaps`. A bad item is **not hidden** from the admin — it renders as a flagged **“⚠️ unreadable swap — id …”** row beside the good ones, because a swap the page cannot read is exactly the thing the admin needs to know exists; on the staff page, which has nothing to offer about it, it is **skipped with a console warning** naming the id. Either way the rest of the list still renders and the badge and the list finally agree. **`build133-test.mjs`** drives all of it in a real browser and is registered at the **top** of `sched/run-all.mjs`: pick the second shift, seed an unrelated request **through the fake** to force the re-render, press Approve → the day cell holds **Call 24**, the dialog said Call 24 and the log says Call 24 and never Call 16; with no pick → the toast, an untouched day, a still-pending request and **no audit entry**; and good / **malformed** / good in `ts` order → three rows on Swaps, three on Today with coverage at a number, two on the staff list with `sw_bad` named in a `console.warn`, and no page error anywhere. Honesty run against the 132/42 baselines (`PRE_ADMIN=/tmp/admin-132.html` md5 `38cef8d32416d1c3f2edca182615038e`, `PRE_STAFF=/tmp/staff-42.html` md5 `a797933785dc0b38a1e6676b1f6f95ee`, on top of `3810e83`): **34 of 50 assertions red, exit 1** — verbatim *“added Call 16”*, *“value "C16"”*, coverage *“—”*, `collection listener threw TypeError: Cannot read properties of undefined (reading 'map')` and staff `pageerror: Cannot read properties of undefined (reading 'includes')`. **§3 rule 12: two suites re-anchored, neither weakened.** `build80-test.mjs` asserted the deny reason is read BEFORE the transaction by looking for the literal `document.getElementById('denyWhy_'+id)` inside `decideReq`; that read now goes through `reqWhyOf(id)`, so the line accepts either spelling and gained a second assertion that `reqWhyOf` really reads the admin's own `#denyWhy_` box. `build125-test.mjs` asserted the work-request picker has exactly **2** options; it has 3 now — the prompt plus both shifts — so it asks the question **by value** (does it offer `C16` *and* `C24`?), which is stricter than the length it replaced, and gained an assertion that the leading option is the empty prompt. **Schedule battery: 84 of 84 suites EXECUTED, 0 failed, exit 0.** Auction battery on unchanged auction bytes: **55 of 55, 2019 assertions, exit 0.** All six inline scripts across both pages `node --check` clean. |
| **132** / staff **42** | `6fa8fbb` | 29 Aug 2026 | **RA-6 V-1, V-2 AND V-14 CLOSED — THE GRID SCROLLS INSIDE ITS OWN BOX, AND THE PAGE STOPS BEING DRAGGED SIDEWAYS.** `.gridwrap` was `overflow:visible`, so the admin Schedule Grid pushed the **document** out: the sidebar, the page title and the month toolbar all scrolled away with the table and the header read *“…ing”*. Measured on the 131 bytes against the audit's own seeded catalog at a 1440 viewport — People/Detail **3136px**, People/Compact 1698, Shifts/Compact 1700 and **Shifts/Detail 10 937px**, about 7.6 screens, with no in-place scroll container, on the density that is the **default**. The Schedule Grid now sits in `.gridfade > .gridwrap.gridscroll#gridWrap`: `overflow-x:scroll` (not `auto`, so the thin bar is **always drawn** — V-14's “no affordance that anything scrolls” answered by the control itself) plus a max-height, because **sticky is measured against the nearest scrollport** and a box that never scrolls vertically would have killed build 67's sticky day header silently while the page still looked fixed. `gridFadeSync()` sizes the box to end at the bottom of the screen and toggles one class for the right-edge fade, from a passive listener. After: **1440 at 1440 and 1100 at 1100 in every view × density**, with the table 2896px inside a 1160px box — contained, not shrunk. **The name column** (person or shift) is sticky-left over an **opaque** `--s1`/`--s2` ground with the edge drawn as a shadow rather than a collapsed border, so a scrolled cell can never show through the name you are reading. **Shifts/Detail's 10 937px** was one nowrap line of spelled-out names: the names **wrap** now inside a 132px column cap (`table.grid.gv-shifts td.cell`), **10 697px → 1600px** in its box — nothing hidden, nothing ellipsised, and every cell already carries the whole list in its `title`. **V-2, the staff page.** The Full Schedule wrapper always had `overflow-x:auto` and it was never the problem: `.app` is a **column** flex box at phone width with `align-items:flex-start`, so `.content` was sized to the table's max-content and took the page with it — **1500px against 390**, “Jump to” clipped off the screen at x=673. `.content` is stretched to the screen in the ≤700px block, the table gets the same `.gridscroll`/`.gridfade` box and a sticky name column, and Home's **7-column Quick View** (which pushed the page 71px out on its own) wraps to `auto-fill minmax(88px,1fr)`. Two more found by measuring rather than by reading: the **month calendar** under it overflowed its card by 38px (seven `1fr` tracks are each at least min-content — they may shrink now, because a week is seven columns or it is not a week), and the My Requests **shift picker** was 9px wider than the screen on its own (`.sel,.inp{max-width:100%}`). After: **390 at 390 on Home, Full Schedule and My Requests**; 555 at 555; 1440 unchanged. **NOT in this build:** the audit's *real phone layout for Full Schedule* (a day-per-row list rather than a 37×31 matrix, M-L) — this is the containment it asked for first; the legend and colour work (V-7/V-20); and the empty-state work (V-17/V-21). **Gates:** `build132-test.mjs`, **77 assertions, browser-driven, both pages**, registered at the **top** of `sched/run-all.mjs` in the same turn. At **1440×900, 1100×800 and 390×844**, for both admin views × both densities: the document never scrolls sideways; the box is genuinely narrower than the table in it; the first column is `sticky`/`left:0` over a **computed-opaque** background; the day header is still `sticky`/`top:0`; the sidebar's bounding box is **byte-identical** after the box is scrolled 600px right; the fade is ON at day 1 and OFF at the last day; and every name in the widest view is also in its cell title. **Honesty: 28 of 77 FAIL on the 131/41 bytes** (`/tmp/admin-131.html` md5 `cf3abe1616f1c9d8ff63efa3766136c0`, `/tmp/staff-41.html` md5 `f6505fd7bd722fd16d1e5345084b9dc1`, on top of `3810e83`), exit 1 — the baselines report *“scrollWidth 3127 vs viewport 1440 → OVER BY 1687px, pushed out by table#schedGrid”*, *“5377 vs 1440 → OVER BY 3937px”*, *“#gridWrap false, #gridFade false”*, *“staff full @390 … 1500 vs 390 → OVER BY 1110px”* and the staff name column as *“static / left auto / rgba(0, 0, 0, 0)”*. **§3 rule 12: `build96-test.mjs` re-anchored** — its “the shift list has no fixed max-height” asked `document.querySelector('.gridwrap')`, which is the **Schedule Grid's** wrap three panels away, not the catalog's; it now reads the wrap the catalog's own sticky column labels sit in, which is stricter than what it replaced. `renderGrid`'s new `gridFadeSync()` call is guarded with `typeof`, because **build69 and build70 lift that function out of the file** and evaluate it alone against stubs. **Schedule battery: 83 of 83 suites EXECUTED, 0 failed, exit 0.** Auction battery on unchanged auction bytes: **55 of 55, 2019 assertions, exit 0.** All six inline scripts across both pages `node --check` clean. |
| **131** / staff **41** | `6fa8fbb` | 29 Aug 2026 | **RA-6 S-2 CLOSED — A HOSTILE SWAP OR REQUEST ID CAN NO LONGER RUN CODE IN A COLLEAGUE'S BROWSER.** Nine buttons on the staff page were built by pasting an id straight into a JavaScript call inside an HTML attribute — `onclick="acceptSwap('${esc(s.id)}')"`. `esc()` protects HTML *text*, not a JavaScript *string*: it turns `'` into `&#39;`, and the parser turns that back into a real `'` **before** the handler is compiled. So an id carrying a quote closed the string and everything after it ran as code, in the reader's own signed-in session, the moment they pressed the button whose whole purpose is to be pressed. The id is a free-form **field** any registered user can write — not a Firestore document key — so no key rule constrained it. **The fix already existed one directory over:** the admin's `jsArg` (`v => esc(JSON.stringify(v))`, build 90), copied into the staff page **byte-for-byte** and wrapped round all nine interpolations (`acceptSwap`, `declineSwap`, `withdrawSwap` ×2, `acceptSwap3`, `declineSwap3`, `withdrawRequest` ×2, `applyOpen`) — the report said seven; grepping every `on*="…${` and `on*="…\'` on the page found nine. The **admin** page was swept for the same class and five more were wrapped, all carrying staff-written ids: `decideReq` ×2, `decideSwap` ×2, `removeOpen`. Admin sites interpolating admin-authored values (shift ids, initials, family keys) were left as they are — a different class, and out of this build's scope. **S-12** — `.btn-success` and `.btn-danger` were used on the staff page and **defined nowhere**, so ✓ Accept and ✗ Decline on a swap rendered as two identical grey buttons; the two rules are copied from `admin/index.html`, measured green-on-white vs red-on-pink. **V-4** — a Shifts-view cell nobody is on read **`/3`**, a fraction with no top half, and those are exactly the cells drawn with the red shortfall ring; it now reads **`0/3`**, and a cell with no staffing rule still shows nothing. **V-9** — the note ⚠ was appended to the whole state expression, so *“nothing standing”* — the phrase that means “no problems here” — carried a warning triangle, contradicting the rule beside it and the report below; the triangle now belongs to *“N standing”* only, where it still is. **V-3** — the Shift Catalog's read-mode header ran its words together (`SHIFTKIND TIMESSITE STAFFINGCAP ROLE…`) and bunched away from the rows it labels. The cause: the class `.catheadrow` was **used on the markup and defined nowhere**, its layout living only in the element's own `style="…display:flex…"` — and `renderCatalog` shows the row with `hr.style.display=''`, which *deletes* that inline property. From the first render on it was a plain block, min-widths and gaps gone. The class is now defined (same flex, same 10px gap as `.rowline`), so every label sits over the column it names — **measured at 0px offset against a real catalog row**, against **321px** on 130. **E-8** — the Holidays year dropdown carries `onchange="renderHolidays()"` and `renderHolidays` was never on `window`; picking a year threw `renderHolidays is not defined` and the list never repainted. Exposed. **N-1** — the Calendar Feeds **Build** button calls `schedToast` from inside its inline `.then`, and `schedToast` was module-scope only: the feed was written and the admin got a console error instead of *“Feed rebuilt.”* Exposed. Both were found by a **rendered-DOM** sweep, not a source sweep. **V-16** — neither page showed its build number anywhere, so no problem report could name the build it was seen on. A quiet monospace **“build 131” / “build 41”** now sits beside the Sign out control on both pages, read from `var BUILD` — **not** a second literal that would drift the first time only one of them was bumped. **NOT in this build:** V-1/V-2, the grid containment work — that is the next build. **Gates:** `build131-test.mjs`, **87 assertions, browser-driven, both pages**, registered at the **top** of `sched/run-all.mjs` in the same turn. It seeds swap, request and open-shift items whose `id` **field** is `…');window.__pwned=1;document.body.insertAdjacentHTML('beforeend','<i id=pwn3d></i>');//</script>"`, renders each page, and **clicks the real rendered button at all fourteen sites** with the handler shimmed to record its argument: nothing runs, nothing is injected, and each handler still receives the **whole** id. **Honesty: 51 of 87 FAIL on the 3810e83 bytes** (`/tmp/admin-130.html` md5 `e2b4d7bc1c57e1de1afeba27c9025cc2`, `/tmp/staff-40.html` md5 `2fdc09cf38f051c8e2d24284df29861a`), exit 1 — on the baselines `window.__pwned` is **set** and `#pwn3d` is **injected** at every site, the handler receives only `sw_to`, the two swap buttons measure **identical** white-on-white, the cell reads `/3`, “nothing standing” carries its ⚠, the header lays out as `display: block` with a **0px** gap between SHIFT and KIND, and the two dead controls raise `renderHolidays is not defined` and `schedToast is not defined` as live page errors. **Schedule battery: 82 of 82 suites EXECUTED, 0 failed, exit 0.** Auction battery on unchanged auction bytes: exit 0. All six inline scripts across both pages `node --check` clean. |
| **130** (staff unchanged) | `3810e83` | 29 Aug 2026 | **RA-6 R-1 CLOSED — A SAME-DATE 3-WAY ROTATION NO LONGER HANDS ALL THREE SHIFTS TO ONE PERSON; AND R-2 CLOSED — A SWAP THAT CAN NO LONGER HAPPEN THE WAY IT WAS AGREED IS REFUSED WHOLE.** The most natural 3-way there is — *“on Saturday I take your OR, you take Bea's D10, Bea takes my D”* — was applied **one leg at a time against an in-memory copy of that day**, and `setCell` wrote back into the very object the next leg read from. So leg 2 picked up what leg 1 had just deposited and leg 3 picked up both. The audit executed it three times out of three: seed `2026-03-21 = {AR:[D], BO:[D10], CD:[OR]}`, approve — **`{"AR":["OR","D10","D"], "BO":[], "CD":[]}`**, the day two people short, the swap ✅ approved and the audit entry saying the rotation worked. **Worse, the confirmation was computed by different code from the write** (`swapWarnings` only ever *subtracts* from its simulated day, so it showed the correct one-shift-each handoff — a dialog describing an outcome that never happened). **ONE READING, ONE BEFORE-PICTURE.** New `swapLegs(sw)` is now the only place a swap is turned into handoffs (a 2-way is two of them pointing opposite ways; a 3-way is A→B→C→A), used by the warning check, the confirmation and the transaction alike. New **`swapOutcome(legs, dayCache)`** works out the finished cells **once, from ONE before-picture of each date**: every leg reads that same picture, each person loses what they hand over and gains what comes to them **in the same step**, and nothing is ever written back into the snapshot. A rotation whose three legs fall on one date therefore comes out as a rotation — **`AR:[OR] BO:[D] CD:[D10]`**, one shift each. Legs on **different** dates are byte-for-byte what 129 did. **THE CONFIRMATION IS THE WRITE, IN WORDS.** Approving now always asks, and the box is built by `swapOutcomeText` from that same `swapOutcome` value and from nothing else — *“What moves: 2026-03-28: Dr. Alan Reyes hands D to Dr. Bea Okafor … Afterwards each person holds: Dr. Alan Reyes: OR extra …”* — so the sentence the admin reads and the cells that land cannot describe different outcomes. Warnings (§51/build 61) are appended below it, unchanged, override still recorded. **R-2 — NOBODY EVER ASKED WHETHER THE SHIFT WAS STILL THERE.** A swap stores only a date and a part of day, and until now a giver holding nothing was silently skipped: a 2-way became a **one-way gift** (the audit executed `{"BO":[],"AR":["D10"]}` — B lost her shift, A gave nothing back) and a 3-way's empty leg **broke the loop open**, with **zero dialogs and zero toasts** in both runs. Now `swapOutcome` reports every giver who no longer holds anything of that kind, and **inside the transaction, on the freshest data there is**, `decideSwapTx` refuses the whole swap when some parties still hold what they agreed to hand over and others do not: **nothing is written, the swap stays `pending_admin`**, a toast names them — *“Nothing was approved — this swap can no longer happen the way it was agreed: Dr. Alan Reyes no longer has a day shift on 2026-03-24 to hand over. It is still waiting for you.”* — and the refusal is written to the **audit log**, because “the site said no” has to be as visible there as “the site said yes”. (A swap where **nobody** still holds anything moves nothing at all; that is a no-op and is recorded as decided, exactly as before — it is what `build68-test.mjs` drives.) There is **no `needs attention` status on this site** — checked — so “still waiting for you” is the pending queue, where the admin already looks. **NOT in this build:** R-5 / S-10 / N-11, the malformed-swap-item shape check — it belongs in `renderSwaps`, `renderDash` and the staff filter, none of which this build touches, so it is left where it is (`swapLegs` is itself shape-safe: a 3-way with no `participants` or no `legs` yields no legs and moves nothing); and R-4, the requests queue's shift picker. **Gates:** `build130-test.mjs`, **58 assertions, browser-driven**, registered at the top of the battery in the same turn — the audit's own Saturday is seeded and approved, and every party is asserted to hold **exactly one** shift; the same-date 2-way, the three-different-dates rotation, the one-sided 2-way and the broken-cycle 3-way are each driven; the refusal is measured as **zero day writes, zero change-feed entries, the item unwritten, the status still `pending_admin`** and one audit line; and the confirmation dialog's own text is read and then checked against the cells that landed. **Honesty: 34 of 58 FAIL on the 129 bytes** (`/tmp/admin-129.html`, md5 `09e112ae7c991a5544aeabf288ac7314`), exit 1 — on 129 the same-date rotation returns the report's **`AR ["OR","D10","D"] · BO [] · CD []`** to the letter, the one-sided swap commits as approved with no toast at all, and a clean approval raises **no confirmation dialog whatsoever**. **§3 rule 12: `build123-test.mjs` re-anchored** — its 2-way seeded `AR:[D]`, `BO:[C24]` and asserted the resulting **one-way gift** as correct; BO gains a `D10` so the exchange is real, and the suite still proves what it was written to prove (atomicity, and that a DAY swap leaves a CALL alone: BO ends `["C24","D"]`). **Schedule battery: 81 of 81 suites EXECUTED, 0 failed, exit 0.** Auction battery on unchanged auction bytes: exit 0. All three inline scripts `node --check` clean. |
| **129** (staff unchanged) | `3810e83` | 29 Aug 2026 | **RA-6 E-7 CLOSED — A PREVIEW PLAN GOES STALE, VISIBLY AND FUNCTIONALLY, THE MOMENT ANYTHING IT WAS WORKED OUT FROM CHANGES.** Nothing invalidated a plan except moving month or applying it. The audit executed the whole thing: Preview August → *"496 assignments"*; add a **Serious** *No call at all* rule; `nav('rules')`; `nav('engine')` — the panel was **byte-for-byte the same**, Apply was **still enabled**, and pressing it wrote **62 day-documents, every one containing the barred shift**. Pass 1 said the panel *looked* stale; it in fact **applied a policy the admin had already revoked**, and it is the multiplier that makes E-1 reachable by one admin working alone (Preview → fix a cell in the grid → back → Apply). **ONE PATH:** `_enginePlanStale(reason)` sets a flag carrying the plan's month and **a sentence in the owner's language** — *"Rules changed since this preview"*, *"The schedule changed since this preview"*, *"You moved to another month since this preview"*, *"Quotas / Groups or categories / Requests / Vacation / The shift list / The call fairness baseline changed since this preview"*. **The first reason wins** — it names what actually killed the plan, and a later change does not rewrite the sentence being read. **THREE THINGS HAPPEN AT ONCE:** `renderEngine` puts an amber banner above the plan carrying that sentence and a **Preview again** button; the **Apply button is disabled** with the reason as its tooltip; and **`engineApply` itself refuses** with a toast, because the button is not the guard — Apply is reachable from a queued click and from anything that calls it. **WIRED FROM THE LISTENERS THAT ALREADY OWN EACH INPUT** (rules · tags, which are the shift lists rules and requests point at · quotas · the shift catalog, which is also where per-shift ELIGIBILITY has lived since 89 · groups · group members · categories · requests · request types · vacation phases · vacation approvals · the call-fairness baseline · the month document · the month's day documents · and `setMonth` itself). The stale path calls **`markDirty('engine')`**, so build 66's dirty-flag design does the rest: a hidden Engine panel does not rebuild on every snapshot, and the flag set by the listener meets the repaint that `nav()` performs on entry — **the banner is there when you walk back in.** **TWO THINGS DELIBERATELY DO NOT MARK STALE.** The **first delivery** of each month listener is the month ARRIVING — it is what completes the load (128), not somebody changing the schedule — so each subscription carries its own `daysSeen` / `docSeen`; and every later delivery is compared against what it replaces, so **a snapshot that changes nothing is not a change**. And the engine's **own Apply writes** never invalidate their own plan: the plan is now discarded **before** `engineWrite`, not after, because the day-documents echo back through the month listener while that await is still running, and `_engineApplying` says the same thing a second way. **Gates:** `build129-test.mjs`, **71 assertions, browser-driven**, registered at the top of the battery in the same turn — the audit's reproduction is executed with the Engine panel **hidden** when the rule lands, and the refusal is measured as **zero day-documents and zero documents carrying the forbidden shift**; the negative control re-delivers the day document and the month document **byte for byte** and asserts NO banner; a cell edited by another writer, a month move and a move back are each driven; and one whole clean Apply is run end to end to prove the engine does not refuse itself. **§3 rule 12: `build127-test.mjs` re-anchored** — its §① used to press Apply after editing a cell between Preview and Apply, which 129 now (correctly) refuses; it asserts the refusal instead, and drives E-1's additive write and the day-off skip through **`writePlannedDay` itself**, the transactional writer Apply calls. **Honesty: 30 of 71 FAIL on the 128 bytes** (`/tmp/admin-128.html`, md5 `c7e16ef0718090ea1524077740c4675b`), exit 1 — on 128 the stale plan applies and writes **31 day-documents, every one carrying the Call 24 the new rule forbids**, which is the report's reproduction to the letter. Re-anchored `build127` still **79 of 79** on 129 and **49 of 79 RED** on its own 126 baseline. **Schedule battery: 80 of 80 suites EXECUTED, 0 failed, exit 0.** Auction battery on unchanged auction bytes: **55 suites / 2,019 assertions, exit 0**. All three inline scripts `node --check` clean. |
| **128** (staff unchanged) | `3810e83` | 29 Aug 2026 | **RA-6 E-2 CLOSED — THE ENGINE WILL NOT PLAN A MONTH THAT HAS NOT ARRIVED; AND N-2 CLOSED — THE PLAN FINALLY HAS WORDS FOR AN OVER-FILL.** `enginePreview` guarded only `monthLockRefuse()`, which tests the OLD STORAGE FORMAT and nothing else. `monthLoaded` already existed and was already respected by the day board, but not here — and `setMonth` empties `monthData` and drops `monthLoaded` **synchronously** while both of its listeners are asynchronous. The ◀ ▶ buttons sit **on the Engine card, beside Preview**, so pressing ▶ and then Preview in the same moment planned against a completely blank month: everybody looked free every day. The audit executed exactly that on a May 2026 that was already built by hand to 100% and was offered **"496 assignments"**; applying it destroyed four people's assignments on day 05 alone and left **two people on Call 24, whose ceiling is 1**. **Now both halves of the load must have arrived AND belong to the month on screen:** `monthLoadedYM` is stamped by `recombine()` and `monthDocSeenYM` by the month-document listener, both cleared by `setMonth` (so returning to a month you have already visited does not read as "already loaded" while its data has just been emptied), and `monthReady()` asks all three questions at once. `enginePreview` and `autoPopulate` refuse through one `monthLoadRefuse()`, the same shape `monthLockRefuse` has always used: a toast reading ***"Still loading June 2026 — try again in a moment"***, no plan, nothing written. The **Preview button is disabled** while the month is in flight with that same sentence as its tooltip, and the plan panel carries it in words (`#engLoading`); the month listeners turn it all back on by themselves, with no second press. **Apply's month guard was verified and KEPT** — a plan is only ever applied to the month it was built for, and since 128 a plan can only exist for a month that had finished loading, so "the plan's month" and "the loaded month" are one question. **N-2 — the over-fill had no name anywhere in the engine.** `plan.filled`, `planSummary`, the Apply confirmation and the conflict report could all say *could not be filled* and *stopped by a rule*; not one of them could say **too many people on this shift** (`quotaConflicts` covers quotas only). New `overCeilingOn()` asks, for **every day the plan touched and every shift held on it**, whether the day would end up with more people on that shift than the shift asks for — counting what the cell holds at planning time **plus what the plan adds**, because since 127 the write appends. It asks the **same** ceiling question `fillDay` fills by (`shiftCeilingOn` → `staffingOn`), called with the date of the day being planned. The answer is a list on the plan panel (*"Over the ceiling — more people on a shift than that shift asks for"*, `#engOver`), a count in the plan summary in **the same words the Coverage page already uses** (§19), a named section in the **Apply confirmation**, and the same section in Auto-populate's completion report — each line reading e.g. *"2026-05-04 Call 24: 2 people would be on this shift and the most it asks for is 1 — the plan adds none here; the day was already over before this run"*. It **reports, it does not block** (§4). **NOT in this build:** E-4 / E-14 (`ddDate()` ignores the day AND the month it is handed, so other ceiling readers answer for the wrong date) — the new call site simply passes the correct date and the defect is left where it is; and **E-7**, the stale preview plan, which is 129. `autoPopulateYear` is deliberately NOT gated: it reads each month with a one-time `readMonth()` rather than the on-screen listener, so `monthLoaded` is not its question. **Gates:** `build128-test.mjs`, **55 assertions, browser-driven**, registered at the top of the battery in the same turn — the audit's own reproduction is executed (`moveMonth(1)` and `enginePreview()` in ONE tick, with the panel repainted the way entering it repaints it), then the button state, the tooltip, the panel notice, the toast and the ABSENCE of an Apply button are all read at that instant; the listener is then proven to re-enable the button with no admin action, and Preview proven to plan once the month is here; the over-fill is driven on a day seeded with two people on Call 24 and read back out of the panel, the summary and the confirm dialog's own message; Auto-populate is raced the same way and asserted to raise no confirmation and write nothing. **Honesty: 27 of 55 FAIL on the 127 bytes** (`/tmp/admin-127.html`, md5 `62bc438cba17f164fe4ffca658914209`), exit 1 — on 127 the raced Preview offers a plan for a month it never read, the raced Auto-populate asks its question and **writes 30 day-documents**, and no over-ceiling list, summary count or confirmation line exists at all. **Schedule battery: 79 of 79 suites EXECUTED, 0 failed, exit 0.** Auction battery on unchanged auction bytes: **exit 0**. All three inline scripts `node --check` clean. |
| **127** (staff unchanged) | `3810e83` | 29 Aug 2026 | **RA-6 E-1 CLOSED — THE ENGINE'S APPLY ADDS TO A CELL; IT NEVER REPLACES ONE, AND A DAY OFF BEATS A STALE PLAN.** `engineWrite` wrote `mergeFields(dayRef(ym,dd), plan.byDay[dd])`, whose keys are INITIALS and never dotted, so `updateDoc` replaced each person's WHOLE day cell with the copy the plan had built from a read taken before the multi-second fairness pass. Anything added to that cell between Preview and Apply — another admin, an approved request, or the same admin fixing the grid — was overwritten, and the written cell always carried `off:false`, so an approved day off was cleared. **Now each planned day is ONE TRANSACTION** (`writePlannedDay`) that re-reads the day document inside the transaction and APPENDS only the shifts the plan assigned, carrying every held entry forward: the read-then-patch shape `mutateCell`'s own header names the old write as "exactly that hole". `fillDay` records WHICH shift ids it added (`plan.addsByDay`) rather than only that a cell changed, because the planned cell is a stale snapshot by the time Apply runs. **AND A DAY OFF MARKED SINCE PREVIEW WINS:** `fillDay`'s `free` excludes anyone already off in the snapshot, so `off:true` at write time is a decision made *after* the plan was built — that person is **SKIPPED for the day** and the shift is not placed beside their day off. **The skip is never swallowed:** `engineWrite` returns a `skipped` list (`{dk, u, shifts, reason:'marked off since preview'}`), the success toast reads *"Applied — N assignments made. M skipped — marked off since preview."*, the plan panel keeps an `#engSkip` notice naming each person, day and shift through the one `skipLine()` (§19), the audit entry carries the count, and `autoPopulate` / `autoPopulateYear` carry it into their completion reports. **BATCHING: one transaction per DAY, not per cell** — a day is the unit the plan was already grouped in and the unit `mutateCell` scopes to (§47); per cell is the identical shape at up to thirty times the transactions. `autoPopulate` inherits it; **`autoPopulateYear` had its OWN copy of the whole-cell patch** and now calls the same writer. **E-11 / N-5:** a failed Apply was silent AND partial — days written, no toast, no audit entry, no change feed, one unhandled rejection. The write loop now stops on a refusal and REPORTS it: a toast naming the failure and the count, a plan-panel notice (`#engFail`) saying how many days landed and which day failed, an `engine.month.partial` audit entry recording both, and change-feed entries for the days that DID land (built from what was actually written, not re-derived from the plan's timestamps). `autoPopulate` and the year runner report the same way. **N-3:** the Engine card's *"Existing assignments are kept; nothing is ever removed"*, the Apply confirmation's *"Existing assignments are kept."* and the `[M-20]` comment claiming the patch protected against a concurrent edit are all replaced with what the write now actually does, skip included. **NOT in this build:** E-7 (a preview plan is still not invalidated when the rules change) and E-2 (Preview still does not wait for `monthLoaded`) — both are named in the audit as the multipliers that make E-1 reachable, and both are their own change. **Gates:** `build127-test.mjs`, **74 assertions, browser-driven**, registered at the top of the battery in the same turn — one day carries TWO cells the plan touches, one marked off after Preview (asserted to keep its day off, keep the manual shift and receive NOTHING) and one not (asserted to receive the planned shift beside the manual one), with the skip then read back out of the toast, the panel and the audit entry; the partial run is driven by a denied write on day 17 of 30 with `engineApply()` pressed exactly as the button presses it (no catch attached), so the unhandled rejection is a real measurement; the copy is read from the laid-out DOM and from the confirm dialog's own message, plus a §3 rule 10 source pass that proves the stripper kept 74% of the page first. **Honesty: 42 of 74 FAIL on the pushed 126 (explicit SHA `c73fbde`), exit 1.** **Schedule battery: 78 of 78 suites EXECUTED, 0 failed, 0 skipped, exit 0.** Auction battery on unchanged auction bytes: **55 suites / 2,019 assertions, exit 0**. All three inline scripts `node --check` clean. |
| **126** (staff unchanged) | `c73fbde` | 28 Aug 2026 | **THE ENGINE HONOURS APPROVED REQUESTS.** With request types on the site (125), the engine had to read them or the list was decoration. **A request the admin has APPROVED is a decision, and the engine keeps it:** an approved **avoid** keeps the person off every shift in that list that day — even when they are the only one free, in which case the slot is reported unfillable rather than filled against the decision; an approved **work** request puts the person first in line for any shift in its list that day, strongest first (MAX · HIGH · LOW · none), and **a person who asked for a different shift today is left free for it** — they go to the back of the line for everything else, so the shift they asked for finds them; an approved **available** goes ahead of the fairness order. **Pending requests are not read** — they are not decisions yet. **A rule still beats a request** (a MAX asker who is post-call is skipped for the next candidate). The legacy kinds map the same way: *nocall* is an avoid over the Overnight call tag, *shift* is a work request over that one shift; a weekend request covers its two days. `requestIndex()` is built once per plan and once per year run; the plan summary counts *placed on request*; the Engine page says requests come first. **Gates:** `build126-test.mjs`, **24 assertions, browser-driven** — `fillDay` driven with a seeded set of approved and pending requests of every kind, the same days filled with and without the requests in hand to prove the requests were the cause. **Honesty: 18 of 24 FAIL on the 125 bytes.** |
| **125 / staff 40** | `c73fbde` | 28 Aug 2026 | **REQUEST TYPES — §41 and `design/REQUEST-TYPES.md`, designed 16 Aug and never built.** *"Not all shifts can be requested, that's why i want just these."* **THE MODEL IS THE DESIGN'S, unchanged:** a shift list is a tag (build 51's), and a request type is `{label, kind, tag, strength, span, note, order, active}` in `dailysched/requestTypes` — five kinds (work · avoid · off · absence · available) and no sixth; **strength is a FIELD** shown as [MAX]/[HIGH]/[LOW], never three entries with the word inside the label; *Use PTO if off* is a modifier and is not an entry, so the standard list is **26**, not the 27 the screenshot counted. **ADMIN:** a *Request Types* page under Setup, after Rules — add by label and kind; **Add the standard list** loads the 26 in his words (*No Call* already pointing at the Overnight call tag); each row edits label, kind, shift list (amber until picked), strength, weekend span, offered; a row says *ready* or *not offered yet* and why. **STAFF (40):** the picker reads the list, grouped in five headings (Work something · Avoid something · Time off · Absence · Availability) plus Other; **while the list is empty the four built-in choices stand**, so nothing changes the day this ships; a work/avoid type with no shift list is not offered — that decision is the admin's; a request carries its meaning (`rtId, kind, tag, strength, span, label`). **THE QUEUE** shows label, strength and what it points at; **a work request over a list of several shifts gets a picker on its row — the admin decides which one**; a work request over an empty list stays pending and says so. **APPROVAL APPLIES EACH KIND:** work → the one shift, through the same warnings as the cell editor (§51); off and absence → OFF on every day covered (a weekend is its Saturday and Sunday, Sunday-anchored like everything here); avoid → remove anything held from that list that day, exactly as *No call* always has, and the engine will not give them one (126); available → recorded. **Gates:** `build125-test.mjs`, **36 assertions, browser-driven on both pages** — each page seeded with its own copy, since each has its own fake store. **Honesty: 30 of 36 FAIL on admin 124 / staff 39.** |
| **124** (staff unchanged) | `c73fbde` | 28 Aug 2026 | **IN-GRID RULE MARKS — §90's "in-grid violation badges" — and the report's click goes to the day.** The Schedule Grid now computes the month's standing violations once per render from **the same `checkMonth` the Rules page uses** (§19 — one checker, two views), marks every cell that carries one with an 8px corner dot in the severity's colour (the report's three: red, amber, grey), names each rule and its reason in the cell's tooltip, and counts them in a chip beside the month state that opens the report. A rule changing re-marks the grid (the rules listener now dirties the grid too). And `design/RULES.md`'s *"a click goes to that day cell"* is real: a report row is a pointer with a tooltip, and clicking it switches to the grid, moves the month if it must, and flashes the day — through `jumpToDate`, which already existed for the date picker. **Gates:** `build124-test.mjs`, **20 assertions, browser-driven** — the mark is MEASURED (an 8px `rgb(185,28,28)` pseudo-element on a positioned cell), the tooltip names both rules on a cell that breaks two, the colour follows a severity change, every mark disappears when every rule is off, and the report row is clicked. **Honesty: 13 of 20 FAIL on the 123 bytes.** **Battery on the 123 bytes: 73 of 74 green** — the one red was the isolation guard's POSITIVE CONTROL pinning `txUpdateItem(swapItemsCol…)`, a mechanism 123 replaced; re-anchored to the invariant (the admin still decides swaps on its own per-item collection, now inside `decideSwapTx`) and green; the cardinal-rule guards themselves were untouched. Clean run on these bytes: see HANDOFF. |
| **123** (staff unchanged) | `c73fbde` | 28 Aug 2026 | **DEFECT 3 CLOSED — a swap's status flip and its schedule legs are ONE transaction.** Since 61 the flip was one transaction and each leg a follow-on `mutateCell` — up to six for a 3-way — so a crash between them left *approved* with only some of it moved, surfaced by a toast and an audit marker rather than prevented (the code said so itself in a standing comment). `decideSwapTx` now reads the swap item (item doc, or the legacy list), every month document and every day document the legs touch — once each, reads before writes as Firestore requires — refuses an unconverted month inside the transaction, computes every cell in memory in the same order as before (2-way: an exchange of each side's shifts of that kind; 3-way: sequential one-directional handoffs), then commits the status and the cells together. **Either everything moves or nothing does and the swap stays pending**; a failed commit is a toast, never a half-state. A second click on an approved swap writes nothing and reverses nothing. The change feed (122) gets one entry per party on a published month. `txUpdateItem` is no longer used for swaps. **Gates:** `build123-test.mjs`, **33 assertions, browser-driven** — the proof is a DENIED day write (the fake stages transaction writes and commits only at the end, exactly as the SDK does): the swap stays pending and nothing but the audit reaches the store, for a 2-way and for the LAST leg of a 3-way where the first two legs must not have moved either. **Honesty: 14 of 33 FAIL on the 122 bytes.** `build61` and `build68`, which drive swap approval, still pass unchanged. |
| **122 / staff 39** | `c73fbde` | 28 Aug 2026 | **THE PERSONAL CHANGE FEED — §6, the other half of roadmap stage 7.** *"Users should only see changes that involve them."* **ONE PLACE FEEDS IT:** `mutateCell` is the single writer of assignments, so the cell editor, request approval, swap approval and the by-hand tool all feed it without knowing — it captures before and after inside the transaction and, when the month is PUBLISHED, writes one `dailysched/changes/items` entry for the person: what was added, what was removed, a day marked off, who did it, when. A write that changes nothing emits nothing. The engine's fills (`engineWrite`) emit the same; **publishing emits one entry per person who holds anything in the month** (§5: *"reaches people at publish"*), naming their count; **Clear Month emits one per person who held anything.** A DRAFT month emits nothing. Entries never throw — the feed cannot break the write it describes (the audit-log rule). **STAFF (39):** a *Changes* entry in the nav with an unread badge; the panel lists my own entries newest first, new ones marked, *Mark all as read* kept in this browser per person. **The query is filtered to me at the source** (`where('u','==',me)`), so nobody else's entries are fetched — and the suite pins that in the code. **NOT rules-enforced:** any signed-in user could read the collection by another path; the page never does, and closing that in `firestore.rules` is an auction deploy (§92), his call — recorded in TODO. **Gates:** `build122-test.mjs`, **25 assertions, browser-driven on both pages** — draft silence, publish fan-out, add/remove/off entries, the no-op that emits nothing, the engine's fills, Clear Month, and the staff badge/list/read/new cycle. **Honesty: 18 of 25 FAIL on admin 121 / staff 38.** Isolation guard green (the feed lives in `dailysched/*`). |
| **121 / staff 38** | `c73fbde` | 28 Aug 2026 | **DRAFT vs PUBLISHED MONTHS — §5, ruled 15 Aug and never built. Roadmap stage 7's first half.** *"Each month has a draft (admins only) and a published version (what staff see)."* Now that the Engine fills a month in one press, a roster needs somewhere to be built before staff see it. **THE DATA MODEL IS ONE FIELD:** the month document (`dailysched/sched_YYYY-MM`, beside the v2 marker) carries `published:false` while a month is being built, with `publishedAt`/`publishedBy` when it is published. **ABSENT MEANS PUBLISHED** (§120 decision) — nothing already on the site disappears from staff the day this ships, and no migration touches a month. **STAFF (38):** a draft month shows NOTHING — no chips on the calendar, the grid or the quick view — and a notice names the month and says it hasn't been published yet; the gate is in `watchMonth`, the page's ONE month reader, so the three views cannot disagree (§19). Vacation still shows (it comes from the auction). **ADMIN (121):** the grid header carries a DRAFT / PUBLISHED badge; the Engine page's first card says which the month is and offers *Publish* or *Return to draft*, each behind a confirmation; **publishing names how many rule violations are standing** — `design/RULES.md`'s *"the publish confirmation names how many are outstanding"* — as a report, not a gate (§4); both are audited. The engine's Apply confirmation says whether staff will see the result now or at publish. Approvals on a draft build the draft, exactly as §5 rules, with no code change — staff simply cannot see it yet. **NOT in this build:** the personal change feed (§6, the other half of stage 7), and rules-level enforcement of the draft (the staff gate is in the page; `firestore.rules` is the auction's file, §92 — his call). **Gates:** `build121-test.mjs`, **23 assertions, browser-driven on BOTH pages** — the staff page is watched flipping draft → published → unflagged without a reload; the admin's badge, both switches, the standing-violation count in the confirmation and the audit entry are all driven. **Honesty: 21 of 23 FAIL on admin 120 / staff 37.** |
| **120** (staff unchanged) | `c73fbde` | 28 Aug 2026 | **THE ENGINE PAGE — the Simulator is retired. §123.** Owner: *"get rid of the simulator… a mechanism to make the engine run and populate the staffing grid… assign shifts 1 at a time or multiple at a time… must follow all rules that are set… clean and intuitive… its own section in the sidebar"* — *"filed under Admin. Admin section should be moved up to be between users and setup."* **① THE SIDEBAR:** Testing is gone; Admin sits between People and Setup and the Engine leads it. Rehearsal mode, its banner and its switch are deleted (§45's testing tool is over); Clear Month keeps its confirmation, which was always the real guard. The grid's old *"moved to Testing"* link now points at the Engine. **② PREVIEW, THEN APPLY.** `enginePlan(ym)` runs the whole month IN MEMORY and returns a plan — what it would assign by day, what it could not fill, what a rule stopped, quota fills and conflicts; `engineWrite(plan)` writes it as the targeted per-day patch of old. The page shows the plan and offers Apply on a second press; a plan for another month is refused, never applied blind. `autoPopulate()` is now a thin wrapper over both, kept for the year runner's wording and the suites that drive it. **③ THE ENGINE FOLLOWS EVERY RULE THAT IS ON.** `ruleDiff(ym,dd,u,addId,days)` is extracted from the warn-at-change path as the ONE question — what NEW violations would this placement create — and `fillDay` asks it of every candidate in fairness order, placing the first one no rule objects to. **Any severity stops the engine** (§120 decision): severity is how loudly a PERSON is warned, not permission for a machine. A slot every eligible person is stopped on is DECLINED and says who was stopped by what; it is not double-reported as *no eligible user*. The built-in post-call fast path reports the same way. **④ ASSIGN BY HAND** — a person, a date or a run of dates, weekday boxes, every catalog shift ticked one at a time or several — previews every row through `assignmentWarnings` (§51: the same checker as the cell editor and the queues), shows each warning, then one confirmation for the batch; each write goes through `addCellShift` and each override is recorded naming the rule. A shift already held is skipped and said. Days outside the open month are read once each. **⑤ The Rules page's *no rules yet* hint survives the built-ins** — it now speaks of rules of his own, above the two built-in cards. **Gates:** `build120-test.mjs`, **49 assertions, browser-driven** — the nav order measured, Preview proven write-free, Apply proven to write, a rule proven to stop the engine and a Note-level rule proven to stop it too, the fairest-but-stopped candidate skipped for the next, and both hand-assignment shapes driven through the form. **Honesty: 41 of 49 FAIL on the 119 bytes.** **Six suites re-anchored by ruling** (§3 rule 12): `build53` and `build60` pinned the Simulator page and rehearsal mode — replaced with §123's invariants (the tools are off the daily page and on the Engine; there is no rehearsal switch; Clear Month asks, and declined it wipes nothing); `build101`'s panel list; `build105` counts only the rule under test now that the built-in post-call fires on its fixture; `build117`'s month-filler anchor moved to `enginePlan`; `build118`'s wording for a slot the post-call rule stops. |
| **119** (staff unchanged) | `c73fbde` | 28 Aug 2026 | **THE COLLAPSE CONTROL IS THE VACATION ADMIN'S, EVERYWHERE. §122.** Owner: *"you also never fixed the problem with the expand/collapse arrows. I want them to be identical to the vacay site and they need to be more visible."* Build 111 made a pill for section bars and kept a bare arrow on per-item cards — neither identical nor visible enough. Now `.secta,.chev` IS the auction admin's `.chev` rule: 12px, weight 600, its `--t2` written as the literal `#3d5568` so the colour matches to the byte, reading **▸ Show / ▾ Hide** on section bars, on every quota, rule and group card, AND on the Shift Catalog's family bands (which carried their own 10px glyph). `text-transform` is reset because the schedule's card titles are uppercase and the auction's control is not. The `.bare` variant is gone. **Gates:** `build119-test.mjs`, **26 assertions, browser-driven** — the auction admin's stylesheet is READ and its rule measured, then every kind of control on the schedule is checked by COMPUTED STYLE against those numbers, and a page-wide sweep proves no `.secta` reads as a lone glyph. **Honesty: 13 of 26 FAIL on the 118 bytes.** **Two suites re-anchored, stricter on the new truth:** `build111` (the pill and the bare card) and `build98` (group-card glyphs, now read as the first character of the word). |
| **118** (staff unchanged) | `c73fbde` | 28 Aug 2026 | **TWO BUILT-IN CHECKS WITH A SWITCH, and the engine never places a post-call shift. §121.** Owner, reviewing the §120 decisions: *"change 1 to something i can turn on/off and check for site clashes"* and *"any shift going past midnight should be such that person isn't given another shift on the post-call day. i want admin to be able to override this with a confirmation dialogue when necessary, but the engine shouldn't create it."* **① BUILT-INS.** `BUILTIN_RULES` — One site per day and Post-call day — are prepended by `ruleList()` as synthetic instances: on by default (absent means on), a severity, everyone covered, never deleted or renamed, not in the Add list; their settings live under reserved keys in `dailysched/rules`, so nothing new is listened to and the checker, the report, the card and the warn-at-change path see them exactly as they see a rule the admin made. `ruleGet()` is the one lookup the writers use. **② THE POST-CALL DAY IS READ FROM THE CLOCK** — `crossesMidnight` on the shift's times — so it needs no tag and cannot be defeated by one; the tag-based *After a shift, avoid…* type stays for anything beyond it. A violation is dated the day AFTER; a shift with no times is named as unjudgeable; the 1st cannot see the month before and says so (§121 ④). **③ THE ENGINE NEVER PLACES ONE:** `fillDay` drops a candidate whose shift yesterday ran past midnight — yesterday being this month's previous day, or on the 1st the previous month's last day when the caller holds it (the year filler does, from its cache; the month filler cannot, and the suite proves the 1st is unguarded there rather than pretending). **④ AN ADMIN MAY,** through the cell editor's confirmation, and the override is recorded — proven through the page. **Gates:** `build118-test.mjs`, **46 assertions, browser-driven**, registered the same turn. **Honesty: 23 of 46 FAIL on the 117 bytes.** **Three suites re-anchored by ruling:** `build112` (One site per day is no longer added, it is there), `build116` (the addable vocabulary is seven), `build115` (the built-in legitimately fires on its fixture). **`RULE_TYPES_PROBE` now reports the ADDABLE vocabulary**, which is what the dropdown must equal. |
| **117** (staff unchanged) | `94ba25f` | 28 Aug 2026 | **QUOTAS AS CONSTRAINTS — the largest piece left of Stage 5. §108/§109/§120.** Build 102 made a quota a READOUT and said in its own row that *"true multi-constraint filling waits for Stage 5."* This is it. **PASS 1 is unchanged** from 78/103: every shift to its own staffing minimum, one shift per person per day (§45), the pick being whoever has the fewest of that shift across the rolling window. **PASS 2 is new: each quota still short of its combined daily total is topped up across its member shifts**, preferring the shift with the most room under its ceiling — and, because a shift may sit in two quotas (§109), preferring a shift whose extra person would NOT tip another quota past its total; only if every member shift would is one of them used. **A shift with no rule of its own for the day is fillable by a quota — the quota IS the ask for that day.** **WHAT IT NEVER DOES: remove anything (§8).** A quota the minimums (or another quota) already exceed is a **CONFLICT**, named in the completion report and the audit entry and left standing for a person. A quota it cannot fill says which and by how many, and whether every member shift was at its ceiling. **AND THE STRUCTURAL HALF, which is why this took one build and not two:** the month and year fillers were **two copies of the same loop** — §19's exact hazard — so the quota pass would have had to land in both and be kept identical for ever. They now call **ONE `fillDay`**, and the suite asserts textually that neither keeps a candidate loop of its own. The Quotas page no longer says *"it does not fill"*. **§120 decisions:** the filler treats a ceiling as its own target (for a PERSON it stays an indicator, §108); a suite finding changed the design mid-build — the first cut let quota A's top-up tip quota B over and then reported B as a conflict, which was correct and unhelpful; the tip-over preference above is the fix. **Gates:** `build117-test.mjs`, **37 assertions, browser-driven** — `fillDay` driven directly for the arithmetic and Auto-populate Month driven through the page for the writes and the report. **Honesty: 26 of 37 FAIL on the 116 bytes.** **One suite re-anchored, stricter:** `build78` pinned *two* occurrences of the target call and *two* skip lines — decaying counts — and now asserts one call in `fillDay` reached by both fillers. **Schedule battery on these bytes: 68 suites / 2,391 assertions / 0 failed / 0 skipped.** Auction battery on unchanged auction bytes: see the HANDOFF entry for 28 Aug. |
| **116** (staff unchanged) | `94ba25f` | 28 Aug 2026 | **FAIRNESS BALANCE — the last of the seven types in `design/RULES.md` — and a frame fix.** The owner's ruling for Stage 5 fairness was **"weighted burden by FTE", never raw counts** — which is exactly what build 51's report maths does (§29). **So this rule CALLS `buildModel` rather than restating it (§19):** the pool is the covered people who count toward comparisons and have an FTE; a rate per 1.0 FTE is taken across it; each person's share is that rate times their own FTE. **Over by more than the tolerance is a row, dated to the person's LAST such shift so a click lands on a day; under by more than the tolerance is a row too** — a real roster's unfairness runs both ways, and the suite proves that a 0.5-FTE with one call is within her share where a flat mean would have accused her. **People the rule covers but the pool does not measure (per diem, locums, no FTE — §26/§36/§43) are counted and NAMED with the reason**, never silently dropped; a person overridden INTO the pool is measured. A pool of one says there is nothing to balance against. **THE FRAME FIX, found while designing this:** the card header said *"not checking anything"* for ANY note — so 106's per-week cap in a month starting mid-week, and 113's cooldown running off the month end, were labelled idle while reporting violations. The header now reads `ran`/`considered`, a rule with a note carries a ⚠ marker instead, and the report's *"N rules checked nothing"* counts only rules that did. **Gates:** `build116-test.mjs`, **43 assertions, browser-driven**, registered the same turn. **Honesty: 20 of 43 FAIL on the 115 bytes.** **Schedule battery on these bytes: 67 suites / 2,354 assertions / 0 failed / 0 skipped.** |
| **115** (staff unchanged) | `94ba25f` | 28 Aug 2026 | **WARN AT THE MOMENT OF CHANGE — the rules fire through the ONE warnings function. §4 · §51 · §111.** Build 105 shipped the rules as a report and named this as its own build. **It is deliberately NOT a new dialog or a new path:** `assignmentWarnings` is §51's single answer to *"is this assignment a problem?"*, called by the cell editor, request approval and swap approval alike — so a rule now fires on Approve exactly as it fires on Add, and `cmConflicts` is still the thin wrapper 61 made it. **THE DESIGN: the rules check a MONTH; a change is one shift on one day. So the month is checked as it stands and with the change applied, and the DIFFERENCE is what this change breaks** — a post-call violation caused by adding a call on the 3rd is dated *(on the 4)*, and a check of the 3rd alone would never see it; a violation already standing elsewhere is not pinned on an unrelated change. **Serious first (⛔ SERIOUS), Warn plain, Note last** (§111). **Nothing is blocked (§4)** — one confirm, and the audit entry records the OVERRIDE naming the rule (§51), proven through the page's own cell editor. **THE HONEST HALF:** request and swap approval routinely act on a month that is not open and hold ONE day; there the day-only rules still fire and the ones that span days (rest, cooldown, caps) are checked against that day and **say the rest of the month was not in view** — reading a month on every Approve is a later cost decision (§2), not a silent read. **Gates:** `build115-test.mjs`, **31 assertions, browser-driven**, registered the same turn. **Honesty: 13 of 31 FAIL on the 114 bytes** — after its own §3 rule 11 lesson (an assertion MESSAGE threw on the baseline; guarded). Writers untouched: `mutateCell`, `addCellShift`, auto-populate still consult no rule — the checker does. |
| **114** (staff unchanged) | `94ba25f` | 28 Aug 2026 | **§7 AS RULES — Not together, and Only approved pairs.** §7 ruled *"compatibility = times AND an approved-pairs list. Both, not either… whatever survives, the admin ticks. An unticked surviving pair is unapproved (warns, per ruling 4), not impossible."* Times were already Minimum rest's business (a negative gap is an overlap) and location became One site per day's in 112; **this build is the LIST**, which had never existed anywhere — two prose strings promised a compatibility check and nothing implemented it (`TODO.md` roadmap row 1). **① NOT TOGETHER** is the blunt form: two named shifts never held by one person on one day, for the pair that is wrong for a reason the clock cannot see. Two shift pickers built from the catalog (`shiftChoices()`, in catalog order, by id so a rename keeps the rule — build 57). Order-free; both boxes naming one shift checks nothing and says so. **② ONLY APPROVED PAIRS** holds the tick list as its one parameter — a **new field kind, `pairs`**: chips you remove, two pickers and an *Approve this pair* button you add with. **The key is order-free (`D10__PM`) and both ids are ALSO stored as fields, so the key is never parsed** — a shift id may contain anything. Refusals without a write: a shift with itself, a pair already on the list, a shift not in the catalog. `setRuleParam` declines the `pairs` kind — the list has its own two audited writers. **An empty list reports every two-shift day, which is what it means, and SAYS that is why** rather than looking like a broken rule; three shifts on one day are three pairs, each judged. `heldPairs()` is the one definition of "the pairs somebody holds", exposed for the warn-at-change build. **⚠ A Firestore trap, caught by the suite's design rather than by luck:** `addRule` writes every field's default, and a `pairs` field with no `def` would have written `undefined`, which Firestore refuses — so the default is `{}` and the suite pins the write. **Gates:** `build114-test.mjs`, **48 assertions, browser-driven** (pairs approved through the page's own pickers and button), registered the same turn. **Honesty: 27 of 48 FAIL on the 113 bytes** — after the suite's own §3 rule 11 lesson: the first run CRASHED on the baseline (`getElementById(...).value` on a picker 113 never had) instead of failing legibly; guarded. **Schedule battery on these bytes: see below.** |
| **113** (staff unchanged) | `94ba25f` | 28 Aug 2026 | **POST-TAG COOLDOWN — the post-call day. §119/§120.** *"After a shift tagged X, for N days, avoid Y — or anything."* Three parameters: the trigger TAG, the window in days (1–14, default 1), and what to avoid — a tag, or *any shift at all*, which is what a post-call day off means and is the default. **Both ends point at TAGS, never at `kind` (§25/§27)** — Eye Call, NAMED call and in no overnight tag, opens no window, and the suite proves it. **The window is counted in calendar days after the day the trigger STARTS**: a 24-hour call beginning Monday makes Tuesday day one, which is what post-call means on a real roster. **One offending shift is ONE row however many triggers reach it** (back-to-back calls both point at Wednesday's D10; it reads once), and when the rule says *nothing at all*, the second of two back-to-back calls is itself an offence — which is correct and is what the rule was asked. **A window that runs off the end of the month is SAID, not swallowed** — the same honesty as the cap's first week: the checker is handed one month, and a call on the 31st carries a note rather than a clean sheet. An empty trigger tag, or an empty avoid tag, says so. **Gates:** `build113-test.mjs`, **40 assertions, browser-driven**, registered the same turn. **Honesty: 21 of 40 FAIL on the 112 bytes.** **Battery:** run on the 114 bytes with 113 inside them (one clean run covers both — see the 114 row); the 112 battery above is the last run on 112 alone. |
| **112** (staff unchanged) | `94ba25f` | 28 Aug 2026 | **TWO RULE TYPES WITHOUT NUMBERS — Not on vacation, and One site per day. §119/§120.** The schedule came off the back burner (§119: *"finish what's left"*) and the owner handed the session its decisions (§120). These are the two cheapest of the five unbuilt types in `design/RULES.md`, and both were chosen to prove the frame takes a type with **no parameters at all** — `fields:[]` — without a card drawing an empty row. **① NOT ON VACATION.** The cell editor has warned about a shift on approved vacation since 61 — but a warning is a toast that is gone the moment it fires, and nothing REPORTED it across a month; a shift approved from the request queue onto somebody's vacation left no trace anyone could find a week later. Now it is a row in the conflict report, per shift, per day. **THE §19 HALF:** the warning and the rule read ONE predicate, `onVacationIn(vac,u,dk)` — the key convention lives in one place, `assignmentWarnings` was re-pointed to it, and the suite asserts textually that the warnings function no longer indexes the map itself. Both vacation sources (completed phases and the live approvals doc) feed it, and the suite proves each. Five other readers still index `vacationDays()` directly; they were left alone (§3 rule 3 — surgical), noted here so nobody thinks it was missed. **② ONE SITE PER DAY.** §18 as a rule: two shifts at two sites on one person on one day, named as *Oakland (Oak Day) and Richmond (Rich Day)*. `sitesOf(ids)` is the one definition, exposed for the warn-at-change build. **A shift with NO site cannot be judged, and the rule says how many it could not judge** rather than treating them as compatible with everything — and it says so in its own words, not by borrowing Minimum rest's *"no times set"*. **§120 DECISION:** it is a type the admin creates once, not an always-on check — consistent with §11, and it gives him the severity knob. `RULE_TYPES` is now exposed as a probe so a suite can assert the dropdown offers the WHOLE vocabulary. **Gates:** `build112-test.mjs`, **55 assertions, browser-driven**, registered the same turn. **Honesty: 27 of 55 FAIL on the pushed 111 bytes (explicit SHA `8aaa841`).** **One suite re-anchored, stricter:** `build106` pinned `length===2` on the type dropdown — §3 rule 16's decaying count — and now asserts the dropdown equals the vocabulary, whatever its size. Battery: see the 113 row for the clean run; this build's own run collided on a port with a still-running baseline battery and one suite (`elig-test`, unrelated) reported EADDRINUSE, passing 32/0 when run alone. |
| **111** (staff unchanged) | `8aaa841` | 26 Aug 2026 | **THE COLLAPSE CONTROL, MADE VISIBLE — and the sweep is over. §115.** Owner, closing the last item on his own wind-down list: *"No need to collapse anymore sections. Just style the collapse/expand like the vacation site to be more visible."* **THE COMPLAINT WAS THE CONTROL, NOT THE COVERAGE.** `.secta` was a **10px `--t3` arrow nine pixels wide** — the faintest thing on the page, sitting on the control he presses more than any other. The auction admin already had the answer in its `.chev`: **"▸ Show" / "▾ Hide"**. The words are the whole point — **an arrow tells you the STATE, a word tells you what pressing it will DO.** It is now a pill: 11.5px, semibold, its own ground, a border, and a 60px hit area instead of nine. **A PER-ITEM CARD KEEPS THE BARE ARROW.** One quota, one rule, one group — the disclosure sits beside a NAME there, and "Show" repeated once per card is noise rather than help. **The keys already told the two apart** (`qta:`, `rul:`, `grp:` are prefixed), so nothing had to be passed in and **no markup changed anywhere** — one CSS rule and four lines of `sectApply`. **AND A BUG THE CHANGE EXPOSED, which is the part worth keeping:** static section bars were only ever relabelled by `sectApply` on a TOGGLE. Every one of them sat showing the raw arrow from the HTML until its first click — **and the one moment the label is most useful is before you have pressed it.** A boot sweep now labels them all. **⚠ §104 IS NOT REPEALED — IT STOPS ACQUIRING NEW GROUND.** Reports, the Simulator's Fill/Wipe and Stats keep their documented exemptions (build-99 row). A later build does not get to collapse something new by citing §104. **Gates:** `build111-test.mjs`, **30 assertions, browser-driven**, registered the same turn — and checked through **COMPUTED STYLE**, not stylesheet text, for the same reason as 109: a rule present in the CSS proves it was typed, not that it reaches the control. **Honesty: 9 of 30 FAIL on the 110 bytes.** **Two suites were re-anchored, and came out stricter:** `build96` and `build99` pinned the exact glyph `'▸'`. The premise moved BY INSTRUCTION, so they now assert open-versus-closed AND require the word — which the old literal did not. Battery: **62 suites / 2,148 assertions / 0 failed.** |
| **110** (staff unchanged) | `41ca7be` ⚠ subject says 108-109 | 26 Aug 2026 | **THE CALENDAR FEED — the half that lives in this repo. §54 · §54a.** Owner: *"i'm ok with this, proceed with what you can."* **THE DESIGN, AND IT IS THE WHOLE SECURITY ARGUMENT:** the admin page renders each doctor's calendar and writes the FINISHED `.ics` TEXT to `dailysched/feeds/items/{token}`. The hosted endpoint looks the token up and returns the string — no schedule logic, no query, one document path — so there is no code in it that could reach `vacations/*` even by mistake. Not *"the code is careful"* but *"there is nothing in it to get wrong"*, which is the only version worth putting on the project that runs the live auction. It also makes a poll ONE read instead of ~120. **⚠ CLAUDE WAS WRONG ABOUT THE COST EARLIER AND THE OWNER CHALLENGED IT.** Checked against published pricing: **$0.03/100k reads, 50,000 free daily.** Even the naive per-request shape is about **$0.50/month**. Cost was never the objection; the security surface and the deploy path are. **FOUR THINGS THAT FAIL SILENTLY, AND ARE THEREFORE WHAT THE SUITE IS ABOUT.** ① **Escaping** — the first draft wrote `'\;'`, which is just `;` in JavaScript, so semicolons were not escaped at all; a raw semicolon splits one field into two and the doctor sees a truncated title, not an error. **The identical trap then bit the test's own regex.** ② **Folding** at 75 OCTETS per line, or strict parsers reject the file whole. ③ **UID stability** — calendar apps REPLACE a UID they hold and ADD one they do not, so an unstable UID does not refresh a calendar, it duplicates it. ④ **A partial feed is never published**: a missing event is not a gap to a calendar app, it is a DELETION, and a short feed reads to the doctor as time off. A month it cannot read makes it refuse. **⚠ OWNER RULING MID-BUILD, OVERTURNING CLAUDE'S DESIGN: vacation is ONE EVENT PER DAY, not a merged block** — *"sometimes docs trade single days of vacation."* He was right on the domain and right on the format: a merged block's UID must be keyed to the RUN's start, so trading one day out of the middle changes the boundaries, changes the UIDs, and strands the old block in the calendar with nothing to replace it. Per-day events are keyed to their own date. The suite proves the traded-day case: give one day away and every surviving UID is byte-identical. Also: midnight-crossing shifts come out 24 hours; a shift with NO times becomes an all-day entry rather than vanishing (a missing shift reads as a day off); only that doctor appears (§54a). **SHIPPED WITH IT: `functions/`** — the endpoint source, its `package.json`, and `DEPLOY.md`. **Nothing is deployed.** **⛔ TWO RELEASE BLOCKERS, NOT WORKED AROUND:** `dailysched/feedTokens` must become admin-only in `firestore.rules` (today the dailysched catch-all lets any signed-in user read it — a readable link list means any doctor can read any other doctor's calendar), and that file is closed by §92; and the endpoint is not deployed. §54's own release gate is also still shut. The page opens with all three in red rather than showing off the feature. **Gates:** `build110-test.mjs`, **63 assertions, browser-driven**, registered the same turn. **Honesty: 42 of 63 FAIL on the 109 bytes.** Battery on the pushed tree: 61 suites, 0 failed. |
| **109** (staff unchanged) | `41ca7be` | 26 Aug 2026 | **A DROPDOWN THAT LOOKS LIKE ONE, AND THE SHEET HE ASKED FOR.** Owner, verbatim: *"the dropdown menu control is needs to become more visible. Drop the the times on the printable daily/weekly sheets. Have those sheets be 3 columns: shifts, Staff, Notes. Shifts should be 1/4 of the width, Staff also 1/4. And notes 1/2."* **① THE DROPDOWN.** `.sel` and `.inp` shared ONE rule, so a control you pick from was pixel-identical to one you type into — the platform caret was the only difference and it is nearly invisible at that border weight. `.sel` now has its own ground, a heavier border, a pointer cursor and a DRAWN caret; `appearance:none` removes the platform one deliberately so the control looks the same on every machine rather than only on the one it was checked on. A list box (`[multiple]`/`[size]`) opts out — it has no caret to draw and nothing to collapse. **⚠ THE SUITE ASKS THE BROWSER, NOT THE STYLESHEET.** Asserting the rule exists in the CSS proves it was TYPED, not that it reaches the control — a later selector, a specificity clash or a typo in the data URI all leave the text intact and the control unchanged. So computed style on a real `<select>` is compared against a real `<input>` beside it, and every one of the 19 selects on the page is checked for the class. **② THE SHEET IS SHIFT · STAFF · NOTES**, at a quarter, a quarter and a half — and the widths are asserted as RENDERED fractions of the table, because `width:25%` in a stylesheet and a quarter of the page on paper are different claims. They come out 25/25/50. Notes is deliberately EMPTY with a minimum height: it is somewhere to write, not somewhere to read. **③ TIMES ARE OFF THE PAPER, NOT OUT OF THE APP** — still editable in the catalog, still read by Minimum rest, and the suite pins both so a later reader does not think they were deleted. **④ A WEEK IS SEVEN DAILY SHEETS, NOT A GRID.** The page break moved onto a per-site wrapper: breaking per day would turn one week into twenty-one pages. **Gates:** `build109-test.mjs`, **37 assertions, browser-driven**, registered the same turn. **Honesty: 18 of 37 FAIL on the 108 bytes.** `build107-test`'s two week-shape assertions were replaced — their premise was changed BY INSTRUCTION, so they now state the new truth (seven blocks per site, the first a Sunday), which the pre-109 build fails. |
| **108** (staff unchanged) | `41ca7be` | 26 Aug 2026 | **OVERNIGHT CALL ACROSS YEARS.** Owner spec, verbatim: *"i want to track overnight calls by person, by day, by month, by year, and across years."* **CHECKED FIRST (§3 rule 14): by day is the grid, and by month and by year were ALREADY BUILT** as the Stats page's two modes. Only *across years* did not exist, so only that was built. **⚠ IT COUNTS THE TAG, NEVER `kind` (§25/§27)** — `overnightSet()` owns that answer already, seed fallback included, so there is no second definition (§19). Eye Call is named "call", runs 07:30–15:30 and is in no overnight tag; counting it would overstate a real person's call burden, silently, in the direction that accuses them of working too much. Asserted twice: behaviourally, and as a code fact that the type never reads `.kind`. **THE ARCHIVE, AND WHY.** One year computed live is twelve month-reads — defect 13 was a read-cost storm from ONE year re-fetched by accident, and five years on screen would be sixty every time anybody looked. So `dailysched/callArchive` keeps a sealed per-year total: **one read instead of twelve.** A year is computed once, deliberately, by a button; **entering the mode costs nothing**, and the suite pins that. **AND THE HONEST HALF, WHICH IS MOST OF THE DESIGN.** A year nobody has computed reads **·**, not 0 — blank and zero are the same pixel and opposite facts. A year that is not over, or one with a month that could not be read, is stored and shown as **incomplete**. An empty tag refuses to produce a table of zeros against real people and says where to fix it. **A gap the suite found:** the unread-month count reached the archive but not the TABLE, so a live-computed short year showed a bare number — the toast fires once and is gone, and the table is what somebody looks at a week later. Fixed, and pinned. **Gates:** `build108-test.mjs`, **55 assertions, browser-driven**, registered the same turn. **Honesty: 40 of 55 FAIL on the 107 bytes** (fixture from the pushed `30c74f5`). **A collision fixed at the source:** a local `const totals={}` tripped build 78's tombstone for defect 25's deleted MODULE-LEVEL `totals`; the local is renamed and that assertion now inspects the module state list it actually guards. |
| **107** (staff unchanged) | `30c74f5` | 26 Aug 2026 | **PRINTABLE SHEETS — his own spec, which he called “brilliant”.** Verbatim: a daily sheet per SITE; a weekly option; a print config that SAVES a by-family selection with each shift selectable and unselectable in an easy-to-view way, persisted. New page, new document `dailysched/printConfig`. **① THE SELECTION IS STORED AS EXCLUSIONS** — `{off:{<shiftId>:true}}` — for §35's reason: a shift added to the catalog tomorrow is on the sheet by default and needs **no write here at all**. The inclusion shape would silently drop every new shift off every printout, and nobody finds that out from a screen. **② ONE SHEET PER SITE.** “All sites” draws one block each rather than a merged list, `page-break-after` between them, and a shift with no site gets its own block instead of vanishing off the paper. **③ THE WEEK IS SUNDAY-ANCHORED**, like every other week on this platform, and it fetches the adjacent month rather than truncating at the 1st. **④ A MONTH THAT COULD NOT BE READ IS NEVER DRAWN AS AN EMPTY DAY** — on paper an empty row and an unread row are indistinguishable and mean opposite things, so an unreadable day prints “not read” under a banner that counts them. The suite denies a read and insists on it. **⚠ ONE DELIBERATE WIDENING, FLAGGED NOT ASSUMED:** he said “a weekly option for Richmond”; the weekly option is offered for EVERY site, because sites are DATA (`siteList()`, §18) and a site name baked into a control breaks the day one is renamed. Superset of the spec; his to narrow. **A COLLISION THIS BUILD CAUSED AND FIXED AT THE SOURCE:** the section key was first `printshifts`, which CONTAINS `shifts` — and five suites select section bars with `onclick*="shifts"`, so three catalog suites clicked the Print page's bar and reported an empty catalog. Key renamed to `printpick`, and all six selectors now name the exact call. A key that is a superstring of another key is a trap for anything matching loosely. **Gates:** `build107-test.mjs`, **61 assertions, browser-driven**, registered the same turn. **Honesty: 52 of 61 FAIL on the 106 bytes** (fixture rebuilt from `5adb8ae` + the recorded diff and md5-proved identical to the bytes that ran green). |
| **106** (staff unchanged) | `30c74f5` | 26 Aug 2026 | **THE SECOND RULE TYPE — “Not more than”. §111 ②.** §21 deferred per-person caps — *“NOT wanted yet, revisit once the Rules section exists”* — and named this exact moment; the owner ruled: build it. A cap is `{tag, n, period}`: no more than N of a TAGGED shift per Sunday-anchored week or per calendar month. **⚠ IT POINTS AT A TAG, NEVER AT `kind`, AND THAT IS THE WHOLE POINT (§25/§27).** **Eye Call** is NAMED “call”, runs 07:30–15:30, and belongs to no overnight-call tag. Written against `kind` this build would be wrong in its first month, silently, in the direction that accuses a real person of working too much. The suite seeds exactly that shift and pins that it is not counted; a code assertion pins that the type never reads `.kind`. **ONE VIOLATION PER SHIFT OVER THE CEILING**, not one per person, each dated to the day that broke it — which is what “a click goes to that day cell” needs. **A WEEK THAT STRADDLES THE MONTH BOUNDARY SAYS SO** rather than quietly under-counting: the checker is handed one month, so a month not beginning on a Sunday carries a note that its first week is counted from the 1st only. **THE FRAME TOOK IT WITHOUT BENDING**, which was build 105's whole claim — the only architecture added is a CHOICE field kind beside the number one, and the Add-a-rule dropdown is now BUILT from `RULE_TYPES` so a type that exists but is unreachable cannot happen (§3 rule 9 in miniature). An unknown choice is REFUSED, not stored. **Gates:** `build106-test.mjs`, **54 assertions, browser-driven**, registered the same turn. **Honesty: 25 of 54 FAIL on the 105 bytes** (fixture reverse-derived and md5-proved identical to the 105 that ran green). |
| **105** (staff unchanged) | `30c74f5` | 26 Aug 2026 | **STAGE 5 — THE RULES FRAME, AND MINIMUM REST. §110/§111.** Owner, lifting his own hold: *"Go with stage 5."* New page, new document `dailysched/rules` — `{<key>:{label,type,order,severity,off?,target,params}}`. **RULE TYPES ARE CODE, RULE INSTANCES ARE DATA** (§11, his own line), so `RULE_TYPES` is a fixed vocabulary and the admin makes as many instances as he likes without a build. **ONE TYPE SHIPS ON PURPOSE.** Minimum rest forces every architectural decision into the open at once — the store, the target model, the checker, the report — while needing NO new data: it reads times from the catalog and assignments from the day documents, both already populated. Caps are 106, for the reason build 52 gave: two changes in one build make a regression impossible to attribute. **§10's TARGETS ARE REAL AND THEY UNION** — everyone · a category · a group · named people · any combination of those, which is the case a single dropdown cannot express. An EMPTY target covers nobody and says so; a new rule is born covering everyone at Warn so it can never be born checking nothing. **THREE SEVERITIES (§111 ①)** — Serious · Warn · Note — sorting the report and, later, the loudness of the confirmation. **None of them refuses anything**, because §4 is a ruling: nothing in this app is ever hard-blocked. **THE CONFLICT REPORT** is grouped by severity then date, each row *who · when · which rule · what specifically*, and it says out loud that it is a report and not a gate. **AND THE HONEST HALF:** a rule that cannot see anything SAYS SO, naming the count — *"the 2 shifts held by the people it covers have no times set"* — because a quiet zero-violations report on a rule that never ran is worse than no rule at all (`design/RULES.md`). Eye Call, which carries no times, is exactly that case. **IT REPORTS ONLY.** `mutateCell`, `addCellShift` and auto-populate are untouched and the suite pins each one; `demandOn` is unchanged. The warn-and-override path at the moment of a change is a later build. **Gates:** `build105-test.mjs`, **75 assertions, browser-driven** (§3 rule 9 — it adds a rule through the box an admin types in, opens the card, changes the severity and reads the report back), registered in `sched/run-all.mjs` in the same turn it was written. **Honesty: 64 of 75 FAIL on the 104 bytes** (fixture from the explicit SHA `5adb8ae`, never `HEAD~1`). |
| **104** (staff unchanged) | `5adb8ae` | 25 Aug 2026 | **THE THIRD READING, AND ONE CEILING INSTEAD OF TWO. [103b of two.]** His ruling in full: auto-populate fills to MIN · MAX indicates, never refuses · **at-min-below-max is ACCEPTABLE**. [103a] carried the shape and changed nothing; this is where the behaviour changes, and it makes three claims. **① A day at or above the minimum but below the ceiling is neither a gap nor an over-staffing — it is COUNTED as spare and deliberately NOT LISTED**, because a row per acceptable day buries the days that actually need him. The summary says *“N more places are available within the ranges — covered, and could take one more.”* **② The percentage still counts MINIMUMS.** A ceiling never enters the denominator: on the suite's fixture, minimums give 124 and ceilings would give 186, so a fully staffed month would have read as two thirds covered. **③ OVER MEANS OVER THE CEILING**, and the number reported is how far past THAT — not past the minimum, which would call a legitimate extra person an over-staffing. **`max` ABSORBS `capacity` (§108⑥):** `shiftCeilingOn()` is the one question, answered by the day's rule when there is one; `capacity` keeps its auto-populate fallback for a shift with NO rules and loses its second job in the cell editor's occupancy label and the swap-approval warning — the latter now reads *“the most it asks for is N… a ceiling is an indicator, not a refusal.”* The grid chip reads **/2–4** and its tooltip agrees; the coverage table's *Asked for* column shows the range. **GATES: new `tests/sched/build104-test.mjs`, 26 assertions**, run over the REAL `coverageRows` for four staffings of one day. Honesty against the pushed **103** (explicit SHA `0792285`): **19 of 26 FAIL**. ⚠ **FOUR EXISTING SUITES HAD TO FOLLOW, and the near-miss is the lesson.** `build69` and `build70` EXTRACT `coverageRows` and eval it alone, so it broke when that function grew a dependency on `staffingOn`. **The first fix was wrong**: extracting the REAL `staffingOn` bypassed the fixture `DEMAND` map those suites drive everything from and silently turned every day into “not asked for”. They stub `demandOn` and always have — what they test is the ROLL-UP, not rule matching — so `staffingOn` is stubbed beside it **in the SHAPE of the real function** (`{min,max}` or null, never a number), which is exactly §3 rule 10's FAST-1 lesson. `build58`'s wording moved with the meaning; `build103`'s *“nothing calls staffingOn yet”* was true of [103a] and deliberately false here, and was replaced with the claim that outlives both — **demandOn still returns ONE number**. **Schedule battery: 55 suites, ZERO failed, ZERO skipped.** Auction battery on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0** (auction honesty blocks not run — no baseline supplied, §6's legitimate skip). `node --check` clean. |
| **103** (staff unchanged) | `0792285` | 25 Aug 2026 | **A STAFFING RULE CARRIES A RANGE — `{when, min, max?}`. [103a of two.]** His ruling, already settled: auto-populate fills to MIN · MAX is an indicator, never a refusal · at-min-below-max is acceptable. **THIS HALF IS THE SHAPE ONLY, AND ITS WHOLE PROMISE IS THAT NOTHING BEHAVES DIFFERENTLY.** `demandOn()` still returns ONE number — the minimum, which is exactly what `need` meant — so **all seven of its consumers are untouched**: the grid cell, `autoTargetFor` and the two auto-populate loops behind it, the 60-day preview, `coverageRows`, and the day board's shortfall. The range is available beside it through a new `staffingOn()`, and **nothing calls it yet** — an assertion pins that. **NO MIGRATION, DELIBERATELY.** A stored `{when,need}` reads as min = max = need through `ruleMin`/`ruleMax`; nothing on disk is rewritten. A migration would touch every shift on a site with no users to gain what a reader does for free, and the one thing worse than two shapes is a half-finished migration between them. **The editor gains an optional “up to” box**, blank by default; a ceiling BELOW the minimum is REFUSED rather than quietly clamped, because silently rewriting what he typed is how a rule ends up meaning something he never asked for; a stored ceiling below the minimum reads as NO ceiling. `ruleText` shows “2–3 people” only when there is a range, and the old wording otherwise. ⚠ **`capacity` IS NOT ABSORBED YET** — that changes behaviour and belongs to [103b], with the third coverage state (*covered · could take more*). **GATES: new `tests/sched/build103-test.mjs`, 25 assertions**, executing the real reader over both shapes and driving the editor in a browser. Honesty against the pushed **102** (explicit SHA `0ff9182`): **16 of 25 FAIL**. ⚠ **AND ONE EXISTING SUITE HAD TO FOLLOW: `build78-test` EXTRACTS `demandOn` and evals it in isolation, so it broke the moment that function grew a dependency** (`ReferenceError: ruleMin is not defined`). `ruleMin`/`ruleMax` were added to its extraction list — the extractor gotcha §3 rule 2 names, arriving for real. **Schedule battery: 54 suites, ZERO failed, ZERO skipped.** Auction battery on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0** (auction honesty blocks not run — no baseline supplied, §6's legitimate skip). `node --check` clean. |
| **102** (staff unchanged) | `0ff9182` | 25 Aug 2026 | **QUOTAS — a named set of SHIFTS that share one combined daily total. §108/§109.** Owner: *"Let's go with quota and this will be its own separate set for times when a variety shifts contribute to the same quota for a day"*, and *"A shift could sit in 2 quotas."* New page, new document `dailysched/quotas` — `{<key>:{label,order,rules:[],shifts:{}}}`. **THE TOTAL REUSES THE STAFFING-RULE SHAPE** the shifts already use (a stacking list, last match wins, `null` = not asked for) so `ruleMatches` and `ruleText` are shared and there is no second vocabulary (§3 rule 14). Membership is many-to-many and stored as dotted fields (`KEY.shifts.D10`), so two quotas holding the same shift cannot overwrite each other. Cards are collapsed by default (§104), the name leads the header (§93), rename is an inline row not a `prompt()` [100], and the gate is re-read at the moment of the write, not at the paint (§5). **⚠ WHAT THIS BUILD DELIBERATELY DOES NOT DO, and it is the point of it.** Two quotas over one shift on one day BOTH hold, which makes a quota a CONSTRAINT — Stage 5's shape, and Stage 5 is on hold. **So a quota only REPORTS.** `demandOn()` and its seven consumers are untouched, auto-populate still targets each shift's own staffing rules, and four assertions exist purely to pin that. The month readout counts placed people from **the same source `coverageRows()` reads** — `shiftsOf(cell[u])` — so a quota and the coverage report can never disagree about who is on a shift (the build-78 defect shape). **GATES: new `tests/sched/build102-test.mjs`, 47 assertions**, driven in a browser. Honesty against the pushed **101** (explicit SHA `49eb0f0`): **40 of 48 FAIL**. **Four of this suite's own bugs were found by running it and are worth carrying: `querySelector` cannot parse an id containing a colon** (`sect_qta:KEY`) while `getElementById` can — the page was right and the test was wrong; **`mergeFields` legitimately writes TWICE on a document that does not exist yet** (updateDoc throws, setDoc follows), so a count assertion was replaced with a shape assertion; **`innerText` reports `.card-title` in CAPITALS** because the CSS says `text-transform:uppercase`, so pinning the rendered case would be asserting a style choice; and **re-seeding a document mid-suite wipes what earlier sections created**, which reported a broken rename that was nothing of the kind. **Schedule battery: 53 suites, ZERO failed, ZERO skipped.** Full auction battery on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0**, auction honesty blocks not run (no baseline supplied — §6's legitimate skip). All three inline scripts `node --check` clean. |
| **101** (staff unchanged) | `49eb0f0` | 25 Aug 2026 | **THE WORD HE SEES IS "STAFFING". "DEMAND" IS RETIRED FROM THE UI — §103.** Owner: *"I don't like using the word demand - present new options."* **34 visible occurrences renamed across ~25 lines**, classified one by one before anything was touched rather than swept with a global replace: screen text, `title=` tooltips, toast and confirm messages, and the catalog column label. **THE LOAD-BEARING HALF IS WHAT DID NOT CHANGE** — `demandOn()`, `demandRules()`, `demandModal`, `DEMAND_WHENS`, `anyDemandConfigured`, every element id and CSS class, and above all **the stored field `shifts.<id>.demand`**, because renaming that is a data-model change wearing a cosmetic disguise (§103's own words). **Comments were left alone too**: they describe code whose names are unchanged. **Three sentences needed rewriting rather than a word swap** — *Unfilled demand for each day* → *Unfilled places for each day*; *never demanded* → *never asked for*, twice. **GATES: new `tests/sched/build101-test.mjs`, 19 assertions**, and it pulls both ways on purpose: nothing readable on seven panels may match `/demand/i` — text, tooltips AND placeholders — while five identifiers and the stored field must still be present. The column label is asked of the DOM, not of `innerText`, because the sticky header does not always surface there. Honesty against the pushed **100**: **4 of 19 FAIL**. **FIVE EXISTING SUITES REPOINTED** (58, 78, 84, 85, 87) — they pinned the retired wording, so their TARGET moved; the page did not lose the sentence (§3 rule 12). Not stubbed: 58 also now REFUSES the old word outright, which is stricter than what it replaced. ⚠ **AND THE FINDING THAT MATTERS MOST HERE HAS NOTHING TO DO WITH THE RENAME: `sched/run-all.mjs` CARRIES A HAND-KEPT SUITE LIST, and builds 99, 100 and 101's suites were none of them in it.** Each passed once by hand on the day it was written and would have been silent for ever after, while the battery reported *"49 suites, all green"* over the top of them. All three are now registered, with a warning at the head of the list. **Schedule battery: 52 suites, ZERO failed, ZERO skipped** — 1,294 assertions in the classic format plus the new suites' own counts (two output formats, summed honestly rather than quoted as one number). Full auction battery on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0**, auction honesty blocks not run (no baseline supplied — §6's legitimate skip). `node --check` clean on all three inline scripts. |
| **100** (staff unchanged) | `49eb0f0` | 25 Aug 2026 | **THE GROUP RENAME STOPS BLOCKING — AND THE GATE THAT HID IT FOR WEEKS IS FIXED.** Renaming a group called `prompt()`. A blocking dialog stalls every browser suite that drives this page and `build80-test` forbids it BY NAME — **so why was it green?** Because that suite strips comments before its textual assertions and stripped **block comments FIRST**, so a `/*` sitting inside a LINE comment (`// … live in dailysched/*, never in the shared roster`) opened a block that ran to the next real `*/`: **347,322 characters, about 70% of the file, deleted before anything was tested.** Every textual assertion in it had been running on the remaining 30%. **THE ORDER IS NOW LINE COMMENTS FIRST**, which removes the stray openers along with the lines holding them, and the suite gained the half §3 rule 10 was missing: **prove the stripper kept the code** — four code landmarks must survive and the stripped source must still be >55% of the page. **THE RENAME ITSELF is an inline row on the group card** — input, Save, Cancel — using the same single-key *which one is open* shape as `_eligOpen` rather than a second mechanism (§3 rule 14). The gate is **re-read at the moment of the write**, not when the row is painted (§5): rehearsal or a lost sign-in between opening and saving still refuses. A blank name is refused and the row stays open to be corrected. **`build67-test` REPOINTED, and it came out stricter** — it had been reading 0 rows since [96] made the catalog collapse by default, which is a retired invariant, not a defect; it now opens Shifts and every family band the way an admin does, proving the rows are REACHABLE as well as present. **GATES: new `tests/sched/build100-test.mjs`, 32 assertions** — including a measurement of the two stripper orders side by side on the real page (the gap is the untested percentage), a Playwright `dialog` listener that RECORDS any blocking dialog rather than hanging on it, and a guarded caller so a MISSING handler on the baseline reports a legible failure instead of throwing (§3 rule 11 — a crash and a regression are indistinguishable at the exit code). Honesty against the pushed **99** (explicit SHA `80e15b9`): **17 of 32 FAIL**. **Schedule battery: 49 suites / 1,293 assertions, ZERO skipped, ALL GREEN** — the first fully green schedule battery in some time, since `build67` and `build80` were both red before this build. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0**, with no auction baseline supplied so those suites' own honesty blocks did not run (§6's one legitimate skip). Isolation guard ran and passed. All three inline scripts `node --check` clean. |
| **99** (staff unchanged) | `80e15b9` ⚠ subject reads "dmin 99" | 25 Aug 2026 | **THE COLLAPSE SWEEP REACHES THE REST OF THE ADMIN.** Owner, for the third time in three different wordings: *"There's too much data everywhere so it's hard to find what i'm looking for. Most things need to collapse throughout the site."* **Six sections join [96]'s mechanism** — Users → *Admin Access* and *Add user*, Open Shifts → *Post an open shift*, Shift Families → *Add a family*, and the two **Decided** lists on Requests and Swaps. Same `sectOpen` / `toggleSect` / `sectApply`; **no second mechanism** (§3 rule 14). What he came to each page for stays open: All Users, Posted, Families & shifts, Pending, Awaiting. **The two Decided bars carry a COUNT while shut** (*2 decisions*) and it steps aside when opened, so a closed bar still says what is behind it. **⚠ THREE PANELS THE BRIEF LISTED WERE DELIBERATELY NOT TOUCHED, and the reasons are the useful part.** *Which shifts count as overnight call?* and *Who counts toward the group comparison?* are **in Reports, not Stats**, already inside an `open <details>` — a bar there is a fold inside a fold; **Stats itself has no collapsible panel at all**; and `renderReports` draws FIVE of those targets in one function, which is the [96] blast radius squared. **Simulator Fill/Wipe were skipped too**: `renderRehearsal` assigns `className` outright (wiping any class a sweep adds) and dims them last, so a bad edit leaves a lock that LOOKS unlocked. **The four set-once sections are named in `SECT_STATIC`** — `toggleSect` ends in `else renderCatalog()`, so an unnamed key re-draws the catalog from the Users page. **GATES: new `tests/sched/build99-test.mjs`, 49 assertions, all measured from the laid-out page** — including a per-page [96] gate that asserts, with each new section CLOSED, that everything else on that page still renders, and a BEHAVIOURAL probe for the `SECT_STATIC` bug (plant a marker in the catalog summary, toggle a section on another panel, see whether it survived). Honesty against the pushed **98** (explicit SHA `67f9d36`): **26 of 49 FAIL**, the probe among them. **Schedule battery 49 suites, 1,289 assertions, ZERO skipped — 47 pass, 2 FAIL.** ⚠ **THE FIRST VERSION OF THIS ROW WAS WRONG ABOUT BOTH, and said "proven by execution" while being so — corrected the same evening.** Those two suites take their page from `SCHED_ROOT`, NOT from `ADMIN`, so every bisect run silently re-tested build 99 and returned the same number each time, which reads exactly like a clean result. Re-run correctly: **`build67` last passed at 95 (`be06fcc`) and first failed at 96 (`16e5977`)** — a stale assertion, not a defect; the catalog list is collapsed by default and its rows appear on opening. **`build80` passes on 95, 96 and 98 and fails ONLY here** — and not because this build introduced a `prompt()`. A stray `/*` inside a line comment at `admin/index.html:1157` had opened a block comment running 347,322 characters, and that suite's comment-stripper deleted ~70% of the page before testing it; the short `/* … */` added to `toggleSect` in this build closes it early and exposes a REAL blocking `prompt()` in the group rename that has been there since the groups build. **So this build hid nothing and fixed nothing — it stopped a gate lying.** Full account and the three decisions it raises: `TODO.md`. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0** — with **no auction baseline supplied, so those suites' own honesty blocks did not run** (§6's one legitimate skip; it was a regression gate here, not an auction build's gate). Isolation guard ran on the right bytes (md5 `576a7d7…`) and passed. All three inline scripts `node --check` clean. |
| **98** (staff unchanged) | `67f9d36` · pushed 25 Aug | 25 Aug 2026 | **A GROUP CARD IS ITS HEADER UNTIL YOU OPEN IT.** Owner: *"same for each group, that should collapse unless i'm adding someone. If I have 10-20 groups 3eventually, the page would scroll forever and be cluttered."* **The cost is countable:** each card draws a checkbox per person per side, so nine groups over a 35-person roster is roughly **315 checkboxes on one screen**. Closed, a card is now its header — which already carries the two things you go to it for: the NAME (given the size in [91], after nine cards read as identical) and the MD/CRNA counts, plus whether it counts toward the averages. Measured: the tallest closed card is **21px**. Opening one opens only that one, and an open card **survives a re-render**, so a Firestore snapshot cannot close the group he is working in — the same guarantee the catalog's family bands got in [96]. **THE SUBTLE PART: the header IS the toggle now**, so Rename, Delete and the fairness switch sit inside it and had to stop the click travelling up — otherwise renaming a group would also open or shut it underneath the dialog. That is asserted by CLICKING them, not by reading the source for `stopPropagation`. **GATES: new `tests/sched/build98-test.mjs`, 18 assertions, driven in a browser.** Honesty against build **97**, fixture taken byte-exact from the transfer bundle and md5-verified (`ad0f38c4…`, since 97 was built in the cloud and has no SHA): **8 of 18 FAIL**. One existing suite repointed: `groups-test` read a message that now lives inside a collapsed subgroup column, so it opens the cards the way an admin does before reading for it (38→39). **Schedule battery 49 suites / 1,561 assertions, ZERO skipped.** Isolation **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions**. `node --check` clean. |
| **97** (staff unchanged) | `67f9d36` · pushed 25 Aug | 25 Aug 2026 | **SHIFT ELIGIBILITY IS ONE LINE PER SHIFT, AND A [96] REGRESSION THE OWNER FOUND IS FIXED.** Owner: *"shift eligibility needs to be cleaner, to many words visible and too crowded. each shift should start collpased and be opened by admin."* **The cause, read out of the renderer rather than guessed: `_eligOpen` gated only the EDITING controls.** A CLOSED row still rendered its requirement chips, its allow/block person chips, and a dot-separated list of up to SIXTEEN doctors — up to four lines each — so closing a row barely changed the page. Now a closed row is ONE LINE (measured: 16px) and still answers the two questions the page exists for: *asks for Call team, OB team · any one* and *8 can work it*. Opening one row shows the chips, the selects and the names; the other rows stay one-liners; **navigating away closes it**, which is §104's single exception (dozens of rows can accumulate open where there are only a handful of sections). The **legend now appears only while a row is open**, because it explains chips that no longer exist on a closed row. **The filter bar becomes a collapsed section that NAMES what is filtering** (*call only*, *shift “abc”*, *only shifts nobody can work*) — a short board with no visible reason is the worst version of this page, not the cleanest. **⚠ AND THE REGRESSION, which is the more important half of this build.** Build 96 returned early from `renderCatalog` when the Shifts section was collapsed — **which is the default** — and everything below that line died with it: the catalog summary, the family dropdown, the SITE dropdown, the **holidays list and its Remove buttons**, the open demand dialog, and the copy-an-existing-shift picker. **Five of the six had nothing to do with the shift list.** The owner reported the visible tip: *"The holidays section should list the holidays with remove feature as well."* Fixed by skipping the WORK and not the REST of the function — `el.innerHTML = sectOpen.shifts ? catRowsHtml(...) : ''`. **The general lesson, worth more than the fix: an early return added for one panel silently owns every line after it.** A six-assertion regression gate now runs the whole catalog page with Shifts CLOSED. **GATES: new `tests/sched/build97-test.mjs`, 33 assertions, all measured from the laid-out page** — including row HEIGHT, after an earlier version of this suite counted `innerText` newlines and was wrong, because the browser puts a newline between flex children so it was counting spans, not lines. Honesty against the pushed **96** (explicit SHA `16e5977`): **18 of 33 FAIL**. Two existing suites repointed: `elig-test` now reads the closed row's WORDS and then opens the row to check the chip is still there (stronger — it asserts both forms); `build96-test` counted `.secth` document-wide while claiming *the catalog has four*, and is now scoped to the catalog panel. **Schedule battery 48 suites / 1,543 assertions, ZERO skipped.** Isolation **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions**. `node --check` clean. |
| **96** (staff unchanged) | `16e5977` · pushed 25 Aug | 25 Aug 2026 | **THE CATALOG OPENS AS FOUR CLOSED BARS.** Owner: *"I think the entire shifts card should collapse by default and then expanded opens to a list of the families, which can then by further expanded to find shifts… a scroller within a scroller can be hard to use… there also appears to be a double header at the top there that doesn't make sense."* **Add a shift, Paste a list, Holidays and Shifts all start collapsed**, each bar saying what is inside it; Shifts opens to a list of FAMILIES and a family opens to its shifts, so the inner scroller is no longer needed rather than merely taller; the two stacked column headers become ONE that matches the mode. State lives in memory (`sectOpen`/`toggleSect`/`sectApply`), so a section he opens survives navigation and a reload is the reset — §104. **⚠ THIS BUILD SHIPPED A REGRESSION**: the early return added for the collapsed Shifts section silently killed five unrelated renders on the same page, the holidays list among them. The owner found it the same day and it was fixed in **[97]**, which also added a six-assertion gate that runs the whole catalog page with Shifts closed. **GATES: `tests/sched/build96-test.mjs`, 36 assertions measured from the laid-out page** (figure from the suite's own commit `546bd51`) — what is closed on arrival, what a bar says while shut, and that opening one survives navigation. Nine suites repointed to the new default; three had assertions the new structure made untrue and all three came out STRICTER. **⚠ ROW WRITTEN 25 Aug, LATE — this build shipped without one, and the omission was found by a subagent survey, not by a gate.** Everything above is from the commit and the suite; **the honesty-check numbers were never recorded and are not reconstructable, so none are quoted here.** |
| **95** (staff unchanged) | `be06fcc` · pushed 25 Aug | 25 Aug 2026 | **EDITING A SHIFT OPENS A CARD, NOT A STRIP OF INPUTS.** Owner: *"i think would be better when editing a shift to immediately open a card for that shift that allows to better see everything"*, *"once edit is clicked, maybe a Done button and Delete shift button?"*, and *"The delete button isn't ideal. it should say delete shift"*. What he was looking at: the read row with every cell swapped for an input — eleven controls on one horizontal line, no heading, no Done, and a button reading only *Delete* with no object. **You could see what you were editing WITH, not WHAT you were editing.** The card is the SAME controls and THE SAME HANDLERS, laid out so the shift is the subject: label pill and kind, then Times · Length · Site · Family · Role · People per day as labelled fields, then the staffing rules written as sentences (*Weekdays (Mon–Fri) → 5 people*) each with its own ✎, then the internal id. **Delete shift is deliberately in the FOOTER, not beside Done** — Done is pressed every visit and Delete almost never, and side-by-side puts the destructive action under the muscle memory of the common one; its confirm, which names the shift, is unchanged (§3 rule 5). **BATCH MODE IS UNTOUCHED and that is the point** — §95 shipped edit-all for a whole-catalog sweep, and nine stacked cards would defeat exactly that, so the card is only for the ONE row the ✎ opens. No handler changed; no Firestore read or write changed; staff bytes untouched. **GATES: new `tests/sched/build95-test.mjs`, 30 assertions, all driven through the page's own controls in a real browser** — the card is proved WIRED, not merely drawn, by changing People per day on it and reading the write back (`dailysched/shiftCatalog {"shifts.D.capacity":9}`). Honesty against the FILED build 94: **24 of 30 FAIL**. That fixture had no SHA — 94 is filed and not yet committed — so it was rebuilt from `cb52344` plus 94's own patch and **proved by md5 against the device's filed copy** (`8dfbc672…`), which is a stronger identity check than a SHA here, not a weaker one. **THREE EXISTING SUITES WENT RED AND ALL THREE WERE REAL.** build54 finds the site `<select>` by its `title`, which the card had dropped for a visible label — fixed in the PAGE, no test edited. build85 pinned the render branch by its literal text — repointed to the three outcomes that now exist, so it is STRICTER (34→36). build86 EXECUTES the real `catRowsHtml` and its sandbox lacked the new renderer — given a symmetric marker exactly as `catEditRowHtml` already had, since that suite's subject is WHICH renderer the branch picks (19→20). Schedule battery **46 suites / 1,471 assertions, ZERO skipped**, run in-cloud on bytes md5-identical to the device's. Isolation **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions**. `node --check` clean on all three inline blocks. |
| **94** (staff unchanged) | `be06fcc` · pushed 25 Aug | 25 Aug 2026 | **A DIALOG CAN NO LONGER BE TALLER THAN THE WINDOW.** Owner, driving the shift catalog himself: *"add rule for shift pop-up screen is cut off and not entirely visible. it needs to be shorter or probably needs a scroller."* He was right and the cause was one missing pair of declarations. `.modal` centres its card and has never scrolled; `.modal .mcard` had **no max-height and no overflow at all**, so a card taller than the window overflowed BOTH ends — title above the top edge, Done button below the bottom one — and neither could be reached, because nothing in the stack was scrollable. The demand dialog hits it as soon as a shift has a few rules, since its 60-day preview alone is sixty cells. Fixed with `max-height:calc(100vh - 32px)` (32px is `.modal`'s own padding) repeated as `dvh` for browsers that know it, plus `overflow-y:auto`. **Scoped to `.modal .mcard`, which is both modals on the page and none on staff — the same defect was latent in the cell dialog.** No JS changed; no Firestore read or write changed; staff bytes untouched. **GATES: new `tests/sched/build94-test.mjs`, 16 assertions, every one a MEASUREMENT taken from the laid-out page in a real browser** — where the card sits, whether it genuinely scrolls, and whether Done can be scrolled to *and clicked* — because a CSS fix is invisible to any test that does not lay the page out (§3 rule 9: what would still pass if the page were inert?). It also proves the cap is a ceiling and not a permanent scrollbar: in a tall window the card does not scroll. Honesty `--pre` against the pushed **93**, fixture from the EXPLICIT SHA `cb52344`: **6 of 16 FAIL, and they reproduce the owner's report as numbers** — card top at **−49px**, bottom at 770px in a 720px window, `scrollHeight == clientHeight` so nothing scrolls. Schedule battery **45 suites / 1,438 assertions, ZERO skipped** (browser suites RUN in-cloud; 1,438 − 1,422 = the 16 new ones, reconciling to the digit). Isolation guard **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions, zero failures**, `test-audit-fixes` 339/339 with its schedule honesty baseline supplied from SHA `898535a` (admin 46). The auction honesty baseline was deliberately left ABSENT — no auction build changed, which is §6's one legitimate skip. `node --check` clean on all three inline blocks. |
| **93** (staff unchanged) | `cb52344` · pushed 25 Aug | 25 Aug 2026 | **§43 — THE FAIRNESS POOL MOVES ONTO THE GROUP, and the per-person switch becomes the override it was always going to be.** §26 ruled per diem and locums out of the fairness pools; §35 could not act on it, because a person carried a name and an FTE and nothing else, so it shipped a per-person switch in build 51 and wrote into the ruling that *"when stage 4 lands, the switch moves onto the group and the per-person flag becomes the override, not the source."* Stage 4 landed in 88–91. This build performs that migration. **The flag lives on the group document** (`fairness:false`, stored ONLY when false — a group counts unless it says otherwise, so a group created tomorrow needs no write to behave correctly, and turning it back on removes the field rather than storing a second way of saying yes). **EXCLUSION WINS across several groups**, the same shape as §98's block-beats-allow: somebody in Per diem AND in OB is per diem, because the flag names a pool a person does not belong in and a second group cannot argue them back into it. A person in NO group has no group answer, so the pre-group behaviour stands. **NOTHING STORED CHANGES MEANING and there is no migration step** — the old document held exclusions only and absent meant "counts"; absent now means "follow your groups", and a group counts by default, so an untouched roster resolves to exactly what it resolved to yesterday. That equivalence is asserted by replaying a build-51-shaped document. **§43 is the one ruling in this project where Claude argued and was overruled** — §19, two places answering one question — and the owner accepted it *with a mitigation that is part of the ruling*: the ruling says it "only holds if the visibility above is actually built". All three conditions are built and each is a gate. **(1) On the person:** `poolStatusText()` gives both halves in the ruling's own wording — *"In Per diem, which does not count toward averages — individually overridden to count"* — and `whyNotInPool()` now names WHICH of the two decided, because "excluded by an admin" could no longer tell a reader where to go and change it. **(2) On the group:** each card carries the toggle and LISTS the members whose individual setting disagrees with it, by name and by direction. **(3) Inside the report:** `buildModel` carries an `overridden` list — somebody overridden INTO the pool never appears in `excluded`, so without it they are the one exception the report could not mention — printed in §29's method paragraph and in the **spreadsheet export too**, because a report that is honest on screen and silent in the file people circulate is not honest. **Only a disagreement is stored:** a tick that agrees with the person's groups is written as `deleteField()`, so they go on FOLLOWING their groups and a later change to the group reaches them; storing the agreeing value would freeze them at today's answer and produce exactly the silent second source §19 warns about. The confirm names the overrides it is about to keep, and the audit row records them. **Three defects were found by the gates, not by reading:** ① `whyNotInPool` called a stored `false` an "individual decision" even when it AGREED with an excluding group — the group was the real reason, and the sentence would have sent the admin to untick a person while the cause stayed put (caught by the non-browser suite); ② the compPool listener marked only `reports` dirty, so **§43·2's override list on the group card would never have repainted when an override changed** — the one screen the ruling requires it on (caught by CLICKING it in the browser suite); ③ **the defect-29 shape**: `runReport` and `runFairnessView` had no gate on `groupsLoaded`/`membersLoaded`, so a report run in the cold-load window would have counted per diem in and printed a wrong expected figure for every doctor with nothing saying so — both now refuse and say why. Gates: NEW `tests/sched/build93-test.mjs` **50/50**, EXECUTING the whole nine-function resolution chain (never stubbed — a stubbed `fteOf` would let §36's "no FTE means no share" quietly stop being true) over multi-group exclusion, override in both directions, the no-group case and the replayed legacy document · `groups-test.mjs` extended to **39** and **CLICKS the new toggle on the rendered card** — build 88 shipped with every control on that page dead and every non-browser suite green, because those suites executed functions and nothing had ever clicked anything · `build51-test` re-based on the new wording (moved, not weakened: it still asserts the exclusion is named and why) · honesty `--pre` vs the pushed **91**, fixture from the EXPLICIT SHA `dc29760` (md5 `c0da209c853af161f0dde14d0147693f`, verified byte-identical to the pre-patch file): **1 passed, 31 FAILED, exit 1**; groups-test `--pre` 30/8 fail. **Schedule battery all 44 suites / 1,286 assertions green with ZERO skipped** (the browser suites RUN — a playwright/chromium version mismatch in this sandbox was resolved rather than skipped past). Isolation guard 29/29. **Full auction battery per §2: 54 suites / 2,050 assertions, zero skips.** `node --check` clean. Staff bytes unchanged. No rules change. ⚠️ **Recommendation recorded, not built:** `tests/audit-handlers.mjs` — the lint for the exact bug class that killed build 88 — covers only the two VACATION pages. Pointed at the schedule pages it reports 17 violations, all false: it reads literal `on*="…"` in static HTML, and the schedule admin generates almost all of its handlers inside JS strings. A lint with false positives is a lint that gets ignored, so it was NOT extended. The real check would be "every interpolation inside a generated handler goes through `jsArg`", which is a page-wide refactor and unasked. |
| **92** (staff unchanged) | `cb52344` · pushed 25 Aug | 25 Aug 2026 | **S5c'S OTHER HALF — the grid filters by MD/CRNA and by GROUP.** Build 84 shipped the filter bar with a hole in it and SAID SO in the markup: filtering by anything about the person was not deferred out of taste, it was impossible, because a person carried a display name and an FTE and nothing else (§22). Stage 4 gave them a category and group membership, so the hole closes on real attributes. `gridPersonMatch(u,cat,group,catOf,inG)` is pure and takes its two lookups as arguments, so the battery drives it with its own fixtures instead of booting a page; an empty filter never calls its lookup, so an unfiltered grid cannot break on data that has not arrived. The two filters are **AND, not OR** — which is the whole point of narrowing twice — and an uncategorised person is never quietly counted as an MD. **Three failure modes drove the design, each worse than a wrong row.** ① **SILENCE:** a grid with people hidden looks exactly like a roster that lost people, so the banner counts the new filters and names both the category and the GROUP BY ITS LABEL (`OB_ANESTHESIA` is not what the admin picked); the empty-grid message says when the person filters alone emptied it, rather than blaming the coverage filter. ② **A CONTROL THAT LIES:** the Shifts view draws shifts, not people, so both selects are DISABLED there and the banner does not count them — a category select that looked live there would be filtering nothing while appearing to filter, and `gridShiftRows` never consults them, which keeps its own "1 of 2" count honest. ③ **A FILTER POINTING AT NOTHING:** deleting a group while it is selected clears the filter instead of hiding every person for a reason no longer on screen. The group drop-down is refilled by the RENDER (groups arrive from Firestore after boot, so a list built once with the toolbar would be permanently empty) from `groupList()`, so it carries the owner's family order (§94) rather than a second, different one, and is only rewritten when the options actually change — rebuilding a `<select>` on every paint would drop the choice just made. All three group listeners now mark the grid dirty, so removing somebody from a group does not leave them sitting in a filtered grid until an unrelated render. Not remembered across a reload, like the other two. Gates: NEW `tests/sched/build92-test.mjs` **38/38**, executing the matcher · `build69`/`build70` re-based with the page's OWN matcher and lookups supplied, never stubs — a stub would let a filter that hides the wrong people pass unnoticed · honesty `--pre` vs the pushed **91** (SHA `dc29760`): **3 passed, 8 FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. ⚠️ The first patch attempt failed its own anchor check on a mis-indented line and **exited without writing** — the file was verified unchanged at build 91's md5 before re-anchoring, rather than the mismatch being forced through. |
| **91** (staff unchanged) | `dc29760` · pushed 25 Aug | 25 Aug 2026 | **SELECT ALL, AND THE GROUP NAME READS FIRST — both asked for by the owner while he was actually setting the groups up, which is §90's whole argument arriving on schedule.** **§ SELECT ALL / NONE**, per subgroup, per group: *"I will need a select all option."* Ticking 35 people into nine groups one box at a time is the friction that makes an admin abandon setup half-done. **Adding is one merged write and asks nothing** — it grants, and every grant is visible and individually undoable. **Clearing passes through a confirm that NAMES the count and the people** (§3.5: no one-click destructive action), because it can silently drop ten minutes of somebody's work. Neither direction touches the other side of the group, and neither touches a person whose category is unset — they are on no side, and a bulk action must not quietly decide that for them. The buttons only appear when they would do something: no *all* on a full side, no *none* on an empty one. **§ THE GROUP NAME READS FIRST.** He seeded his nine groups from the shift families, got them correctly, and read the result as *"they all got called MD subgroup 0"* — because the name was 12.5px sitting between two uppercase `MD SUBGROUP · 0` headings, so nine cards looked identical and the only thing identifying a card was the smallest text on it. The name is now 15px/800 with the counts demoted to a muted chip, and the column headings are quietened. **This is the §93 day-board lesson for the third time: the thing you came to read must be the thing you see first.** He had deferred the fuller layout rework (*"will have to change that layout, but fine for now"*) — that stays deferred and is recorded in TODO; this is the part he then asked for outright. Gates: `groups-test.mjs` extended to **27/27**, driving select-all by CLICKING it — a second MD added, one click puts both in, the CRNA side is proved untouched, then *none* is clicked and the dialog is asserted to name the count and dismissing it is proved to change nothing. **Schedule battery 42 suites, ZERO skipped** (in-cloud). Isolation **29/29**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **90** (staff unchanged) | `47af2ea` · rules `5994a1e` · pushed 25 Aug | 25 Aug 2026 | **THE GROUPS PAGE WAS DEAD ON ARRIVAL IN BUILD 88, AND THE OWNER FOUND IT — not the battery.** His words: *"I see the Groups tab, but I'm unable to edit or make changes."* **§ THE DEFECT.** Every control on that page built its inline handler with `JSON.stringify(u)`, which returns `"AR"` — **with double quotes** — dropped straight into `onclick="…"`. The quote CLOSES the attribute, so the browser parsed a truncated handler and threw `Unexpected end of input` on every click. Category buttons, membership checkboxes, Rename and Delete: all inert. Add Group appeared to work only because that one button takes no argument. **§ WHY NO TEST CAUGHT IT, and this is the part worth keeping.** `build88-test` **executes the functions**, and every function was correct — `groupSides`, `categoryOf`, `inGroup`, every writer's gate, all green. **What was broken was the WIRING, and nothing had ever CLICKED anything on that page.** This is FAST-1 (admin 303) in different clothes: bytes present, feature unreachable, suite passing over it. **§ THE FIX.** One helper — `jsArg(v) = esc(JSON.stringify(v))` — used by every inline handler that carries an argument; `esc` turns `"` into `&quot;`, which the HTML parser decodes back before JS ever parses the handler. The catalog's family bands have done this since build 85; the Groups page simply did not. **§ AND THE DEAD END BEHIND IT.** An empty subgroup said *"nobody has this category yet"* — twice, once per side — which reads as broken software. It now says **"Nobody is marked CRNA yet — set that above, and they appear here to tick."** The mechanism was there; the page never told him the order of operations (categories first, then people become tickable), and §99 calls that intuitive-ness a standard, not a nicety. **§ THE GATE THAT WAS MISSING NOW EXISTS:** NEW `tests/sched/groups-test.mjs` **20/20**, driving the real page in a real browser — it CLICKS Add group, CLICKS the MD button, TICKS the membership box, UNTICKS it, and checks the fake Firestore store after each, plus zero page errors throughout and the cardinal rule. A source lint in `build88-test` catches the same anti-pattern statically. **A crude second lint ("every handler must end in `)`") was written and then REMOVED** — it flagged legitimate assignment handlers, and a lint with false positives is a lint that gets ignored. **Schedule battery all 42 suites / 1,198 assertions, ZERO skipped** (in-cloud; md5-identical bytes both sides). Isolation **29/29**. **Full auction battery 54 / 2,050, zero skips.** `node --check` clean. Staff bytes unchanged. **No auction file touched (§92).** |
| **89** (staff unchanged) | `655d49b` · pushed 24 Aug | 24 Aug 2026 | **§98 — THE ELIGIBILITY REBUILD. The tick grid is RETIRED and the page becomes a board.** Owner: *"The current ticks should be forgotten, they mean nothing… it will never be useful to have the option to view 3,000 ticks at once."* **§ WHAT WENT, and it went properly:** every writer (`setElig`, `eligAll`, `eligBandBulk`), the click/crosshair delegation, the listener, the `eligRef` handle and the `elig` state variable — removed, not disabled, because a dead writer left in place is what a later session wires back up by accident. A tombstone says what was removed and why. **`dailysched/eligibility` is untouched on disk** and is now read by nothing (§3: nothing is ever deleted). **This supersedes §53b·1 and §56** — option C and the cutover switch both rested on those ticks meaning something, and the owner has said they do not; re-interpreting 3,000 meaningless ticks into exceptions would have manufactured data rather than preserved it. He agreed to drop the switch outright. **§ WHAT A SHIFT CARRIES NOW:** a list of **groups**, or **one named side of a group** (`OB|MD`), plus per-shift **individual allow and block** lists, plus an any-one/all mode (§53a). **BLOCK BEATS ALLOW BEATS GROUP** — an override a group could silently outvote would not be an override, which is precisely what he asked for. Adding somebody to one list removes them from the other, so the answer can never depend on which check ran first. **§ A whole-group entry asks only about membership; a SIDE entry also asks the person's category** — so somebody with no category still qualifies through a whole-group entry, and their missing category is surfaced on the Groups page rather than silently costing them work. **§ THE SHIFT'S OWN MD/CRNA/Both MARKING NO LONGER GATES ELIGIBILITY.** It used to be the only way to say "MD side"; a side can now be named outright, and two mechanisms for one answer is §19's forbidden second source of truth. `role` stays a catalog label and a filter. **§ A SHIFT THAT NAMES NOTHING ADMITS NOBODY** (his choice, put to him with the alternative) and says so on its own row — an unconfigured shift that silently admitted everyone would look finished. **§ THE BOARD.** One quiet line per shift; controls only on the row you open — **the first cut rendered two selects and two buttons on EVERY row, which is the clutter §93 rejected, and it was rebuilt before he saw it.** Filter by family, type and text; sort four ways; a "only shifts nobody can work" filter; and a **By person** view answering the other question — what can this person work, and is anybody in no group. **No dense mode is offered and none should be added: deliberately unlike the Shift Catalog (§95), because he ruled the dense view useless here.** **§ Removing a person now clears their groups, their category and every per-shift override naming them** — otherwise reusing initials would quietly re-admit somebody. Gates: NEW `tests/sched/build89-test.mjs` **48/48** executing the rule in every shape (precedence, both modes, both sides, no-category, unset vs nogroup); `elig-test.mjs` **REWRITTEN, 31/31**, driving the board through its own selects and buttons in a real browser. Honesty `--pre` vs the pushed **83** (`e260f4f`): **24 FAILED, exit 1**. **Schedule battery all 42 suites / 1,245 assertions, ZERO skipped, in-cloud on byte-identical bytes (md5 both sides).** Isolation **29/29**. **Full auction battery 54 / 2,050, zero skips.** ⚠️ **Six suites were re-based and none weakened** — detail in HANDOFF; the isolation guard also learned that `mergeFields()` is a write, which it had never known. `node --check` clean. Staff bytes unchanged. **No auction file touched (§92).** |
| **88** (staff unchanged) | shipped inside `655d49b` (one commit for 88–89) · pushed 24 Aug | 24 Aug 2026 | **STAGE 4 — GROUPS AND SUBGROUPS.** The shape is the owner's (§97): a **GROUP** is the unit people name — OB, Pediatric, ICU — and it holds exactly **two subgroups**, its MD side and its CRNA side. **A person joins the GROUP; their category picks the side** — they are never asked which, because that would be a second way to say what the category already says (§19). Nobody crosses (§53b). Three new documents, all in `dailysched/*` and never in the roster the live auction shares (§2): `groups`, `groupMembers`, `categories`. New Groups page: set who is an MD or a CRNA, add people to groups, rename and delete groups, and **seed groups from the shift families in one click** — *seeded, not chained*, because whether a group stays tied to its family is not settled and must not be assumed (§22). **§ Somebody in a group with NO category is on neither side, and the page says so** rather than hiding it — it is the one thing that would stop them being eligible for anything. **§ Every writer is gated on all four snapshots having arrived** (the [48] pattern; defect 29 is what skipping it costs), and deleting a group removes the memberships with it so no invisible keys are stranded. **§ §98·3 arrived after this build and is surfaced here:** every user should be in at least one group — counted and named on the page, and deliberately NOT enforced by refusing a save, since everybody is at zero the day this ships and a rule that blocks setup would be obeyed by nobody. Gates: NEW `tests/sched/build88-test.mjs` **36/36**, executing group membership, the two sides, the no-category case and every writer's gate. Honesty `--pre` vs the pushed **83** (`e260f4f`): **FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** ⚠️ **A rules gap this build creates, and it is NOT closed:** `firestore.rules` lets any *registered user* write those three documents, because they are not named in `isSchedAdminOnlyDoc()`. Group membership decides who may work what, so they belong on that list — but the rules live in the AUCTION repo, which makes it an auction deploy, **closed without the owner's specific decision (§92)**. Nothing is live and the schedule has no users, so it is not urgent; it is recorded in TODO as the one outstanding item of Stage 4. |
| **87** (staff unchanged) | `ac47833` · tests `ff590fd` · docs `3096ce5`/`1371f08` · pushed 24 Aug | 24 Aug 2026 | **§95 part 3 — A PASTED BLOCK LANDS FULLY CONFIGURED.** Owner: *"Ideally, I'd also be able to add a group of shifts from a copy paste list that can read the data… Then I could enter a block of shifts with times all at once."* **⚠️ Paste-a-list was NOT built here — it has existed since build 56**, with a preview before anything is written, an update-existing option, per-row rejection reasons and duplicate detection inside the block. What it READ was `label · start · end · site` and nothing else, so a pasted block still needed a second pass by hand. **That gap is what this closes, and nothing else.** Columns are now `label · start · end · site · family · role · capacity`, tab or comma separated, and you may stop after any of them — **a three- or four-column list written months ago pastes exactly as it always did**, which is pinned by two assertions. **§ A PASTE NEVER CREATES A FAMILY.** An unknown family REFUSES the row and names the families that do exist. One typo would otherwise scatter shifts into a near-duplicate nobody meant, and a family is a deliberate thing with a colour attached (§22). Families match by label or by internal key, in any case. **§ Role must be MD, CRNA or Both** (normalised to the catalog's spelling) and **capacity a whole number 1–20** — the same ceiling the row editor enforces; anything else is refused BY NAME rather than silently coerced (§17). **§ COLOUR FOLLOWS FAMILY, on both paths:** a pasted new shift is born with its family's colour like one added by hand, and moving a shift between families in a paste carries the colour with it — otherwise the old colour would survive and §94's whole colour reading would quietly lie. **§ Before/after now includes family, role and capacity**, so *already exactly this* stays true for a row that only moves a family. **§ Demand rules stay OUT** and the page says so: they are a structured per-shift editor, and inventing a text syntax for them is the kind of guess §22 forbids. Gates: NEW `tests/sched/build87-test.mjs` **31/31**, EXECUTING the real parser (with the page's own `parseHHMM`, not a stub) over three-, four- and seven-column rows, commas, a header row, every refusal shape, four bad capacities, a rejected row surrounded by good ones, and a duplicate inside the block. Honesty `--pre` vs the pushed **83** (`e260f4f`): **0 passed, 10 FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **86** (staff unchanged) | shipped inside `ac47833` (one commit for 84–87) · pushed 24 Aug | 24 Aug 2026 | **§95 parts 1+2 — THE CATALOG DOES BOTH: ONE AT A TIME, AND BATCH.** Owner: *"I need a way to do both edit 1 at a time and big batch edits."* S7 (85) made the page read-first, which is what §93 asked for — and in doing so it removed the tab-straight-down-the-list pass that **build 55 exists to protect**. This puts that back as a MODE rather than as the default. **One at a time** stays the default: quiet lines, ✎ opens one row. **Batch — edit all** renders every row as its editor, the pre-85 page on demand, so a whole-catalog sweep is one uninterrupted tab-through. The [55] mid-edit guard protects it exactly as it always did. **§ THE CHOICE IS REMEMBERED**, like the grid's view and density ([69], [70]) and for the same stated reason: it changes how the SAME data looks. The catalog's problem FILTER is deliberately NOT remembered — that one HIDES shifts, the same distinction [84] draws. **§ Batch mode says what it is and what it costs** — about eleven controls per shift — rather than pretending the dense view is free. **§ Switching modes closes any single open row**, so the two ideas of *open* cannot disagree; an unknown mode is refused rather than leaving the page with no rows. Gates: NEW `tests/sched/build86-test.mjs` **19/19**, EXECUTING `catRowsHtml` in all four states (read, batch, read-with-one-open, batch-with-one-open) and proving the two modes compose with collapsed bands and the problem filter — so *show me the twelve with no demand and let me fix them all in one pass* actually works. **The stubbed editor row returns a real `<div>`, not `''`** — a stub must have the SHAPE of the real function or the suite could pass over a mode that renders nothing (the FAST-1 lesson, written into the suite). Honesty `--pre` vs the pushed **83** (`e260f4f`): **0 passed, 14 FAILED, exit 1**. ⚠️ **AND THE BATTERY FOUND A REAL DEFECT IN THIS BUILD BEFORE IT SHIPPED:** clicking ✎ on a second row, or switching modes, did NOTHING while a field still held focus — the [55] mid-edit guard defers re-rendering the list, and these handlers honoured it. That guard exists to stop a SNAPSHOT destroying what you are typing; **the admin's own click is the opposite of that** — asking for the list to change IS the request. A new `catUserAction()` clears the guard on all four deliberate actions (✎, mode switch, band collapse, problem filter). `build55-test` caught it by driving the page, not by reading it. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **85** (staff unchanged) | shipped inside `ac47833` (one commit for 84–87) · pushed 24 Aug | 24 Aug 2026 | **S7 — READ-FIRST CATALOG ROWS.** The Shift Catalog rendered **eleven live controls for every shift, all the time** — roughly a thousand on a real catalog — with NO column headers, so what each control meant lived in a hover tooltip (invisible on a touch screen, and to anyone in a hurry). Now each shift is **one quiet line** — colour pill · kind · times+duration · site · demand · capacity · role · a badge for each thing missing — and **only the row you open has inputs**. **§ THIS IS A SAFETY FIX AS MUCH AS A TIDINESS ONE.** Every input on this page is a re-render hazard: a Firestore snapshot arriving while an admin tabs through 92 shifts destroys the element they were about to reach ([55], and defect 12 before it). **One open row is one hazard instead of ninety-two.** The editor itself is UNCHANGED — the same markup, the same handlers, the same guards; S7 changes WHEN it renders, not what it does. **§ ONE DEFINITION OF WHAT IS WRONG WITH A SHIFT.** New `catShiftProblems()` decides *no times / half set / provisional / no site / no demand*, and it now feeds BOTH the badge on a row AND every count in the summary card — which previously computed five separate filter expressions of its own. That is build 78's defect in miniature (two readers, two definitions, one of them wrong), closed before it could happen. **§ THE SUMMARY COUNTS ARE NOW BUTTONS** (UX-PLAN fix 4): clicking *3 with no site* filters the list to exactly those three; clicking it again clears. A filter that matches nothing says so and offers the way back. **§ COLLAPSIBLE COLOURED FAMILY BANDS** (fix 3) — 92 rows become ~10 groups, each band carrying its family colour and its count, so a collapsed band never hides shifts silently. Bands appear only in family sort; a band under an A–Z sort would be a lie ([62]). **§ STICKY COLUMN HEADERS** (fix 2) — the meanings finally leave the tooltips. **§ AND §94 REACHES THE THIRD LIST:** `catalogList()`'s family grouping was still A–Z, so the same catalog read in two different orders on two adjacent pages. It now uses `bdFamRank` like the day board and the Families page. Gates: NEW `tests/sched/build85-test.mjs` **34/34**, EXECUTING the real `catShiftProblems` over ten shift shapes including a null shift and the provisional-only-when-times-are-set case, plus structural pins that the read row emits NO `<input>` at all, that the editor still calls the same three writers, and that the summary derives every count from the shared function. Honesty `--pre` vs the pushed **83**, fixture from the EXPLICIT SHA `e260f4f`: **0 passed, 16 FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **84** (staff unchanged) | shipped inside `ac47833` (one commit for 84–87) · pushed 24 Aug | 24 Aug 2026 | **S5c — THE GRID FILTER BAR.** A name/text box and two coverage filters, pinned in the grid toolbar. The text box hides ROWS — matching a display name **or the initials**, which is how you find one person out of 35 quickly, and the shift label or id in the flipped view. The coverage select hides DAY COLUMNS: *short days only* or *fully covered days*, read from the same `coverageRows` roll-up the coverage strip and the Coverage page use. **§ THE FILTERS GO INERT WHEN NOTHING IS ASKED FOR.** With no demand rules anywhere, §9's *not asked for* is not the same as *covered*, so filtering to short days would hide the entire month on a question nobody has answered. Both coverage modes therefore do nothing in that state, **and the banner says the filter is doing nothing** rather than leaving an empty grid to be misread. **§ THE REAL RISK HERE IS SILENCE, and it is handled explicitly.** A grid with rows or days hidden looks exactly like a month that is empty or a roster that has lost people. So: a blue banner above the grid states what is filtered and counts what is shown (*6 of 35 people · 12 of 31 days*), a **Clear filters** button sits inside it, and when the filters match nothing the table body SAYS so instead of rendering blank. **§ NEITHER FILTER IS REMEMBERED across a reload — deliberately.** View and density ([69], [70]) are remembered because they change how the SAME data looks; a filter changes what you can SEE, and a remembered one greets you tomorrow with people missing for a reason you have forgotten. **§ ROLE AND SITE ARE STILL NOT FILTERABLE, and that is not taste:** people on this site carry only a display name and an FTE — `role` and `site` are fields on SHIFTS. Do not design around an attribute that does not exist (§22); that half waits for Stage 4. **§ The filter box lives in the TOOLBAR, outside the element the render replaces** — inside it, typing would be eaten mid-keystroke, which is defect 12 exactly. Gates: NEW `tests/sched/build84-test.mjs` **33/33**, EXECUTING the real `gridRowMatch` (case, fragments, initials, trimming, null labels) and the real `gridVisibleDays` (short / covered / all, and the inert case), plus pins that all three of renderGrid's loops walk the SAME visible-day list so a header column can never drift from the cells under it. Honesty `--pre` vs the pushed **83**, fixture from the EXPLICIT SHA `e260f4f`: **4 passed, 7 FAILED, exit 1**. ⚠️ **It also broke two existing suites, exactly as intended:** `build69-test` and `build70-test` eval the REAL `renderGrid`, which now asks for two functions that did not exist — both were given the page's own extracted code (never a stub — the FAST-1 lesson), and `build70`'s direct `gridShiftRows` call was updated for its new signature and now also asserts the row count it reports. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **83** (staff unchanged) | `e260f4f` · tests `0832ebf` · docs `ae11548` · pushed 24 Aug — ⚠️ **all three subjects say "82"; the bytes are 83** | 24 Aug 2026 | **§94 — THE SHIFT FAMILY ORDER IS THE OWNER'S, NAMED BY HIM, ON BOTH PAGES.** *"Call at top, then AP, then weekday daytime, then ob, then richmond, then pediatric, then PCV, then ICU, then admin, then unassigned. Also re do shift families section to this order."* Build 82 had INFERRED an order — whichever family held `kind:'call'` shifts first, the rest alphabetical. Reasonable, and superseded within the hour: **a named order beats an inferred one, and the inference is now gone from the page entirely** (a test asserts `hasCall` appears nowhere). **§ TWO ORDERS, DELIBERATELY DIFFERENT.** RANK is his list. MATCHING is tried MOST-SPECIFIC-FIRST, so a family called *Richmond Daytime* lands under Richmond rather than under weekday daytime — the generic words are matched last, on purpose. Synonyms are the ones a real catalog will actually hold: AP also matches *acute pain*, pediatric also matches *peds*, ICU also matches *critical care*, OB also matches *obstetrics*, admin also matches *administration*. **§ A FAMILY HE NEVER NAMED IS NOT GUESSED AT (§22)** — it sorts alphabetically AFTER his ten and BEFORE Unassigned, so the department can invent families this code has never heard of and they appear somewhere predictable rather than vanishing. **§ "Unassigned" is his word** for the bucket build 82 called *Other*, and it is ALWAYS LAST — which also moved it on the **Shift Families page**, where it had led the row as a drop target since that page was built. **That page now sorts with the SAME comparator as the day board**, so the two can never drift apart; its old `localeCompare` is gone. Gates: NEW `tests/sched/build83-test.mjs` **32/32**, EXECUTING the real `bdFamRank`/`bdFamCmp` over all ten positions shuffled, every synonym, the Richmond-Daytime specificity case, an unnamed family, an empty label and a null one. Honesty `--pre` vs the pushed **81**, fixture from the EXPLICIT SHA `8084190`: **0 passed, 4 FAILED, exit 1** — baselined against 81 and NOT 82 deliberately, because 82 was filed and never separately committed, so no fixture for it exists or ever will (START-HERE's rule: baseline against the last PUSHED build and say so). `build82-test.mjs` was amended in the same breath — its six family-order assertions were REMOVED rather than left to contradict §94, and its three `Other` assertions became `Unassigned`; the file says where the order now lives. **Schedule battery: all 35 suites green, 995 assertions, ZERO skipped.** Isolation guard **27/27**. **Full auction battery per §2: 54 suites / 2,050 assertions, zero skips.** `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **82** (staff unchanged) | shipped INSIDE `e260f4f` — see build 83 | 24 Aug 2026 | **THE DAY BOARD, REBUILT — the owner rejected 81's version and said exactly why (§93).** His words: *"not a useful addition in it's current form… I want to very easily be able to see who IS working and care less about who isn't. I want clean and organized, not cluttered."* Build 81 answered the brief and missed the point: five statistics, two strips, four footnotes and a per-site column layout around the one thing he asked for. **What research into daily anesthesia boards actually says** — the enduring artefact is the DAILY ASSIGNMENT SHEET: an ordered list where the people are the payload and the shift is the label; *who is on call* is the question asked most; and the schedule's value is that it is glanceable and public (ASRA's call-schedule guidance makes visibility the thing that earns acceptance). **So the board is now one table.** Grouped in shift families using the catalog's own colours, **call first** — decided by the catalog's `kind`, not by guessing at family names — then alphabetical, catch-all last. Inside a family: by start time, untimed rows at the bottom, then catalog order, then name. Columns are TIME · WHO · SHIFT · SITE, the site column appearing only when any shift has one. **ON NOW is the only badge** (plus *on now · since yesterday* for the overnight case): a *later* pill on every row meant the whole sheet wore one at 02:00, and the time column already says when. **§93's rules, applied:** the *not on anything today* LIST is gone; vacation is a COUNT behind a drawer; the four footnotes are one line; a name is a name — the *· no e-mail* marker from 81 is retired as Users-page housekeeping. **The five dashboard cards are one line of six numbers** (requests · swaps · coverage · on vacation · people · latest change), each opening a drawer, one at a time. **Every element id the old cards carried is unchanged, so `renderDash()` was not touched at all** — and the suite proves it by reading the ids OUT of the real `renderDash` body and checking each still exists in the markup, rather than trusting a hand-typed list. Gates: NEW `tests/sched/build82-test.mjs` **60/60**, EXECUTING `bdRowSort` (seven orderings, including the one it CAUGHT: two shifts with no `order` both give `NaN`, and `NaN!==NaN` is true, so the old shape returned 0 from the order branch and never fell through to the name — fixed in this build because a test found it), `bdFamOf` over four catalog shapes and `bdFamCmp` over six. Honesty `--pre` vs the pushed **81**, fixture from the EXPLICIT SHA `8084190`: **19 passed, 29 FAILED, exit 1**. `build81-test` **58/58 STILL GREEN on the rebuilt page** — the day board's logic is unchanged and now has a regression gate behind it; its two brittle assertions (an exact build number, and the card-wall's position) were replaced with the questions that hold forever. **Schedule battery: all 34 suites, 969 assertions, ZERO skipped** (in-cloud, browser suites RUN). Isolation guard **27/27**. **Full auction battery per §2: 54 suites / 2,050 assertions, zero skips.** Rendered and SCREENSHOTTED before hand-over — the real CSS and the real `renderBoard`, driven in a real browser over sample data, at midday and at 02:10, which is how the overnight row was confirmed by eye and not only by assertion. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **81** (staff unchanged) | `8084190` · tests `e64b110` · pushed 24 Aug | 24 Aug 2026 | **S6 — THE DAY BOARD, AND IT IS THE LANDING PAGE.** Approved 18 Aug (*"build and yes"*), built under **§90** (base features before defect polish) and **§91** (no contact number). The admin now opens on **who is on today, at which site, and who is on right now** — the five dashboard cards keep their place underneath, and the first nav entry reads *Today*. Each site gets a column; each shift shows its family chip, its catalog times, a running state, and the people on it, every name a `mailto:` on the KP address the Users page already holds. Two strips follow: who is on vacation today (from the auction's approvals, read-only as always) and who is on nothing. **§ WHAT "NOW" MEANS — decided in the code, because the brief asked for a decision and not a guess.** A shift belongs to the day it STARTS: `Call 24` dated Tuesday runs Tue 19:00 → Wed 07:00, so **at 02:00 on Wednesday the person on duty is on YESTERDAY's shift and today's has not begun.** A board that read only today's record would show an empty hospital at exactly the hour somebody needs to know who is in it — so it reads the previous day too and lists those people as *still on from yesterday*, but only while they genuinely are. A shift with no times is never placed on a clock; it says *no times* instead. **§ WHAT IT COSTS: nothing, normally.** Assignments live one document per day ([59]). When a day it needs is inside the month the grid already holds — the usual case, since the grid opens on today — it reads that state and opens NO listener. Only for a month the grid is not showing does it subscribe, and then to **one document per day plus the month's marker**, never the month's day collection: a board that needs two days must not pay for thirty (FB-3, one bill across both sites). Per [66] it never fetches per render; a one-minute ticker only marks it dirty, so *on now* stays true as the clock moves without costing a read. **§ LOADING IS NOT EMPTY.** `monthLoaded` was added for this: until the month's first snapshot lands the board says so, rather than rendering an unarrived day as *nobody is on* — the same distinction [59] draws between an unconverted month and an empty one. **§ Shortfalls come from `demandOn()`** — the SAME function the Coverage page and the grid's coverage strip use. Build 78 exists because auto-populate had grown a second definition of what a day needs; this board does not add a third. Where nothing has demand rules it says so in §9's words rather than claiming full coverage. Gates: NEW `tests/sched/build81-test.mjs` **58/58**, EXECUTING the real `bdShiftState` across midnight, the 24-hour case and the untimed case (24 assertions on the clock decision alone), the real `bdCellsOn` over eight loading/legacy/routing states, and the real `bdEnsureSubs`/`bdSubFor` under fake listeners that COUNT what is subscribed — proving nothing is watched before sign-in, nothing is re-read for the grid's own month, and the day collection is never listed. Honesty `--pre` vs the pushed **80**, fixture from the EXPLICIT SHA `64888af` (md5-verified against the pre-edit file): **1 passed, 19 FAILED, exit 1**. **Schedule battery: all 33 suites green, 909 assertions, ZERO skipped** (browser suites RUN, in-cloud). Isolation guard **27/27** — the cardinal rule re-proved. **Full auction battery per §2.** `node --check` clean on all three inline blocks. Staff bytes unchanged. No rules change. **No auction file was touched (§92).** |
| **tests only** | `36c2a53` (tests) · pushed 24 Aug | 24 Aug 2026 | **THE COVERAGE GAP CLOSED: builds 71–74 and staff 31–35 finally have a suite in THIS battery.** They shipped with none. Their only executable gate was `tests/test-appcheck-login.mjs`, which lives at the tests ROOT and is swept up by the **auction** battery — so five schedule builds were being guarded by the other site's suite, which is exactly the mixing `tests/sched/README.md` exists to prevent. NEW `tests/sched/appcheck-signin-test.mjs`, **28 assertions**, covering both pages: App Check initialised BEFORE `getFirestore` (a token must exist before the first read) with the two distinct Firebase app names the badge selector depends on; staff's readiness fallback raised 1500→3000 ms. **It pins the REVERSAL honestly rather than pretending both shipped:** build 72's rule that un-hid the reCAPTCHA badge must be ABSENT (§66 undid it), and the attribution that Google requires once the badge is hidden must be PRESENT with both links — the suite refuses to let those two drift apart, because hiding the badge without the attribution breaks Google's terms. Plus: exactly ONE security box per page, outside both sign-in panels (the duplicate-per-panel version is explicitly forbidden in the markup note), and staff 35's retired passcodes listener gone. Two assertions were rewritten mid-build because they tested PROSE rather than CODE — a tombstone comment names the very variable it removed, so `!/passcodesData/` failed on a correct page. A third was removed outright for being a tautology (`… || true`). Honesty `--pre` vs the pushed admin **70** / staff **30**, fixtures from the EXPLICIT SHA `2c97296`: **24 of 28 FAIL, exit 1**. Tests only — no page bytes, no build number. |
| **80 · staff 37** | `64888af` · tests `caf960f` · pushed 24 Aug | 24 Aug 2026 | **THE REQUEST LIFECYCLE TELLS THE TRUTH TO THE PERSON WHO ASKED.** Two defects, one subsystem, no roster writes. **§ defect 15 — WITHDRAW.** A submitted request could only ever be decided by an admin; there was no way to take it back. People ask for a day off and then no longer need it, and the only route was to message the admin and hope. Now a pending request, an open-shift claim and a swap you PROPOSED can all be retracted. **The guard runs on FRESH data inside the transaction, never on the rendered row** — by the time the click lands an admin may already have decided it, and *a withdrawal must never undo a decision*. Refused if it is not yours, or no longer pending. **Deliberate scope on swaps: only while it is still `pending_target`.** Once the other side has accepted (`pending_admin`), retracting unilaterally would undo someone else's agreement, so that case is not offered. A 3-way reads its initiator from `participants[0]`. **§ defect 16 — A DENIAL CARRIES A REASON.** Deny was one click writing a bare status, so the requester saw a flat "❌ denied" and had to come and ask why. **The machinery already existed on the APPROVE path** — an override reason captured and written to the audit log — it had simply never been wired to deny. Implemented as an INLINE OPTIONAL field, not a modal, for two reasons stated in the code: a blocking `prompt()` would stall the 21 Playwright suites that drive this page, and forcing a reason would slow an admin working a queue. Empty behaves exactly as before. **The ordering is the load-bearing part:** the reason is read BEFORE the transaction, because the row re-renders the moment the status changes — reading afterwards would find an input that no longer exists and silently record nothing. Bounded to 140 characters. Shown to the requester, to the admin's decided list, and in the audit line; the machine-written `autoDenied` reason for a lost claim race is now surfaced too, having previously been stored and never displayed. A withdrawn SWAP also joins the admin's decided list — it never reaches admin, so without that it would vanish from every view instead of being recorded. Gates: NEW `tests/sched/build80-test.mjs` **26/26**, EXECUTING the real `withdrawRequest`/`withdrawSwap` and capturing what they hand the transaction — **stubbing `txUpdateItem` to "just succeed" would have proved the button exists and nothing about whether it is safe.** The race case is asserted explicitly: an already-approved item is refused. Honesty `--pre` vs the pushed admin **79** / staff **36**, fixtures from the EXPLICIT SHA `3e816d1`: **1 passed, 23 FAILED, exit 1**. **Schedule battery all 32 suites green with ZERO skipped**; isolation 27/27; **full auction battery per §2: 54 suites / 2,049 assertions, zero skips.** `node --check` clean on both pages. No rules change. ⚠️ **Two of this suite's own assertions were wrong first** — one tested a COMMENT (`!/prompt\(/` failed because the comment explains why prompt was avoided) and one used too small a search window and reported a FAIL on correct code. The suite now strips comments before textual assertions. Third time this session; the fix is now in the file rather than in a resolution. |
| **79** (staff unchanged) | `3e816d1` · tests `38f4ce5` · pushed 24 Aug | 24 Aug 2026 | **THE USERS PAGE — kept as its own build ON PURPOSE.** It is the single sanctioned exception to the cardinal rule: the one place the schedule writes the roster the LIVE auction reads. Build 78 carried the grid work so that if anything here misbehaves, the blast radius is identifiable. **§ S-hygiene — the KP address is normalised on save.** The LOGIN (Google) address has been trimmed and lower-cased at every writer since [47], because `firestore.rules` matches it case-sensitively. The KP address — which shares `vacations/emails` with the auction and is read back by BOTH sites — had `.trim()` and nothing else, **not even an "@" check**. The auction has normalised it since ITS build 270 and lower-cases at READ time, so the divergence was MASKED rather than absent: this page could write a mixed-case or malformed address into a document the auction believes is clean. `_normKpEmail` is ported and is **byte-for-byte identical to the auction's copy — verified by extracting both function bodies and comparing, not asserted**. That identity is the point: two sites writing one document must agree on what it may contain, and a "slightly better" copy here would be the very divergence this closes. Scope unchanged from the owner's 17 Aug ruling — trim, lower-case, refuse five unarguable shapes, NO domain policy. Wired at all three LIVE writers, each degrading the way that field deserves: `addSchedUser` still ADDS the person and refuses the address out loud (failing a whole add over a secondary e-mail would be worse than the thing it prevents); `saveSchedField('em')` refuses instead of storing; `saveAllSchedUsers` fails the BATCH, exactly as a bad login e-mail or FTE always has — it was the one field on that page with no validation at all. There is no fourth call site: `saveSchedUser` was deleted in 77, so the helper was not written into dead code. **§ defect 29 — `schedFteLoaded` is finally READ.** It had been set since the FTE listener was written and consulted by nothing, while `saveAllSchedUsers` turns a blank FTE box into `deleteField()`. Before that snapshot arrives every box is blank, so **one click on Save All in the cold-load window would have wiped every FTE in `dailysched/fteMap`** — the [48] shape, on the one flag already shaped for the job. Now gating both FTE writers alongside its three siblings. Gates: NEW `tests/sched/build79-test.mjs` **30/30**, EXECUTING the real extracted `_normKpEmail` over trim/case, missing-@, internal space, double-@, dotless domain, leading/trailing dots — and over two cases that guard the SCOPE rather than the code: a gmail address must survive (no domain policy) and an apostrophe/plus address must not be refused for being unusual. Honesty `--pre` vs the pushed **78**, fixture from the EXPLICIT SHA `e141d13`: **11 of 20 FAIL, exit 1** (20 not 30 because the executed block reports a single legible failure when the helper is absent, rather than throwing). **Schedule battery all 31 suites green, ZERO skipped** (browser suites RUN, not skipped). Isolation guard 27/27 — the cardinal rule re-proved after a build that touches roster writers. **Full auction battery per §2: 54 suites / 2,049 assertions, zero skips.** `node --check` clean. Staff bytes unchanged. No rules change. |
| **78** (staff unchanged) | `e141d13` · tests `36c2a53` · pushed 24 Aug | 24 Aug 2026 | **AUTO-POPULATE FINALLY ASKS THE DEMAND RULES — plus a stats read-storm guard and one more dead listener.** Three items, all read/compute paths; nothing here writes the roster (that is build 79's blast radius, kept separate on purpose). **§ defect 5:** `autoPopulate` and `autoPopulateYear` used the static per-shift `capacity` for EVERY day of the month and never called `demandOn()` — which the coverage report has used since build 58. So a weekdays-only shift was filled on Saturdays and Sundays; a shift with NO demand rules — §9's *"not being asked for at all"*, which is not the same as "nobody needed" — still had somebody assigned every single day, and then appeared in the `unfillable` warnings for slots nobody had ever requested. **The filler and the report were working from two different definitions of what a day needs, and only one of them was the owner's.** Both now take their target from a new `autoTargetFor()`: `null`→nobody, `0`→nobody, `n`→n. **Deliberate fallback, and it is a product decision worth knowing:** if NOT ONE shift in the catalog carries demand rules, the old `capacity` behaviour stands — otherwise auto-populate would silently do nothing on a catalog nobody has configured yet. The condition is the same `shiftsWithDemand` count `coverageRows()` uses for `demanded`, so the two cannot drift. Both confirm dialogs now say which of the two behaviours will run (§84's carve-out: wording that causes a wrong action is a defect), and both completion alerts NAME the shift-days skipped as not-asked-for, so "complete — 12 assignments" can never read as a bug. **§ defect 13:** `renderStatsYear` had no in-flight guard — `statsYearLoadedFor` is set only after the 12-month `Promise.all` resolves, so a second click, or any of the ~9 listeners that call `renderAll()` while the panel is open, started another twelve reads. The existing staleness check catches a fetch for a DIFFERENT year and structurally cannot see a duplicate of the SAME one. Now one fetch at a time, released in a `finally`; a deliberate Refresh still forces a re-fetch, so the guard blocks only accidental re-entry. **§ defect 25:** `dailysched/callTotals` and its listener deleted — a legacy fairness counter superseded by `windowShiftCounts()`, still subscribed on every page load, feeding a variable with ZERO read sites. Same shape as the passcodes listener removed in staff 35. One fewer Firestore read per admin page load; the document itself is untouched. Gates: NEW `tests/sched/build78-test.mjs` **26/26** — defect 5 proved by EXECUTING the real `demandOn`/`autoTargetFor` chain over weekday / weekend / holiday / explicit-zero / stacked-rule / no-rules-anywhere cases, and defect 13 proved by EXECUTING `renderStatsYear` three times concurrently and counting **12 reads, not 36**. Honesty `--pre` vs the pushed **77**, fixture from the EXPLICIT SHA `a796d02`: **0 passed, 16 FAILED, exit 1** — and the suite was changed to report a missing build-78 function as a legible FAILURE rather than throwing, because a crash and a regression are indistinguishable from an exit code. **Schedule battery: all 30 suites green with ZERO skipped** — the 21 Playwright suites were made to RUN (cloud, module installed), so this build was gated by the browser suites and not by watching them skip. Isolation guard 27/27. **Full auction battery re-run per §2: 54 suites / 2,049 assertions, zero skips.** *(That re-run was delayed by a stale desktop sign-in; the COST GATE was applied — the owner was told rather than a workaround being ground out — and in the meantime the three auction suites that actually read the schedule admin page were run on the device: 84/84, 338/338, 93/93. The full battery followed once he re-authenticated.)* `node --check` clean on all three inline blocks. Staff bytes unchanged. No rules change. |
| **tests only** | `bc751aa` (tests) · pushed 24 Aug | 24 Aug 2026 | **THE SCHEDULE BATTERY STOPS LYING ABOUT WHAT IT RAN.** ⚠️ **Its commit message does not mention this change** — the message was written before the fix and pushed unrevised, so this row is the record (same ruling as `cbd4a5b`/`427cab8` below: the CODE is right, and rewriting a pushed message is a bigger risk than an inaccurate label). `sched/run-all.mjs` decided whether to skip its 21 Playwright suites by testing for a chromium **BINARY** — but every browser suite's first line is `import { chromium } from 'playwright'`, and `playwright` is not a dependency of this repo. On a machine with the binary and no module all 21 RAN and died on `ERR_MODULE_NOT_FOUND` ("21 of 27 failed"); on the owner's Mac, which has neither, they skipped cleanly. **Same battery, same code, opposite report — which is how two rows in this very file came to read "27/27 with ZERO skipped" and "N green + 21 skipped".** It now asks the two questions the suites themselves ask, in their order, and NAMES WHICH ONE FAILED: can `playwright` be imported, and can a browser actually **launch**. The launch is real, not a path check — `executablePath()` names the full chrome build while `chromium.launch()` will use the headless shell, so the first attempt at this fix skipped a run that would have worked. **Substituting a cheaper proxy for the real question is exactly how the old gate lied.** The summary now prints `⚠ N of 28 suite(s) DID NOT RUN — this is NOT full coverage` beside the green tick, and each skip reads `SKIPPED, not passed (<reason>)`. Proven BOTH ways: on the Mac it names the missing module and skips 21 loudly, exit 0; in the cloud with playwright installed it reports `ENABLED — playwright launched a chromium successfully` and runs **all 28 green, zero skipped** — where the old code reported 21 FAILURES. Tests only — no page bytes, no build number. |
| **77** (staff unchanged) | `a796d02` · pushed 24 Aug | 24 Aug 2026 | **THE CALL-FAIRNESS BASELINE CAN NO LONGER BE ERASED BY A CLICK — plus one dead roster writer removed.** `saveBaseline` REPLACES `dailysched/callBaseline` (correct: the table is the full source of truth and a blank box means zero — so `{merge:true}` is NOT the fix, it would make blanking impossible). But `renderBaseline()` emits **no inputs at all** when the roster is empty or the catalog holds no call shifts, and the Save button stayed live either way — so one click during a slow, empty or permission-denied load wrote an empty map and destroyed every baseline. That document is the **one input that cannot be recomputed from the schedule**; it exists to record call worked BEFORE the system. 77 counts the boxes it expects using the SAME id derivation the writer uses, and refuses the whole write if even one is missing — a partial table is never saved over a complete document. Unchanged since admin build 30. **Second change: `window.saveSchedUser` DELETED (defect 26).** No call site since July 2026, when the Users grid moved to per-field saves — yet it was one of the **six sanctioned handlers** in `tests/sched/isolation-test.mjs`, the cardinal-rule allowlist for writing the roster the LIVE auction reads, and still carried real roster writes, the [47] duplicate guard and the [48] loaded-gate on unreachable code. **The sanctioned set is now FIVE and equals the reachable write surface — a stricter guard, not a looser one.** Three test pins moved with it and NONE is a weakening: the isolation allowlist shrinks; `build75-test`'s sibling COUNT goes 3→2 while its direct assertion (the gate inside `addSchedUser` itself) is untouched; and `test-audit-fixes`'s pin on the deleted function is removed — the other three roster writers keep theirs, one of them EXECUTED. Gates: NEW `tests/sched/build77-test.mjs` **23/23**, executing the real extracted `saveBaseline` against a DOM stub shaped like the real render (a box exists only where the table drew one — a stub that always returned a box would have hidden the defect, which is the [304 · FAST-1] lesson). Honesty `--pre` vs the pushed **76**, fixture from the EXPLICIT SHA `6d31b3e`: **9 of 23 FAIL, exit 1** — a real failure, not a skip. Schedule battery **all 28 suites green with ZERO skipped** — the 21 Playwright suites were made to RUN by installing playwright in the cloud, so for the first time in this site's history a schedule build was gated by the browser suites instead of watching them skip. Isolation guard green. Full auction battery re-run per §2: **54 suites / 2,049 assertions, zero skips** (2,050 − the one pin removed above — reconciles exactly). `node --check` clean on all three inline blocks. Staff bytes unchanged. No rules change. No new Firestore reads or writes. |
| **auction admin 299** (staff unchanged) | `2cd6a55` · pushed 21 Aug (one commit with 298) | 21 Aug 2026 | **§79 — approvals and denials are BLOCKED while bidding is open, not merely warned about.** Owner: *"we currently have a warning not to do that, but blocking is better."* Confirmed first that `adApprove` and `adDeny` consulted NOTHING about bidding state — the readiness warning rendered in the dialog body but the click was never stopped. **ONE definition of open:** the expression was LIFTED out of `approvalReadiness` into a single named `biddingOpenOnServer()` that both it and the new guard call, so they cannot drift — SERVER truth only (the biddingClosed flag, or an ENABLED timer past its deadline), because a merely switched-OFF timer once read as "closed" while every bidder could still write from devtools. **Scope, deliberate:** the five CREATING surfaces are gated (Approve, Deny, Approve All Current Winners, Deny All Losers, confirmApprove); the two REVOKES are NOT — a revoke only ever removes a decision, and blocking correction would trap the admin with a stale decision and no way out (§72: a false refusal is its own incident). The refusal names what is blocked, why, and which button clears it, and in Phase 4 it names the ROUND rather than the phase. **Gates:** NEW `test-admin-299-bidding-open.mjs` **18/18**, executing the real extracted handlers against real server states — including the historical timer-OFF trap, both genuinely-closed states, the revokes staying available, and a structural pin that readiness DELEGATES rather than keeping a copy · honesty `--pre` vs the explicit SHA `6bbb4af` (admin 297 — **298 was filed but never pushed, so per §4 the baseline is the last PUSHED build and this says so**) fails **12 of 17**, and the 5 that pass are exactly the must-keep-working ones · full auction battery **48 suites / 1,881 green** · schedule isolation guard 27/27. **Four suites went red and each was fixed at the cause, not the symptom:** `test-delta-fixes` and `test-high-fixes` pinned the readiness expression by its literal text, so the pins were RE-POINTED to its new home and gained an assertion that readiness delegates — moved, never weakened; `test-c4-phase-identity` got the real guard plus a CLOSED server state (the only state a decision is legitimate in); `test-admin-296` opens the gate for its §72 floor scenario under the file's existing convention, naming the suite that pins it. **Schedule battery RE-RUN after the owner re-authenticated: 27/27 in-cloud against admin 299, ZERO skipped.** (It was blocked mid-session — staging refused with `untrusted_device` — and the §6 cost gate was obeyed: the owner was told the 30-second fix instead of a workaround being attempted.) **Live verified after the push: `versions.json` = admin 299 / staff 161 / mobile 18, fetched twice.** The SCHEDULE repo's commit `fe168c5` carries GitHub's default subject "Update BUILD-LOG.md" rather than the prepared summary — **the owner was asked and is fine with it** (21 Aug: *"I'm ok with the brief uninformative commit summary"*), so it is NOT to be re-raised. Noted only because it is the fourth time and it costs nothing: the CONTENTS are correct, and these rows are the record precisely so a thin commit subject never matters. |
| **auction admin 298** (staff unchanged) | `2cd6a55` · pushed 21 Aug (one commit with 299; suites in tests `985e9b4`) | 21 Aug 2026 | **§77 LANDS — the projection freezes, and §82d is fixed before it ever shipped.** The §70 apparatus is gone (`_natFilter`, `_natReqs`, `_natContested`, `_blockFloor`, the demotion loop); `computeApprovals` is computed from BIDS ALONE, so an approval or denial can no longer re-deal a week — the defect the owner had SEEN as the projected list rearranging under him. `decidedWinners(wk)` and `weekLedger(wk)` answer "who is getting this week" and "how full is it"; four consumers repointed. **The pre-commit check earned its place:** a differential harness ran BOTH real engines (297 and the draft 298) over ~4,000 random week states and found that `weekLedger.committedFte` never subtracted the DENIED — under §77 the projection still lists them, so denied bids counted as filling a week and 2,864 of 3,958 states disagreed with 297, worst case a capacity-3 week reading 0.2 remaining when every bidder on it was denied. One line fixed it, and the same harness proved the fix: differences fell to 852, exactly the intended re-deal cases, matching the three consumers that were already correct. **Suites (§80):** `test-admin-294-engine.mjs` ARCHIVED (it guarded deleted machinery); `test-priority-inversion`, `test-engine-fuzz` and `test-never-events` had their denied-is-never-a-winner assertions REPLACED — not rewritten to agree with the new code — by the §77 INVARIANCE (run the real engine twice, with and without decisions, demand identical output); NEW `test-admin-298-frozen` 17/17 and NEW `test-admin-298-readouts` 4/4, the latter proving its own teeth by putting the §82d defect back. `test-c4-phase-identity`'s sandbox updated — it went red because `adApprove` gained a real ledger dependency, which is exactly what a battery is for. **Gates:** full auction battery **47 suites / 1,861 assertions green**, run with PRE_ADMIN set so the in-suite honesty checks executed (priority-inversion reports 12 assertions, not 11) · `test-admin-298-frozen --pre` vs the explicit SHA `6bbb4af` FAILS 11 of 12 · never-events 5a and the readouts mutation both fire · full schedule battery **27/27 green in-cloud with ZERO skipped** (first complete run — 21 browser suites had been skipping on the owner's Mac) · isolation green · CRNA restamped 298 · `node --check` clean. One archaeological honesty check (the pre-239 mutation) now says loudly that it did NOT run rather than passing silently — its baseline predates the standing fixture rule. |
| **36 (staff) · 76 (admin)** | `6d31b3e` · pushed 20 Aug | 20 Aug 2026 | **RA-1 fixes.** Staff 36 · RA-H12: Phase-4 vacation weeks finally PAINT — a completed Phase 4 shows its winners once results are e-mailed (currentPhase never advances past 4, so the old ph>=curPh skip hid a whole year of awarded weeks), and ANNOUNCED Phase-4 round wins paint all year under the same released-results embargo. Admin 76 · RA-M7: a failed bid-security-map sync can no longer hide behind the caller's green "Saved" — persistent audit-log row + the warning re-issued after the success toast. Gates: `sched/ra-fixes-36-76-test.mjs` 10/10 executed, honesty vs `dc6b8cc` FAILED exit 1; registered in sched run-all; isolation 27/27; auction battery re-run green (standing rule). |
| **75** (staff unchanged) | `dc6b8cc` · pushed 20 Aug | 20 Aug 2026 | **AUDIT LOW — addSchedUser gains the build-48 loaded-gate.** Its duplicate-login-email refusal consulted a possibly UNLOADED loginEmailsData mirror — vacuous in the cold-load window, so a duplicate address slipped past ENTRY (the fail-closed sync backstop still blocked NE-6, but entry refusal is the never-event’s named first guard). Same gate + wording as its three siblings. Cardinal rule: adds NO reads or writes. Gates: NEW `tests/sched/build75-test.mjs` 4/4 (executed, non-Playwright) · honesty `--pre` vs pushed `4f2e7ef` fails 4/4, exit 1 · schedule battery 26 suites green · isolation 27/27 · full auction battery 34 suites / 1,638 green (re-run after a schedule build per §2) · node --check clean. |
| **tests only** | tests `1d9e8da` · pushed 19 Aug | 20 Aug 2026 | **PW-1 — the Playwright harness learns App Check.** Since 71/31 both pages import a FOURTH gstatic module, `firebase-app-check.js`, which the 21 browser suites’ swap lists never gained — on any machine that cannot reach gstatic (the cloud containers) every page-load suite died at boot; 20 of 25 failed in-cloud on 20 Aug. Fix: the fourth URL added to every suite’s SWAPS and an inert `fake/firebase-app-check.js` (no-op `initializeAppCheck`, stub `ReCaptchaV3Provider`). Harness only — no page bytes change, no build bump. Honesty for a harness change: `staff30-test.mjs` crashed at page load pre-fix, 20/20 post-fix. Gates: schedule battery **25/25 suites green in-cloud** (first time since App Check shipped) · full auction battery 23 suites / 1,495 green · isolation 27/27 · `node --check` clean on all 21. |
| **staff 35** (admin unchanged) | `3fda419` · pushed 19 Aug | 19 Aug 2026 | **FB-5 PREP — the retired passcodes listener is deleted** (auction staff 150). Identical defect and identical fix: a live listener on the auction's `vacations/passcodes`, written into a variable nothing reads, with `_schedSignInDataReady()` blocking this site's sign-in card on it. Removing it also removes one of only four things this login screen waits for. The schedule ADMIN page never referenced the document at all, so nothing there changes. Cardinal rule respected — this REMOVES a read of a `vacations/*` document, it adds none, and the isolation guard is green. Gates: shared `test-appcheck-login.mjs` 72/72 with honesty at `0d84ece` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,482 green · node --check clean. |
| **74 · staff 34** | `0d84ece` · pushed 19 Aug | 19 Aug 2026 | **FB-6 — the security box now shows on BOTH sign-in panels** (auction 284/149). Same defect and same fix: "Remember me" is checked by default, so returning users got the WELCOME BACK panel and never saw the box — which since 73/33 carries Google's required reCAPTCHA attribution. These pages use their own panel ids (`signPicker`/`signWelcome` on staff, `authGateCard`/`authGateWelcome` on admin), which is exactly why the fix was written against each page's real structure rather than copied from the auction. Gates: shared `test-appcheck-login.mjs` 64/64 with its own honesty baseline at `fb42ed7` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,470 green · node --check clean. |
| **73 · staff 33** | `fb42ed7` · pushed 19 Aug | 19 Aug 2026 | **HIDE THE BADGE AGAIN — schedule half** (auction 283/148; DECISIONS §66). Owner reversed §65 after seeing the badge live. Un-hide rule removed; the sign-in security box gains Google's required reCAPTCHA attribution with both links, because hiding the badge makes that mandatory. Same paired invariant enforced by the shared suite. FB-6 (the box is invisible to returning users) applies to these pages too and is deliberately deferred. Gates: shared `test-appcheck-login.mjs` 52/52 · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,458 green · node --check clean. |
| **72 · staff 32** | `4fa726d` · pushed 19 Aug | 19 Aug 2026 | **SHOW THE reCAPTCHA BADGE — the schedule half** (auction 282/147; DECISIONS §65). Same single prefix-selector CSS rule, same reason: Firebase App Check hides the badge inside its own `display:none` container, and the owner wants it visible. Prefix selector matters more here than anywhere — these two pages use app names `sched-user-site` and `sched-admin`, so a rule copied from the auction with a hardcoded id would have silently done nothing on both. No other bytes change. Gates: covered by `tests/test-appcheck-login.mjs` (52 assertions across all six pages) with its own honesty baseline at `324c797` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,458 green (re-run after a schedule build per §2) · `node --check` clean. |
| **71 · staff 31** | `324c797` · pushed 19 Aug | 19 Aug 2026 | **APP CHECK — the schedule half of the same build** (auction 281/146; DECISIONS §63). The schedule shares the `vacation-25e8e` Firebase project with the auction, so it shares ONE App Check registration and one site key — and, as FB-3 already records, one quota and one bill. Both pages initialise App Check before `getFirestore`, with auto-refresh on. Staff page: the same 1500 → 3000 ms sign-in readiness fallback as the auction, for the same reason (App Check adds a reCAPTCHA script load and a token fetch in front of the first Firestore request; the old guard could unlock the sign-in card with an empty person list), and the warning now names which document was missing. Both pages take the replaced login security box (DECISIONS §64). **Enforcement is NOT enabled** — console step, staged after monitoring. Cardinal rule respected: no auction bytes are touched by this half, and the auction battery was re-run after it anyway (§2 — it has broken three times). Gates: `tests/test-appcheck-login.mjs` covers both schedule pages with the same structural checks and the same executed init block · honesty from explicit SHA `34f19b7` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,443 green · `node --check` clean. |
| **70** | `2c97296` · pushed 19 Aug | 19 Aug 2026 | S5b (UX-PLAN S5, part 2 of 3) — THE GRID FLIPS. A View switch beside the S5a Density switch: **People** is the original grid, one row per person; **Shifts** turns it on its side — one row per shift in catalog order, each with its family-colour swatch, and cells naming who is on it. A cell under its demand gets an inset ring — amber at half or better, red below — and `box-shadow` was chosen over a background so the ring STACKS with the weekend and today tints instead of fighting them. A shift with no demand is never marked short (§9: silence is not a gap). Density applies to both views: Compact shows filled-over-needed instead of name chips. Choice remembered in `localStorage['dsGridView']`, read in a try/catch like S5a. **Read-only by design, stated:** a shift-view cell can hold several people, so there is no single obvious thing a click should edit; editing stays in the People view rather than inventing a second editor. Worth revisiting if the owner wants it. Admin page only; staff bytes unchanged; no rules change; no new Firestore reads or writes. Gates: NEW `tests/sched/build70-test.mjs` 26/26 executing the real `gridShiftRows`/`renderGrid` against a DOM shim (non-Playwright, so it RUNS) · honesty `--pre` vs the pushed 68 fails 13/13 · **build69-test.mjs's shim updated** — `renderGrid` gained a second axis and the S5a suite went red until it declared `gridView`, which is exactly what a battery is for · schedule battery 4 suites green + 21 skipped · isolation 27/27 · auction battery 22 suites / 1,403 green · `node --check` clean. |
| **69** | `2c97296` (one commit with 70) · pushed 19 Aug | 19 Aug 2026 | **MESSAGE MIS-PASTE, recorded not rewritten (same as `7fcd3f1`/`594778e` on 18 Aug):** commit `2c97296` carries the TESTS repo's summary, and an older over-length draft of it at that. The CONTENTS are correct and verified — `admin/index.html` at BUILD 70, `versions.json` admin:70, this log's two rows — and live is serving 70/30. The real record is these rows, which is exactly why they exist. S5a (UX-PLAN S5, part 1 of 3) — THE GRID FINALLY SHOWS WHERE THE HOLES ARE. **Coverage strip:** a second sticky header row under the dates, one cell per day, green when that day's demand is fully filled, amber under half short, red otherwise, showing the shortfall as `-n`. It reads the SAME `coverageRows()` the coverage report uses — build 69 adds a per-day roll-up (`byDay`) INSIDE the existing loop rather than recomputing, so the strip and the report cannot drift apart; a suite assertion pins that the per-day totals sum to the report totals. Hidden entirely when no demand is configured anywhere (`demanded`), so a department that never sets demand sees no empty strip. **Density toggle:** Detail keeps the text chips; Compact swaps each for one family-coloured 9px block, capped at three with a `+n` so a busy cell cannot blow the row height — labels move to tooltips, nothing is lost. Choice remembered in `localStorage['dsGridDensity']`, read inside a try/catch so a browser refusing storage still boots on Detail. **Scope, stated:** S5 as specced is FOUR features; this is two of them. The flipped shifts-as-rows view is S5b. The filter bar is S5c and its role/site half is BLOCKED — people carry no role or site attribute today (only `usernamesData` and `fteMap`; MD/CRNA and site live on SHIFTS), so row grouping waits for Stage 4. Admin page only; staff bytes unchanged; no rules change; no new Firestore reads or writes. Gates: NEW `tests/sched/build69-test.mjs` 35/35, executing the real `coverageRows`/`renderGrid`/`cellBlocksHtml` against a DOM shim — deliberately NOT Playwright so it RUNS in the battery instead of skipping · honesty `--pre` vs the pushed 68 fails 19 of 20 (coverageRows is loaded separately so the byDay assertions genuinely run and fail there, rather than the suite bailing early) · schedule battery 3 suites green + 21 Playwright-skipped · isolation guard 27/27 · auction battery 22 suites / 1,403 green · `node --check` clean on all three inline blocks. |
| **68 · staff 30** | `7fcd3f1` · pushed 18 Aug (one commit with 67) | 18 Aug 2026 | S4 (UX-PLAN) — THE LAST {list:[]} DOCS GO PER-ITEM. requests, swaps and openShifts were the only single-doc arrays left: every submit/decide transacted on the WHOLE list (publish-day contention at 80 users) and the arrays grew without bound. Same cure as the audit log in 63: one doc per item in dailysched/{requests|swaps|openShifts}/items/{id}. Readers merge legacy + items (item wins by id; _gone = tombstone); ALL writes go to item docs; the three legacy docs are frozen forever; a legacy item migrates itself the first time it is touched — no conversion step. Claims get structural dedup: doc id = claim_{openId}_{user}, with the legacy array still checked in the same transaction and decided history archived before re-apply. BOTH pages must deploy together. ⚠️ firestore.rules gained the dailysched subcollection match (also needed by 59 days/ and 63 entries/ — the repo file never had it); publish to the console BEFORE pushing these pages, and first COMPARE the console’s current rules against the repo file. Gates: build68-test 24/24 ×3 · staff30-test 20/20 ×3 · honesty vs 67 = 21 fails, vs staff 29 = 15 fails · ALL 23 schedule suites green (isolation/50/61/staff29 probes re-anchored to read both places; fake deny gained RegExp) · auction battery 15/1,101 green. |
| **67** | `7fcd3f1` · pushed 18 Aug (one commit with 68) | 18 Aug 2026 | S3 (UX-PLAN) — THE QUICK-WINS VISUAL BATCH. Five hard-to-read sections got the Families treatment. GRID: real thead + sticky header and name column in a scrolling wrap, today highlighted, federal holidays dotted. ELIGIBILITY: opens on the FIRST family instead of the 90-column wall — "All" is one click (latch guarded against the boot listener storm, which briefly ran it with an empty catalog). STATS: default month view is one column per FAMILY (family colors in the headers) + Call total/Total/OFF/VAC + a Group-total row; "Every shift" toggle keeps the full matrix, now with sticky name column. REPORTS: run cards first; the two config lists moved below them into an OPEN ⚙️ Settings fold; overnight-call chips grouped by family; comparison pool split FTE-set / no-FTE. CATALOG: sticky column-label bar over the shift list in its own scroll wrap. Gates: build67-test 28/28 ×3 · honesty vs 66 = 21 fails · ALL 21 schedule suites green (elig-test now clicks "All" before asserting the whole grid — entry point changed, no assertion weakened) · auction battery 15/1,101 green. |
| **staff 29** | `106addb` · pushed 17 Aug | 17 Aug 2026 | §55 — USERS CANNOT REQUEST INELIGIBLE SHIFTS (defect 4's staff half, closing defect 4 entirely). The staff page had subscribed to the eligibility grid forever and never read it. Now: the request dropdown offers only the signed-in user's eligible shifts; the submit handler refuses an ineligible shift even against a manipulated page; late-arriving eligibility refreshes the list; a no-ticks user gets an honest empty state. Hard block by owner ruling — a deliberate exception to §51, which still governs admin overrides. Stored request shape unchanged. Gates: staff29-test 19/19 ×3 · honesty --pre vs 28 = 7 pass/12 fail on exactly the defect · isolation 27/27 · elig 33/33 · build64 re-run green · auction battery RAN GREEN in-session 17 Aug (14 suites / 1,074, honesty skipped). |
| **66** | `623804e` · pushed 18 Aug | 18 Aug 2026 | S2 (UX-PLAN) — DIRTY-FLAG RENDERING. Every snapshot used to rebuild ~20 panels including hidden ones — the disease behind every typing-eaten defect. Now listeners mark panels dirty; one animation-frame flush renders the chrome (badges, banners, cell modal) plus only the VISIBLE panel; hidden panels render fresh on entry; boot still paints everything once (gated on roster+catalog arrival, not on flush count — a first-flush race the suite caught). The [64] typing guards unchanged and re-proven. Suites 59/62 now navigate before asserting hidden panels, as a user would. Gates: build66-test 14/14 ×3 · honesty vs 65 = 5 fails · ALL 20 schedule suites green · auction battery 15/1,101 green. |
| **65** | `aab97a2` · pushed 18 Aug | 18 Aug 2026 | S1 (UX-PLAN) — THE SECTION MAP. §S00 table of contents + 21 greppable §S banners, each naming owned state and writes; the Users banner carries the live-auction ⛔; auth-gate CSS pointer added. NO behavior change — proven by the ENTIRE battery green on 65. FINDING recorded: 8 suites had been red at full-sweep since build 63 (audit probes read the legacy array); anchors updated, no assertion weakened; full sweep is now 19 suites green and the claim is executed, not recalled. Auction battery 15 suites / 1,101 green against this page. |
| **—** (no build: admin 64 / staff 29) | `b8c078e` · pushed 18 Aug | 18 Aug 2026 | **THE OVERNIGHT REVIEW — `design/UX-PLAN.md`.** Commissioned by the owner on going to bed, answers expected in the morning: a per-section visual diagnosis with fixes, industry patterns, missing-feature gaps, a code-organization verdict and a proposed build sequence. **No code and no build number changed.** It is load-bearing rather than a one-off document: builds **65 (S1, the section map)**, 66-68 and 70 cite it by section as the source of what they shipped. **⚠ ROW WRITTEN 25 Aug, LATE.** The artefact had no row and no ruling, so the thing several builds were built FROM was unrecorded — found by a subagent survey. The owner's verbatim brief is in `anesthesia-kp.github.io/TODO.md` §1. |
| **64** | `13db983` · pushed 17 Aug | 17 Aug 2026 | Defect 12 — TYPING SURVIVES. renderAll() rebuilt the Users grid and the baseline grid on every snapshot, replacing every input mid-edit; unsaved typing died. Build 55's catalog guard (whose comment named defect 12 as the same shape) now covers both grids: rows are left alone while focus is inside; one rebuild with fresh data on focus-leave (250ms settle). Same accepted trade as [55]. Gates: build64-test 19/19 ×3 · honesty --pre vs 63 = 9 pass/10 fail on exactly the defect · isolation 27/27 · elig 33/33 · auction battery RAN GREEN in-session 17 Aug (14 suites / 1,074 assertions, honesty blocks skipped — old baselines unavailable in-cloud). |
| **63** | `c0021e4` — pushed, live | 16 Aug 2026 | The AUDIT LOG stops forgetting. It was never a size problem — it trimmed itself at 400 entries and kept 300, silently discarding the rest, including build 61's override records. Now one document per entry in `auditLog/entries`, never trimmed, read newest-300 with a real query. Pre-63 entries still shown, merged, with no conversion step (append-only, so unlike §47 it cannot split-brain). Also fixes `isolation-test`, which had stopped completing at all — `startsRegex()` re-scanned the whole emitted buffer per `/`, which went quadratic on a 318 KB page and hung the CARDINAL-RULE suite with no output. |
| **62** | `b2e0458` — pushed, live. ⚠️ carries builds 61 AND 62 in one commit, and its subject describes the TEST SUITES, not the build | 16 Aug 2026 | §49a — BOTH the real holiday and the federal observed day count, for all holidays (Independence Day 2026 = Sat 04 Jul AND Fri 03 Jul), including the cross-year case where 1 Jan on a Saturday is observed 31 Dec of the year before. Plus §52 — the Shift Catalog grouped by family with headings, and sortable by family / catalog order / A–Z. |
| **61** | inside `b2e0458` (with 62) — pushed, live; no commit of its own | 16 Aug 2026 | Defect 1 — ONE definition of "is this assignment a problem?". Request and swap approval now run the same eligibility / capacity / vacation / collision checks the cell editor runs; warns and records an override, never blocks (§51). Eligibility checked on every path for the first time, closing half of defect 4. |
| **60** | `15822e3` — pushed, live | 16 Aug 2026 | The config locks open by default (§50) — Shift Catalog, Report settings, Simulator. The Users page and the unconverted-month guard deliberately stay closed (§50a), and both refusals are asserted so a later session cannot "finish the job". |
| **59** | `b7dd372` | 16 Aug 2026 12:48 | §47 — a month became one document PER DAY (`sched_YYYY-MM/days/{DD}`). Unconverted months are shown in full but read-only behind a banner; converting copies in one atomic batch and keeps the old record as a backup. Plus the repo housekeeping and the `_archive/` folder. ⚠️ **commit message lost** — a test file was pasted instead. |
| **58** | `9f8a8b1` | 16 Aug 2026 11:25 | Stage 2 — COVERAGE against demand, closing defect 6. "Nothing has been asked for" and "nothing has been filled" are reported as different states. |
| **57** | `1e4e39c` | 16 Aug 2026 11:02 | Renaming a shift: the label changes, the internal id never does. |
| **56** | `3e2b6cc` | 16 Aug 2026 10:53 | Stage 1 — BULK ENTRY: duplicate-an-existing, and paste-a-list with a preview. |
| **55** | `427cab8` | 16 Aug 2026 10:39 | Stage 1 — DEMAND rules (stacking, last match wins), the holiday calendar, and tab-through time entry. ⚠️ **its commit message says "Build 54"** — the code is build 55. |
| **54** | `8763e18` | 16 Aug 2026 08:01 | Stage 1 — shift TIMES and SITES in the Shift Catalog, with the 24-hour parser and midnight-aware durations. |
| **53** | `e3cfef3` | 16 Aug 2026 07:46 | The Testing section — auto-populate and Clear Month moved behind rehearsal mode (§45). |
| **52** | `cbd4a5b` | 16 Aug 2026 07:20 | THE ASSIGNMENT MODEL — a list per person per day, closing defect 2 (the silent overwrite). ⚠️ **its commit message says "Build 51"** — the code is build 52. |
| **51** | `0027a5e` | 16 Aug 2026 07:12 | Reports — per-doctor shift counts, dated comparisons, FTE-adjusted fairness, and the overnight-call tag. |
| **50** | `ebf7a27` | 16 Aug 2026 | Small-fix batch — stale-build gate, Quick View month boundary, staff error surface, Users lock, audit log, sticky name column. Closes defects 11, 14, 19, 30. |
| **49** | `9dbbd9b` | 15 Aug 2026 | Eligibility rebuild; demo banner on both pages. |

## Commits whose message names the wrong build

**`41ca7be` (26 Aug 2026) says "Admin 108-109" and contains build 110.** The commit message file
still held the previous wave's text when the owner pushed; builds 108 and 109 had already gone out
in `3828922`. **Not rewritten** — the row above is the record, which is what this file is for.


Recorded here rather than rewritten. Rewriting pushed history to correct a message is a far
bigger risk than an inaccurate label, and the CODE in both commits is correct.

- `e260f4f` (this repo), `0832ebf` (tests) and `ae11548` (the docs repo) all say **82**; every
  one of them contains **83**. The owner pushed while build 83 was being written, so the tree
  moved on under a message prepared for 82. Both builds are real, both are described above, and
  the 83 row names this. Nothing is missing and nothing is rewritten.
- `cbd4a5b` contains **build 52**; its message says "Build 51".
- `427cab8` contains **build 55**; its message says "Build 54".
- `7fcd3f1` (this repo) and `594778e` (tests) carry the combined COMMIT-MESSAGES file
  pasted whole, so their SUBJECT line is a section divider ("═══ schedule ═══"). The
  bodies contain the right messages. Recorded, not rewritten — same ruling as above.
  The combined file now opens with a "copy ONE section per repo" banner.

## The tests repo

Seven of its recent commits carry a test file's contents as the message
(`6cee6a6`, `c097b38`, `0a88e0a`, `f8d6565`, `60c9942`, `0c208b9`, `8ba3348`). Each one is
the battery for the schedule build of the same era, and the schedule row above describes
what it covers. Nothing is missing from the test files themselves — every suite opens with
a header comment stating what it proves and what its honesty check is.

## How to not lose another one

The summary for the build you are about to push is written to **`COMMIT-MESSAGE.txt`** in
each repo — deliberately NOT a dotfile (the old `.claude-commit-msg.txt` was invisible in
Finder because of the leading dot, which is exactly how messages got lost). It is
gitignored but visible; open it and paste the whole thing into the commit box. It is also
delivered to the chat outputs column every build. Copy from either — never from a source or
test file.
