# BUILD LOG — Daily Schedule

**Why this file exists.** On 16 Aug the owner asked: *"do i really have all the commit
summaries?"* The answer was **no**. Several commits carry a test file's opening lines
instead of the summary written for them, and one auction commit says only "hosuekeeping".

Nothing substantive was lost — `DECISIONS.md` holds every ruling and `anesthesia-kp.github.io/HANDOFF.md` holds the
per-build detail, and both are committed — but the *commit history on its own* no longer
tells the story. So the story lives here too, in a file that is committed and cannot be
lost to a mis-paste.

**Every build gets a row here, written at the same time as the code.** A poor commit
message then costs nothing.

The build number below is authoritative: it was read out of `var BUILD` in that commit's
own bytes, NOT taken from the commit's subject line — two subjects are wrong, and they are
flagged. Where the full prose survives it is in the named commit, or in
`~/Documents/GitHub/_archive/schedule/commit-messages/`.

| build | commit | date | what shipped |
|---|---|---|---|
| **99** (staff unchanged) | _in working tree, awaiting owner push_ | 25 Aug 2026 | **THE COLLAPSE SWEEP REACHES THE REST OF THE ADMIN.** Owner, for the third time in three different wordings: *"There's too much data everywhere so it's hard to find what i'm looking for. Most things need to collapse throughout the site."* **Six sections join [96]'s mechanism** — Users → *Admin Access* and *Add user*, Open Shifts → *Post an open shift*, Shift Families → *Add a family*, and the two **Decided** lists on Requests and Swaps. Same `sectOpen` / `toggleSect` / `sectApply`; **no second mechanism** (§3 rule 14). What he came to each page for stays open: All Users, Posted, Families & shifts, Pending, Awaiting. **The two Decided bars carry a COUNT while shut** (*2 decisions*) and it steps aside when opened, so a closed bar still says what is behind it. **⚠ THREE PANELS THE BRIEF LISTED WERE DELIBERATELY NOT TOUCHED, and the reasons are the useful part.** *Which shifts count as overnight call?* and *Who counts toward the group comparison?* are **in Reports, not Stats**, already inside an `open <details>` — a bar there is a fold inside a fold; **Stats itself has no collapsible panel at all**; and `renderReports` draws FIVE of those targets in one function, which is the [96] blast radius squared. **Simulator Fill/Wipe were skipped too**: `renderRehearsal` assigns `className` outright (wiping any class a sweep adds) and dims them last, so a bad edit leaves a lock that LOOKS unlocked. **The four set-once sections are named in `SECT_STATIC`** — `toggleSect` ends in `else renderCatalog()`, so an unnamed key re-draws the catalog from the Users page. **GATES: new `tests/sched/build99-test.mjs`, 49 assertions, all measured from the laid-out page** — including a per-page [96] gate that asserts, with each new section CLOSED, that everything else on that page still renders, and a BEHAVIOURAL probe for the `SECT_STATIC` bug (plant a marker in the catalog summary, toggle a section on another panel, see whether it survived). Honesty against the pushed **98** (explicit SHA `67f9d36`): **26 of 49 FAIL**, the probe among them. **Schedule battery 49 suites, 1,289 assertions, ZERO skipped — 47 pass, 2 FAIL.** ⚠ **Both failures are PRE-EXISTING and were proven so by execution**, not by argument: `build67` and `build80` fail identically on the pushed 98 (`67f9d36`), on 96 (`16e5977`) and on 95 (`be06fcc`). They are recorded in `TODO.md`; **this build neither caused nor fixed them.** Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,049 assertions, exit 0** — with **no auction baseline supplied, so those suites' own honesty blocks did not run** (§6's one legitimate skip; it was a regression gate here, not an auction build's gate). Isolation guard ran on the right bytes (md5 `576a7d7…`) and passed. All three inline scripts `node --check` clean. |
| **98** (staff unchanged) | `67f9d36` · pushed 25 Aug | 25 Aug 2026 | **A GROUP CARD IS ITS HEADER UNTIL YOU OPEN IT.** Owner: *"same for each group, that should collapse unless i'm adding someone. If I have 10-20 groups 3eventually, the page would scroll forever and be cluttered."* **The cost is countable:** each card draws a checkbox per person per side, so nine groups over a 35-person roster is roughly **315 checkboxes on one screen**. Closed, a card is now its header — which already carries the two things you go to it for: the NAME (given the size in [91], after nine cards read as identical) and the MD/CRNA counts, plus whether it counts toward the averages. Measured: the tallest closed card is **21px**. Opening one opens only that one, and an open card **survives a re-render**, so a Firestore snapshot cannot close the group he is working in — the same guarantee the catalog's family bands got in [96]. **THE SUBTLE PART: the header IS the toggle now**, so Rename, Delete and the fairness switch sit inside it and had to stop the click travelling up — otherwise renaming a group would also open or shut it underneath the dialog. That is asserted by CLICKING them, not by reading the source for `stopPropagation`. **GATES: new `tests/sched/build98-test.mjs`, 18 assertions, driven in a browser.** Honesty against build **97**, fixture taken byte-exact from the transfer bundle and md5-verified (`ad0f38c4…`, since 97 was built in the cloud and has no SHA): **8 of 18 FAIL**. One existing suite repointed: `groups-test` read a message that now lives inside a collapsed subgroup column, so it opens the cards the way an admin does before reading for it (38→39). **Schedule battery 49 suites / 1,561 assertions, ZERO skipped.** Isolation **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions**. `node --check` clean. |
| **97** (staff unchanged) | `67f9d36` · pushed 25 Aug | 25 Aug 2026 | **SHIFT ELIGIBILITY IS ONE LINE PER SHIFT, AND A [96] REGRESSION THE OWNER FOUND IS FIXED.** Owner: *"shift eligibility needs to be cleaner, to many words visible and too crowded. each shift should start collpased and be opened by admin."* **The cause, read out of the renderer rather than guessed: `_eligOpen` gated only the EDITING controls.** A CLOSED row still rendered its requirement chips, its allow/block person chips, and a dot-separated list of up to SIXTEEN doctors — up to four lines each — so closing a row barely changed the page. Now a closed row is ONE LINE (measured: 16px) and still answers the two questions the page exists for: *asks for Call team, OB team · any one* and *8 can work it*. Opening one row shows the chips, the selects and the names; the other rows stay one-liners; **navigating away closes it**, which is §104's single exception (dozens of rows can accumulate open where there are only a handful of sections). The **legend now appears only while a row is open**, because it explains chips that no longer exist on a closed row. **The filter bar becomes a collapsed section that NAMES what is filtering** (*call only*, *shift “abc”*, *only shifts nobody can work*) — a short board with no visible reason is the worst version of this page, not the cleanest. **⚠ AND THE REGRESSION, which is the more important half of this build.** Build 96 returned early from `renderCatalog` when the Shifts section was collapsed — **which is the default** — and everything below that line died with it: the catalog summary, the family dropdown, the SITE dropdown, the **holidays list and its Remove buttons**, the open demand dialog, and the copy-an-existing-shift picker. **Five of the six had nothing to do with the shift list.** The owner reported the visible tip: *"The holidays section should list the holidays with remove feature as well."* Fixed by skipping the WORK and not the REST of the function — `el.innerHTML = sectOpen.shifts ? catRowsHtml(...) : ''`. **The general lesson, worth more than the fix: an early return added for one panel silently owns every line after it.** A six-assertion regression gate now runs the whole catalog page with Shifts CLOSED. **GATES: new `tests/sched/build97-test.mjs`, 33 assertions, all measured from the laid-out page** — including row HEIGHT, after an earlier version of this suite counted `innerText` newlines and was wrong, because the browser puts a newline between flex children so it was counting spans, not lines. Honesty against the pushed **96** (explicit SHA `16e5977`): **18 of 33 FAIL**. Two existing suites repointed: `elig-test` now reads the closed row's WORDS and then opens the row to check the chip is still there (stronger — it asserts both forms); `build96-test` counted `.secth` document-wide while claiming *the catalog has four*, and is now scoped to the catalog panel. **Schedule battery 48 suites / 1,543 assertions, ZERO skipped.** Isolation **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions**. `node --check` clean. |
| **96** (staff unchanged) | `16e5977` · pushed 25 Aug | 25 Aug 2026 | **THE CATALOG OPENS AS FOUR CLOSED BARS.** Owner: *"I think the entire shifts card should collapse by default and then expanded opens to a list of the families, which can then by further expanded to find shifts… a scroller within a scroller can be hard to use… there also appears to be a double header at the top there that doesn't make sense."* **Add a shift, Paste a list, Holidays and Shifts all start collapsed**, each bar saying what is inside it; Shifts opens to a list of FAMILIES and a family opens to its shifts, so the inner scroller is no longer needed rather than merely taller; the two stacked column headers become ONE that matches the mode. State lives in memory (`sectOpen`/`toggleSect`/`sectApply`), so a section he opens survives navigation and a reload is the reset — §104. **⚠ THIS BUILD SHIPPED A REGRESSION**: the early return added for the collapsed Shifts section silently killed five unrelated renders on the same page, the holidays list among them. The owner found it the same day and it was fixed in **[97]**, which also added a six-assertion gate that runs the whole catalog page with Shifts closed. **GATES: `tests/sched/build96-test.mjs`, 36 assertions measured from the laid-out page** (figure from the suite's own commit `546bd51`) — what is closed on arrival, what a bar says while shut, and that opening one survives navigation. Nine suites repointed to the new default; three had assertions the new structure made untrue and all three came out STRICTER. **⚠ ROW WRITTEN 25 Aug, LATE — this build shipped without one, and the omission was found by a subagent survey, not by a gate.** Everything above is from the commit and the suite; **the honesty-check numbers were never recorded and are not reconstructable, so none are quoted here.** |
| **95** (staff unchanged) | `be06fcc` · pushed 25 Aug | 25 Aug 2026 | **EDITING A SHIFT OPENS A CARD, NOT A STRIP OF INPUTS.** Owner: *"i think would be better when editing a shift to immediately open a card for that shift that allows to better see everything"*, *"once edit is clicked, maybe a Done button and Delete shift button?"*, and *"The delete button isn't ideal. it should say delete shift"*. What he was looking at: the read row with every cell swapped for an input — eleven controls on one horizontal line, no heading, no Done, and a button reading only *Delete* with no object. **You could see what you were editing WITH, not WHAT you were editing.** The card is the SAME controls and THE SAME HANDLERS, laid out so the shift is the subject: label pill and kind, then Times · Length · Site · Family · Role · People per day as labelled fields, then the staffing rules written as sentences (*Weekdays (Mon–Fri) → 5 people*) each with its own ✎, then the internal id. **Delete shift is deliberately in the FOOTER, not beside Done** — Done is pressed every visit and Delete almost never, and side-by-side puts the destructive action under the muscle memory of the common one; its confirm, which names the shift, is unchanged (§3 rule 5). **BATCH MODE IS UNTOUCHED and that is the point** — §95 shipped edit-all for a whole-catalog sweep, and nine stacked cards would defeat exactly that, so the card is only for the ONE row the ✎ opens. No handler changed; no Firestore read or write changed; staff bytes untouched. **GATES: new `tests/sched/build95-test.mjs`, 30 assertions, all driven through the page's own controls in a real browser** — the card is proved WIRED, not merely drawn, by changing People per day on it and reading the write back (`dailysched/shiftCatalog {"shifts.D.capacity":9}`). Honesty against the FILED build 94: **24 of 30 FAIL**. That fixture had no SHA — 94 is filed and not yet committed — so it was rebuilt from `cb52344` plus 94's own patch and **proved by md5 against the device's filed copy** (`8dfbc672…`), which is a stronger identity check than a SHA here, not a weaker one. **THREE EXISTING SUITES WENT RED AND ALL THREE WERE REAL.** build54 finds the site `<select>` by its `title`, which the card had dropped for a visible label — fixed in the PAGE, no test edited. build85 pinned the render branch by its literal text — repointed to the three outcomes that now exist, so it is STRICTER (34→36). build86 EXECUTES the real `catRowsHtml` and its sandbox lacked the new renderer — given a symmetric marker exactly as `catEditRowHtml` already had, since that suite's subject is WHICH renderer the branch picks (19→20). Schedule battery **46 suites / 1,471 assertions, ZERO skipped**, run in-cloud on bytes md5-identical to the device's. Isolation **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions**. `node --check` clean on all three inline blocks. |
| **94** (staff unchanged) | `be06fcc` · pushed 25 Aug | 25 Aug 2026 | **A DIALOG CAN NO LONGER BE TALLER THAN THE WINDOW.** Owner, driving the shift catalog himself: *"add rule for shift pop-up screen is cut off and not entirely visible. it needs to be shorter or probably needs a scroller."* He was right and the cause was one missing pair of declarations. `.modal` centres its card and has never scrolled; `.modal .mcard` had **no max-height and no overflow at all**, so a card taller than the window overflowed BOTH ends — title above the top edge, Done button below the bottom one — and neither could be reached, because nothing in the stack was scrollable. The demand dialog hits it as soon as a shift has a few rules, since its 60-day preview alone is sixty cells. Fixed with `max-height:calc(100vh - 32px)` (32px is `.modal`'s own padding) repeated as `dvh` for browsers that know it, plus `overflow-y:auto`. **Scoped to `.modal .mcard`, which is both modals on the page and none on staff — the same defect was latent in the cell dialog.** No JS changed; no Firestore read or write changed; staff bytes untouched. **GATES: new `tests/sched/build94-test.mjs`, 16 assertions, every one a MEASUREMENT taken from the laid-out page in a real browser** — where the card sits, whether it genuinely scrolls, and whether Done can be scrolled to *and clicked* — because a CSS fix is invisible to any test that does not lay the page out (§3 rule 9: what would still pass if the page were inert?). It also proves the cap is a ceiling and not a permanent scrollbar: in a tall window the card does not scroll. Honesty `--pre` against the pushed **93**, fixture from the EXPLICIT SHA `cb52344`: **6 of 16 FAIL, and they reproduce the owner's report as numbers** — card top at **−49px**, bottom at 770px in a 720px window, `scrollHeight == clientHeight` so nothing scrolls. Schedule battery **45 suites / 1,438 assertions, ZERO skipped** (browser suites RUN in-cloud; 1,438 − 1,422 = the 16 new ones, reconciling to the digit). Isolation guard **29/29**. Full auction battery per §2 on unchanged auction bytes: **54 suites / 2,050 assertions, zero failures**, `test-audit-fixes` 339/339 with its schedule honesty baseline supplied from SHA `898535a` (admin 46). The auction honesty baseline was deliberately left ABSENT — no auction build changed, which is §6's one legitimate skip. `node --check` clean on all three inline blocks. |
| **93** (staff unchanged) | `cb52344` · pushed 25 Aug | 25 Aug 2026 | **§43 — THE FAIRNESS POOL MOVES ONTO THE GROUP, and the per-person switch becomes the override it was always going to be.** §26 ruled per diem and locums out of the fairness pools; §35 could not act on it, because a person carried a name and an FTE and nothing else, so it shipped a per-person switch in build 51 and wrote into the ruling that *"when stage 4 lands, the switch moves onto the group and the per-person flag becomes the override, not the source."* Stage 4 landed in 88–91. This build performs that migration. **The flag lives on the group document** (`fairness:false`, stored ONLY when false — a group counts unless it says otherwise, so a group created tomorrow needs no write to behave correctly, and turning it back on removes the field rather than storing a second way of saying yes). **EXCLUSION WINS across several groups**, the same shape as §98's block-beats-allow: somebody in Per diem AND in OB is per diem, because the flag names a pool a person does not belong in and a second group cannot argue them back into it. A person in NO group has no group answer, so the pre-group behaviour stands. **NOTHING STORED CHANGES MEANING and there is no migration step** — the old document held exclusions only and absent meant "counts"; absent now means "follow your groups", and a group counts by default, so an untouched roster resolves to exactly what it resolved to yesterday. That equivalence is asserted by replaying a build-51-shaped document. **§43 is the one ruling in this project where Claude argued and was overruled** — §19, two places answering one question — and the owner accepted it *with a mitigation that is part of the ruling*: the ruling says it "only holds if the visibility above is actually built". All three conditions are built and each is a gate. **(1) On the person:** `poolStatusText()` gives both halves in the ruling's own wording — *"In Per diem, which does not count toward averages — individually overridden to count"* — and `whyNotInPool()` now names WHICH of the two decided, because "excluded by an admin" could no longer tell a reader where to go and change it. **(2) On the group:** each card carries the toggle and LISTS the members whose individual setting disagrees with it, by name and by direction. **(3) Inside the report:** `buildModel` carries an `overridden` list — somebody overridden INTO the pool never appears in `excluded`, so without it they are the one exception the report could not mention — printed in §29's method paragraph and in the **spreadsheet export too**, because a report that is honest on screen and silent in the file people circulate is not honest. **Only a disagreement is stored:** a tick that agrees with the person's groups is written as `deleteField()`, so they go on FOLLOWING their groups and a later change to the group reaches them; storing the agreeing value would freeze them at today's answer and produce exactly the silent second source §19 warns about. The confirm names the overrides it is about to keep, and the audit row records them. **Three defects were found by the gates, not by reading:** ① `whyNotInPool` called a stored `false` an "individual decision" even when it AGREED with an excluding group — the group was the real reason, and the sentence would have sent the admin to untick a person while the cause stayed put (caught by the non-browser suite); ② the compPool listener marked only `reports` dirty, so **§43·2's override list on the group card would never have repainted when an override changed** — the one screen the ruling requires it on (caught by CLICKING it in the browser suite); ③ **the defect-29 shape**: `runReport` and `runFairnessView` had no gate on `groupsLoaded`/`membersLoaded`, so a report run in the cold-load window would have counted per diem in and printed a wrong expected figure for every doctor with nothing saying so — both now refuse and say why. Gates: NEW `tests/sched/build93-test.mjs` **50/50**, EXECUTING the whole nine-function resolution chain (never stubbed — a stubbed `fteOf` would let §36's "no FTE means no share" quietly stop being true) over multi-group exclusion, override in both directions, the no-group case and the replayed legacy document · `groups-test.mjs` extended to **39** and **CLICKS the new toggle on the rendered card** — build 88 shipped with every control on that page dead and every non-browser suite green, because those suites executed functions and nothing had ever clicked anything · `build51-test` re-based on the new wording (moved, not weakened: it still asserts the exclusion is named and why) · honesty `--pre` vs the pushed **91**, fixture from the EXPLICIT SHA `dc29760` (md5 `c0da209c853af161f0dde14d0147693f`, verified byte-identical to the pre-patch file): **1 passed, 31 FAILED, exit 1**; groups-test `--pre` 30/8 fail. **Schedule battery all 44 suites / 1,286 assertions green with ZERO skipped** (the browser suites RUN — a playwright/chromium version mismatch in this sandbox was resolved rather than skipped past). Isolation guard 29/29. **Full auction battery per §2: 54 suites / 2,050 assertions, zero skips.** `node --check` clean. Staff bytes unchanged. No rules change. ⚠️ **Recommendation recorded, not built:** `tests/audit-handlers.mjs` — the lint for the exact bug class that killed build 88 — covers only the two VACATION pages. Pointed at the schedule pages it reports 17 violations, all false: it reads literal `on*="…"` in static HTML, and the schedule admin generates almost all of its handlers inside JS strings. A lint with false positives is a lint that gets ignored, so it was NOT extended. The real check would be "every interpolation inside a generated handler goes through `jsArg`", which is a page-wide refactor and unasked. |
| **92** (staff unchanged) | `cb52344` · pushed 25 Aug | 25 Aug 2026 | **S5c'S OTHER HALF — the grid filters by MD/CRNA and by GROUP.** Build 84 shipped the filter bar with a hole in it and SAID SO in the markup: filtering by anything about the person was not deferred out of taste, it was impossible, because a person carried a display name and an FTE and nothing else (§22). Stage 4 gave them a category and group membership, so the hole closes on real attributes. `gridPersonMatch(u,cat,group,catOf,inG)` is pure and takes its two lookups as arguments, so the battery drives it with its own fixtures instead of booting a page; an empty filter never calls its lookup, so an unfiltered grid cannot break on data that has not arrived. The two filters are **AND, not OR** — which is the whole point of narrowing twice — and an uncategorised person is never quietly counted as an MD. **Three failure modes drove the design, each worse than a wrong row.** ① **SILENCE:** a grid with people hidden looks exactly like a roster that lost people, so the banner counts the new filters and names both the category and the GROUP BY ITS LABEL (`OB_ANESTHESIA` is not what the admin picked); the empty-grid message says when the person filters alone emptied it, rather than blaming the coverage filter. ② **A CONTROL THAT LIES:** the Shifts view draws shifts, not people, so both selects are DISABLED there and the banner does not count them — a category select that looked live there would be filtering nothing while appearing to filter, and `gridShiftRows` never consults them, which keeps its own "1 of 2" count honest. ③ **A FILTER POINTING AT NOTHING:** deleting a group while it is selected clears the filter instead of hiding every person for a reason no longer on screen. The group drop-down is refilled by the RENDER (groups arrive from Firestore after boot, so a list built once with the toolbar would be permanently empty) from `groupList()`, so it carries the owner's family order (§94) rather than a second, different one, and is only rewritten when the options actually change — rebuilding a `<select>` on every paint would drop the choice just made. All three group listeners now mark the grid dirty, so removing somebody from a group does not leave them sitting in a filtered grid until an unrelated render. Not remembered across a reload, like the other two. Gates: NEW `tests/sched/build92-test.mjs` **38/38**, executing the matcher · `build69`/`build70` re-based with the page's OWN matcher and lookups supplied, never stubs — a stub would let a filter that hides the wrong people pass unnoticed · honesty `--pre` vs the pushed **91** (SHA `dc29760`): **3 passed, 8 FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. ⚠️ The first patch attempt failed its own anchor check on a mis-indented line and **exited without writing** — the file was verified unchanged at build 91's md5 before re-anchoring, rather than the mismatch being forced through. |
| **91** (staff unchanged) | `dc29760` · pushed 25 Aug | 25 Aug 2026 | **SELECT ALL, AND THE GROUP NAME READS FIRST — both asked for by the owner while he was actually setting the groups up, which is §90's whole argument arriving on schedule.** **§ SELECT ALL / NONE**, per subgroup, per group: *"I will need a select all option."* Ticking 35 people into nine groups one box at a time is the friction that makes an admin abandon setup half-done. **Adding is one merged write and asks nothing** — it grants, and every grant is visible and individually undoable. **Clearing passes through a confirm that NAMES the count and the people** (§3.5: no one-click destructive action), because it can silently drop ten minutes of somebody's work. Neither direction touches the other side of the group, and neither touches a person whose category is unset — they are on no side, and a bulk action must not quietly decide that for them. The buttons only appear when they would do something: no *all* on a full side, no *none* on an empty one. **§ THE GROUP NAME READS FIRST.** He seeded his nine groups from the shift families, got them correctly, and read the result as *"they all got called MD subgroup 0"* — because the name was 12.5px sitting between two uppercase `MD SUBGROUP · 0` headings, so nine cards looked identical and the only thing identifying a card was the smallest text on it. The name is now 15px/800 with the counts demoted to a muted chip, and the column headings are quietened. **This is the §93 day-board lesson for the third time: the thing you came to read must be the thing you see first.** He had deferred the fuller layout rework (*"will have to change that layout, but fine for now"*) — that stays deferred and is recorded in TODO; this is the part he then asked for outright. Gates: `groups-test.mjs` extended to **27/27**, driving select-all by CLICKING it — a second MD added, one click puts both in, the CRNA side is proved untouched, then *none* is clicked and the dialog is asserted to name the count and dismissing it is proved to change nothing. **Schedule battery 42 suites, ZERO skipped** (in-cloud). Isolation **29/29**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **90** (staff unchanged) | `47af2ea` · rules `5994a1e` · pushed 25 Aug | 25 Aug 2026 | **THE GROUPS PAGE WAS DEAD ON ARRIVAL IN BUILD 88, AND THE OWNER FOUND IT — not the battery.** His words: *"I see the Groups tab, but I'm unable to edit or make changes."* **§ THE DEFECT.** Every control on that page built its inline handler with `JSON.stringify(u)`, which returns `"AR"` — **with double quotes** — dropped straight into `onclick="…"`. The quote CLOSES the attribute, so the browser parsed a truncated handler and threw `Unexpected end of input` on every click. Category buttons, membership checkboxes, Rename and Delete: all inert. Add Group appeared to work only because that one button takes no argument. **§ WHY NO TEST CAUGHT IT, and this is the part worth keeping.** `build88-test` **executes the functions**, and every function was correct — `groupSides`, `categoryOf`, `inGroup`, every writer's gate, all green. **What was broken was the WIRING, and nothing had ever CLICKED anything on that page.** This is FAST-1 (admin 303) in different clothes: bytes present, feature unreachable, suite passing over it. **§ THE FIX.** One helper — `jsArg(v) = esc(JSON.stringify(v))` — used by every inline handler that carries an argument; `esc` turns `"` into `&quot;`, which the HTML parser decodes back before JS ever parses the handler. The catalog's family bands have done this since build 85; the Groups page simply did not. **§ AND THE DEAD END BEHIND IT.** An empty subgroup said *"nobody has this category yet"* — twice, once per side — which reads as broken software. It now says **"Nobody is marked CRNA yet — set that above, and they appear here to tick."** The mechanism was there; the page never told him the order of operations (categories first, then people become tickable), and §99 calls that intuitive-ness a standard, not a nicety. **§ THE GATE THAT WAS MISSING NOW EXISTS:** NEW `tests/sched/groups-test.mjs` **20/20**, driving the real page in a real browser — it CLICKS Add group, CLICKS the MD button, TICKS the membership box, UNTICKS it, and checks the fake Firestore store after each, plus zero page errors throughout and the cardinal rule. A source lint in `build88-test` catches the same anti-pattern statically. **A crude second lint ("every handler must end in `)`") was written and then REMOVED** — it flagged legitimate assignment handlers, and a lint with false positives is a lint that gets ignored. **Schedule battery all 42 suites / 1,198 assertions, ZERO skipped** (in-cloud; md5-identical bytes both sides). Isolation **29/29**. **Full auction battery 54 / 2,050, zero skips.** `node --check` clean. Staff bytes unchanged. **No auction file touched (§92).** |
| **89** (staff unchanged) | `655d49b` · pushed 24 Aug | 24 Aug 2026 | **§98 — THE ELIGIBILITY REBUILD. The tick grid is RETIRED and the page becomes a board.** Owner: *"The current ticks should be forgotten, they mean nothing… it will never be useful to have the option to view 3,000 ticks at once."* **§ WHAT WENT, and it went properly:** every writer (`setElig`, `eligAll`, `eligBandBulk`), the click/crosshair delegation, the listener, the `eligRef` handle and the `elig` state variable — removed, not disabled, because a dead writer left in place is what a later session wires back up by accident. A tombstone says what was removed and why. **`dailysched/eligibility` is untouched on disk** and is now read by nothing (§3: nothing is ever deleted). **This supersedes §53b·1 and §56** — option C and the cutover switch both rested on those ticks meaning something, and the owner has said they do not; re-interpreting 3,000 meaningless ticks into exceptions would have manufactured data rather than preserved it. He agreed to drop the switch outright. **§ WHAT A SHIFT CARRIES NOW:** a list of **groups**, or **one named side of a group** (`OB|MD`), plus per-shift **individual allow and block** lists, plus an any-one/all mode (§53a). **BLOCK BEATS ALLOW BEATS GROUP** — an override a group could silently outvote would not be an override, which is precisely what he asked for. Adding somebody to one list removes them from the other, so the answer can never depend on which check ran first. **§ A whole-group entry asks only about membership; a SIDE entry also asks the person's category** — so somebody with no category still qualifies through a whole-group entry, and their missing category is surfaced on the Groups page rather than silently costing them work. **§ THE SHIFT'S OWN MD/CRNA/Both MARKING NO LONGER GATES ELIGIBILITY.** It used to be the only way to say "MD side"; a side can now be named outright, and two mechanisms for one answer is §19's forbidden second source of truth. `role` stays a catalog label and a filter. **§ A SHIFT THAT NAMES NOTHING ADMITS NOBODY** (his choice, put to him with the alternative) and says so on its own row — an unconfigured shift that silently admitted everyone would look finished. **§ THE BOARD.** One quiet line per shift; controls only on the row you open — **the first cut rendered two selects and two buttons on EVERY row, which is the clutter §93 rejected, and it was rebuilt before he saw it.** Filter by family, type and text; sort four ways; a "only shifts nobody can work" filter; and a **By person** view answering the other question — what can this person work, and is anybody in no group. **No dense mode is offered and none should be added: deliberately unlike the Shift Catalog (§95), because he ruled the dense view useless here.** **§ Removing a person now clears their groups, their category and every per-shift override naming them** — otherwise reusing initials would quietly re-admit somebody. Gates: NEW `tests/sched/build89-test.mjs` **48/48** executing the rule in every shape (precedence, both modes, both sides, no-category, unset vs nogroup); `elig-test.mjs` **REWRITTEN, 31/31**, driving the board through its own selects and buttons in a real browser. Honesty `--pre` vs the pushed **83** (`e260f4f`): **24 FAILED, exit 1**. **Schedule battery all 42 suites / 1,245 assertions, ZERO skipped, in-cloud on byte-identical bytes (md5 both sides).** Isolation **29/29**. **Full auction battery 54 / 2,050, zero skips.** ⚠️ **Six suites were re-based and none weakened** — detail in HANDOFF; the isolation guard also learned that `mergeFields()` is a write, which it had never known. `node --check` clean. Staff bytes unchanged. **No auction file touched (§92).** |
| **88** (staff unchanged) | shipped inside `655d49b` (one commit for 88–89) · pushed 24 Aug | 24 Aug 2026 | **STAGE 4 — GROUPS AND SUBGROUPS.** The shape is the owner's (§97): a **GROUP** is the unit people name — OB, Pediatric, ICU — and it holds exactly **two subgroups**, its MD side and its CRNA side. **A person joins the GROUP; their category picks the side** — they are never asked which, because that would be a second way to say what the category already says (§19). Nobody crosses (§53b). Three new documents, all in `dailysched/*` and never in the roster the live auction shares (§2): `groups`, `groupMembers`, `categories`. New Groups page: set who is an MD or a CRNA, add people to groups, rename and delete groups, and **seed groups from the shift families in one click** — *seeded, not chained*, because whether a group stays tied to its family is not settled and must not be assumed (§22). **§ Somebody in a group with NO category is on neither side, and the page says so** rather than hiding it — it is the one thing that would stop them being eligible for anything. **§ Every writer is gated on all four snapshots having arrived** (the [48] pattern; defect 29 is what skipping it costs), and deleting a group removes the memberships with it so no invisible keys are stranded. **§ §98·3 arrived after this build and is surfaced here:** every user should be in at least one group — counted and named on the page, and deliberately NOT enforced by refusing a save, since everybody is at zero the day this ships and a rule that blocks setup would be obeyed by nobody. Gates: NEW `tests/sched/build88-test.mjs` **36/36**, executing group membership, the two sides, the no-category case and every writer's gate. Honesty `--pre` vs the pushed **83** (`e260f4f`): **FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** ⚠️ **A rules gap this build creates, and it is NOT closed:** `firestore.rules` lets any *registered user* write those three documents, because they are not named in `isSchedAdminOnlyDoc()`. Group membership decides who may work what, so they belong on that list — but the rules live in the AUCTION repo, which makes it an auction deploy, **closed without the owner's specific decision (§92)**. Nothing is live and the schedule has no users, so it is not urgent; it is recorded in TODO as the one outstanding item of Stage 4. |
| **87** (staff unchanged) | `ac47833` · tests `ff590fd` · docs `3096ce5`/`1371f08` · pushed 24 Aug | 24 Aug 2026 | **§95 part 3 — A PASTED BLOCK LANDS FULLY CONFIGURED.** Owner: *"Ideally, I'd also be able to add a group of shifts from a copy paste list that can read the data… Then I could enter a block of shifts with times all at once."* **⚠️ Paste-a-list was NOT built here — it has existed since build 56**, with a preview before anything is written, an update-existing option, per-row rejection reasons and duplicate detection inside the block. What it READ was `label · start · end · site` and nothing else, so a pasted block still needed a second pass by hand. **That gap is what this closes, and nothing else.** Columns are now `label · start · end · site · family · role · capacity`, tab or comma separated, and you may stop after any of them — **a three- or four-column list written months ago pastes exactly as it always did**, which is pinned by two assertions. **§ A PASTE NEVER CREATES A FAMILY.** An unknown family REFUSES the row and names the families that do exist. One typo would otherwise scatter shifts into a near-duplicate nobody meant, and a family is a deliberate thing with a colour attached (§22). Families match by label or by internal key, in any case. **§ Role must be MD, CRNA or Both** (normalised to the catalog's spelling) and **capacity a whole number 1–20** — the same ceiling the row editor enforces; anything else is refused BY NAME rather than silently coerced (§17). **§ COLOUR FOLLOWS FAMILY, on both paths:** a pasted new shift is born with its family's colour like one added by hand, and moving a shift between families in a paste carries the colour with it — otherwise the old colour would survive and §94's whole colour reading would quietly lie. **§ Before/after now includes family, role and capacity**, so *already exactly this* stays true for a row that only moves a family. **§ Demand rules stay OUT** and the page says so: they are a structured per-shift editor, and inventing a text syntax for them is the kind of guess §22 forbids. Gates: NEW `tests/sched/build87-test.mjs` **31/31**, EXECUTING the real parser (with the page's own `parseHHMM`, not a stub) over three-, four- and seven-column rows, commas, a header row, every refusal shape, four bad capacities, a rejected row surrounded by good ones, and a duplicate inside the block. Honesty `--pre` vs the pushed **83** (`e260f4f`): **0 passed, 10 FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **86** (staff unchanged) | shipped inside `ac47833` (one commit for 84–87) · pushed 24 Aug | 24 Aug 2026 | **§95 parts 1+2 — THE CATALOG DOES BOTH: ONE AT A TIME, AND BATCH.** Owner: *"I need a way to do both edit 1 at a time and big batch edits."* S7 (85) made the page read-first, which is what §93 asked for — and in doing so it removed the tab-straight-down-the-list pass that **build 55 exists to protect**. This puts that back as a MODE rather than as the default. **One at a time** stays the default: quiet lines, ✎ opens one row. **Batch — edit all** renders every row as its editor, the pre-85 page on demand, so a whole-catalog sweep is one uninterrupted tab-through. The [55] mid-edit guard protects it exactly as it always did. **§ THE CHOICE IS REMEMBERED**, like the grid's view and density ([69], [70]) and for the same stated reason: it changes how the SAME data looks. The catalog's problem FILTER is deliberately NOT remembered — that one HIDES shifts, the same distinction [84] draws. **§ Batch mode says what it is and what it costs** — about eleven controls per shift — rather than pretending the dense view is free. **§ Switching modes closes any single open row**, so the two ideas of *open* cannot disagree; an unknown mode is refused rather than leaving the page with no rows. Gates: NEW `tests/sched/build86-test.mjs` **19/19**, EXECUTING `catRowsHtml` in all four states (read, batch, read-with-one-open, batch-with-one-open) and proving the two modes compose with collapsed bands and the problem filter — so *show me the twelve with no demand and let me fix them all in one pass* actually works. **The stubbed editor row returns a real `<div>`, not `''`** — a stub must have the SHAPE of the real function or the suite could pass over a mode that renders nothing (the FAST-1 lesson, written into the suite). Honesty `--pre` vs the pushed **83** (`e260f4f`): **0 passed, 14 FAILED, exit 1**. ⚠️ **AND THE BATTERY FOUND A REAL DEFECT IN THIS BUILD BEFORE IT SHIPPED:** clicking ✎ on a second row, or switching modes, did NOTHING while a field still held focus — the [55] mid-edit guard defers re-rendering the list, and these handlers honoured it. That guard exists to stop a SNAPSHOT destroying what you are typing; **the admin's own click is the opposite of that** — asking for the list to change IS the request. A new `catUserAction()` clears the guard on all four deliberate actions (✎, mode switch, band collapse, problem filter). `build55-test` caught it by driving the page, not by reading it. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **85** (staff unchanged) | shipped inside `ac47833` (one commit for 84–87) · pushed 24 Aug | 24 Aug 2026 | **S7 — READ-FIRST CATALOG ROWS.** The Shift Catalog rendered **eleven live controls for every shift, all the time** — roughly a thousand on a real catalog — with NO column headers, so what each control meant lived in a hover tooltip (invisible on a touch screen, and to anyone in a hurry). Now each shift is **one quiet line** — colour pill · kind · times+duration · site · demand · capacity · role · a badge for each thing missing — and **only the row you open has inputs**. **§ THIS IS A SAFETY FIX AS MUCH AS A TIDINESS ONE.** Every input on this page is a re-render hazard: a Firestore snapshot arriving while an admin tabs through 92 shifts destroys the element they were about to reach ([55], and defect 12 before it). **One open row is one hazard instead of ninety-two.** The editor itself is UNCHANGED — the same markup, the same handlers, the same guards; S7 changes WHEN it renders, not what it does. **§ ONE DEFINITION OF WHAT IS WRONG WITH A SHIFT.** New `catShiftProblems()` decides *no times / half set / provisional / no site / no demand*, and it now feeds BOTH the badge on a row AND every count in the summary card — which previously computed five separate filter expressions of its own. That is build 78's defect in miniature (two readers, two definitions, one of them wrong), closed before it could happen. **§ THE SUMMARY COUNTS ARE NOW BUTTONS** (UX-PLAN fix 4): clicking *3 with no site* filters the list to exactly those three; clicking it again clears. A filter that matches nothing says so and offers the way back. **§ COLLAPSIBLE COLOURED FAMILY BANDS** (fix 3) — 92 rows become ~10 groups, each band carrying its family colour and its count, so a collapsed band never hides shifts silently. Bands appear only in family sort; a band under an A–Z sort would be a lie ([62]). **§ STICKY COLUMN HEADERS** (fix 2) — the meanings finally leave the tooltips. **§ AND §94 REACHES THE THIRD LIST:** `catalogList()`'s family grouping was still A–Z, so the same catalog read in two different orders on two adjacent pages. It now uses `bdFamRank` like the day board and the Families page. Gates: NEW `tests/sched/build85-test.mjs` **34/34**, EXECUTING the real `catShiftProblems` over ten shift shapes including a null shift and the provisional-only-when-times-are-set case, plus structural pins that the read row emits NO `<input>` at all, that the editor still calls the same three writers, and that the summary derives every count from the shared function. Honesty `--pre` vs the pushed **83**, fixture from the EXPLICIT SHA `e260f4f`: **0 passed, 16 FAILED, exit 1**. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **84** (staff unchanged) | shipped inside `ac47833` (one commit for 84–87) · pushed 24 Aug | 24 Aug 2026 | **S5c — THE GRID FILTER BAR.** A name/text box and two coverage filters, pinned in the grid toolbar. The text box hides ROWS — matching a display name **or the initials**, which is how you find one person out of 35 quickly, and the shift label or id in the flipped view. The coverage select hides DAY COLUMNS: *short days only* or *fully covered days*, read from the same `coverageRows` roll-up the coverage strip and the Coverage page use. **§ THE FILTERS GO INERT WHEN NOTHING IS ASKED FOR.** With no demand rules anywhere, §9's *not asked for* is not the same as *covered*, so filtering to short days would hide the entire month on a question nobody has answered. Both coverage modes therefore do nothing in that state, **and the banner says the filter is doing nothing** rather than leaving an empty grid to be misread. **§ THE REAL RISK HERE IS SILENCE, and it is handled explicitly.** A grid with rows or days hidden looks exactly like a month that is empty or a roster that has lost people. So: a blue banner above the grid states what is filtered and counts what is shown (*6 of 35 people · 12 of 31 days*), a **Clear filters** button sits inside it, and when the filters match nothing the table body SAYS so instead of rendering blank. **§ NEITHER FILTER IS REMEMBERED across a reload — deliberately.** View and density ([69], [70]) are remembered because they change how the SAME data looks; a filter changes what you can SEE, and a remembered one greets you tomorrow with people missing for a reason you have forgotten. **§ ROLE AND SITE ARE STILL NOT FILTERABLE, and that is not taste:** people on this site carry only a display name and an FTE — `role` and `site` are fields on SHIFTS. Do not design around an attribute that does not exist (§22); that half waits for Stage 4. **§ The filter box lives in the TOOLBAR, outside the element the render replaces** — inside it, typing would be eaten mid-keystroke, which is defect 12 exactly. Gates: NEW `tests/sched/build84-test.mjs` **33/33**, EXECUTING the real `gridRowMatch` (case, fragments, initials, trimming, null labels) and the real `gridVisibleDays` (short / covered / all, and the inert case), plus pins that all three of renderGrid's loops walk the SAME visible-day list so a header column can never drift from the cells under it. Honesty `--pre` vs the pushed **83**, fixture from the EXPLICIT SHA `e260f4f`: **4 passed, 7 FAILED, exit 1**. ⚠️ **It also broke two existing suites, exactly as intended:** `build69-test` and `build70-test` eval the REAL `renderGrid`, which now asks for two functions that did not exist — both were given the page's own extracted code (never a stub — the FAST-1 lesson), and `build70`'s direct `gridShiftRows` call was updated for its new signature and now also asserts the row count it reports. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **83** (staff unchanged) | `e260f4f` · tests `0832ebf` · docs `ae11548` · pushed 24 Aug — ⚠️ **all three subjects say "82"; the bytes are 83** | 24 Aug 2026 | **§94 — THE SHIFT FAMILY ORDER IS THE OWNER'S, NAMED BY HIM, ON BOTH PAGES.** *"Call at top, then AP, then weekday daytime, then ob, then richmond, then pediatric, then PCV, then ICU, then admin, then unassigned. Also re do shift families section to this order."* Build 82 had INFERRED an order — whichever family held `kind:'call'` shifts first, the rest alphabetical. Reasonable, and superseded within the hour: **a named order beats an inferred one, and the inference is now gone from the page entirely** (a test asserts `hasCall` appears nowhere). **§ TWO ORDERS, DELIBERATELY DIFFERENT.** RANK is his list. MATCHING is tried MOST-SPECIFIC-FIRST, so a family called *Richmond Daytime* lands under Richmond rather than under weekday daytime — the generic words are matched last, on purpose. Synonyms are the ones a real catalog will actually hold: AP also matches *acute pain*, pediatric also matches *peds*, ICU also matches *critical care*, OB also matches *obstetrics*, admin also matches *administration*. **§ A FAMILY HE NEVER NAMED IS NOT GUESSED AT (§22)** — it sorts alphabetically AFTER his ten and BEFORE Unassigned, so the department can invent families this code has never heard of and they appear somewhere predictable rather than vanishing. **§ "Unassigned" is his word** for the bucket build 82 called *Other*, and it is ALWAYS LAST — which also moved it on the **Shift Families page**, where it had led the row as a drop target since that page was built. **That page now sorts with the SAME comparator as the day board**, so the two can never drift apart; its old `localeCompare` is gone. Gates: NEW `tests/sched/build83-test.mjs` **32/32**, EXECUTING the real `bdFamRank`/`bdFamCmp` over all ten positions shuffled, every synonym, the Richmond-Daytime specificity case, an unnamed family, an empty label and a null one. Honesty `--pre` vs the pushed **81**, fixture from the EXPLICIT SHA `8084190`: **0 passed, 4 FAILED, exit 1** — baselined against 81 and NOT 82 deliberately, because 82 was filed and never separately committed, so no fixture for it exists or ever will (START-HERE's rule: baseline against the last PUSHED build and say so). `build82-test.mjs` was amended in the same breath — its six family-order assertions were REMOVED rather than left to contradict §94, and its three `Other` assertions became `Unassigned`; the file says where the order now lives. **Schedule battery: all 35 suites green, 995 assertions, ZERO skipped.** Isolation guard **27/27**. **Full auction battery per §2: 54 suites / 2,050 assertions, zero skips.** `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **82** (staff unchanged) | shipped INSIDE `e260f4f` — see build 83 | 24 Aug 2026 | **THE DAY BOARD, REBUILT — the owner rejected 81's version and said exactly why (§93).** His words: *"not a useful addition in it's current form… I want to very easily be able to see who IS working and care less about who isn't. I want clean and organized, not cluttered."* Build 81 answered the brief and missed the point: five statistics, two strips, four footnotes and a per-site column layout around the one thing he asked for. **What research into daily anesthesia boards actually says** — the enduring artefact is the DAILY ASSIGNMENT SHEET: an ordered list where the people are the payload and the shift is the label; *who is on call* is the question asked most; and the schedule's value is that it is glanceable and public (ASRA's call-schedule guidance makes visibility the thing that earns acceptance). **So the board is now one table.** Grouped in shift families using the catalog's own colours, **call first** — decided by the catalog's `kind`, not by guessing at family names — then alphabetical, catch-all last. Inside a family: by start time, untimed rows at the bottom, then catalog order, then name. Columns are TIME · WHO · SHIFT · SITE, the site column appearing only when any shift has one. **ON NOW is the only badge** (plus *on now · since yesterday* for the overnight case): a *later* pill on every row meant the whole sheet wore one at 02:00, and the time column already says when. **§93's rules, applied:** the *not on anything today* LIST is gone; vacation is a COUNT behind a drawer; the four footnotes are one line; a name is a name — the *· no e-mail* marker from 81 is retired as Users-page housekeeping. **The five dashboard cards are one line of six numbers** (requests · swaps · coverage · on vacation · people · latest change), each opening a drawer, one at a time. **Every element id the old cards carried is unchanged, so `renderDash()` was not touched at all** — and the suite proves it by reading the ids OUT of the real `renderDash` body and checking each still exists in the markup, rather than trusting a hand-typed list. Gates: NEW `tests/sched/build82-test.mjs` **60/60**, EXECUTING `bdRowSort` (seven orderings, including the one it CAUGHT: two shifts with no `order` both give `NaN`, and `NaN!==NaN` is true, so the old shape returned 0 from the order branch and never fell through to the name — fixed in this build because a test found it), `bdFamOf` over four catalog shapes and `bdFamCmp` over six. Honesty `--pre` vs the pushed **81**, fixture from the EXPLICIT SHA `8084190`: **19 passed, 29 FAILED, exit 1**. `build81-test` **58/58 STILL GREEN on the rebuilt page** — the day board's logic is unchanged and now has a regression gate behind it; its two brittle assertions (an exact build number, and the card-wall's position) were replaced with the questions that hold forever. **Schedule battery: all 34 suites, 969 assertions, ZERO skipped** (in-cloud, browser suites RUN). Isolation guard **27/27**. **Full auction battery per §2: 54 suites / 2,050 assertions, zero skips.** Rendered and SCREENSHOTTED before hand-over — the real CSS and the real `renderBoard`, driven in a real browser over sample data, at midday and at 02:10, which is how the overnight row was confirmed by eye and not only by assertion. `node --check` clean. Staff bytes unchanged. No rules change. **No auction file touched (§92).** |
| **81** (staff unchanged) | `8084190` · tests `e64b110` · pushed 24 Aug | 24 Aug 2026 | **S6 — THE DAY BOARD, AND IT IS THE LANDING PAGE.** Approved 18 Aug (*"build and yes"*), built under **§90** (base features before defect polish) and **§91** (no contact number). The admin now opens on **who is on today, at which site, and who is on right now** — the five dashboard cards keep their place underneath, and the first nav entry reads *Today*. Each site gets a column; each shift shows its family chip, its catalog times, a running state, and the people on it, every name a `mailto:` on the KP address the Users page already holds. Two strips follow: who is on vacation today (from the auction's approvals, read-only as always) and who is on nothing. **§ WHAT "NOW" MEANS — decided in the code, because the brief asked for a decision and not a guess.** A shift belongs to the day it STARTS: `Call 24` dated Tuesday runs Tue 19:00 → Wed 07:00, so **at 02:00 on Wednesday the person on duty is on YESTERDAY's shift and today's has not begun.** A board that read only today's record would show an empty hospital at exactly the hour somebody needs to know who is in it — so it reads the previous day too and lists those people as *still on from yesterday*, but only while they genuinely are. A shift with no times is never placed on a clock; it says *no times* instead. **§ WHAT IT COSTS: nothing, normally.** Assignments live one document per day ([59]). When a day it needs is inside the month the grid already holds — the usual case, since the grid opens on today — it reads that state and opens NO listener. Only for a month the grid is not showing does it subscribe, and then to **one document per day plus the month's marker**, never the month's day collection: a board that needs two days must not pay for thirty (FB-3, one bill across both sites). Per [66] it never fetches per render; a one-minute ticker only marks it dirty, so *on now* stays true as the clock moves without costing a read. **§ LOADING IS NOT EMPTY.** `monthLoaded` was added for this: until the month's first snapshot lands the board says so, rather than rendering an unarrived day as *nobody is on* — the same distinction [59] draws between an unconverted month and an empty one. **§ Shortfalls come from `demandOn()`** — the SAME function the Coverage page and the grid's coverage strip use. Build 78 exists because auto-populate had grown a second definition of what a day needs; this board does not add a third. Where nothing has demand rules it says so in §9's words rather than claiming full coverage. Gates: NEW `tests/sched/build81-test.mjs` **58/58**, EXECUTING the real `bdShiftState` across midnight, the 24-hour case and the untimed case (24 assertions on the clock decision alone), the real `bdCellsOn` over eight loading/legacy/routing states, and the real `bdEnsureSubs`/`bdSubFor` under fake listeners that COUNT what is subscribed — proving nothing is watched before sign-in, nothing is re-read for the grid's own month, and the day collection is never listed. Honesty `--pre` vs the pushed **80**, fixture from the EXPLICIT SHA `64888af` (md5-verified against the pre-edit file): **1 passed, 19 FAILED, exit 1**. **Schedule battery: all 33 suites green, 909 assertions, ZERO skipped** (browser suites RUN, in-cloud). Isolation guard **27/27** — the cardinal rule re-proved. **Full auction battery per §2.** `node --check` clean on all three inline blocks. Staff bytes unchanged. No rules change. **No auction file was touched (§92).** |
| **tests only** | `36c2a53` (tests) · pushed 24 Aug | 24 Aug 2026 | **THE COVERAGE GAP CLOSED: builds 71–74 and staff 31–35 finally have a suite in THIS battery.** They shipped with none. Their only executable gate was `tests/test-appcheck-login.mjs`, which lives at the tests ROOT and is swept up by the **auction** battery — so five schedule builds were being guarded by the other site's suite, which is exactly the mixing `tests/sched/README.md` exists to prevent. NEW `tests/sched/appcheck-signin-test.mjs`, **28 assertions**, covering both pages: App Check initialised BEFORE `getFirestore` (a token must exist before the first read) with the two distinct Firebase app names the badge selector depends on; staff's readiness fallback raised 1500→3000 ms. **It pins the REVERSAL honestly rather than pretending both shipped:** build 72's rule that un-hid the reCAPTCHA badge must be ABSENT (§66 undid it), and the attribution that Google requires once the badge is hidden must be PRESENT with both links — the suite refuses to let those two drift apart, because hiding the badge without the attribution breaks Google's terms. Plus: exactly ONE security box per page, outside both sign-in panels (the duplicate-per-panel version is explicitly forbidden in the markup note), and staff 35's retired passcodes listener gone. Two assertions were rewritten mid-build because they tested PROSE rather than CODE — a tombstone comment names the very variable it removed, so `!/passcodesData/` failed on a correct page. A third was removed outright for being a tautology (`… || true`). Honesty `--pre` vs the pushed admin **70** / staff **30**, fixtures from the EXPLICIT SHA `2c97296`: **24 of 28 FAIL, exit 1**. Tests only — no page bytes, no build number. |
| **80 · staff 37** | `64888af` · tests `caf960f` · pushed 24 Aug | 24 Aug 2026 | **THE REQUEST LIFECYCLE TELLS THE TRUTH TO THE PERSON WHO ASKED.** Two defects, one subsystem, no roster writes. **§ defect 15 — WITHDRAW.** A submitted request could only ever be decided by an admin; there was no way to take it back. People ask for a day off and then no longer need it, and the only route was to message the admin and hope. Now a pending request, an open-shift claim and a swap you PROPOSED can all be retracted. **The guard runs on FRESH data inside the transaction, never on the rendered row** — by the time the click lands an admin may already have decided it, and *a withdrawal must never undo a decision*. Refused if it is not yours, or no longer pending. **Deliberate scope on swaps: only while it is still `pending_target`.** Once the other side has accepted (`pending_admin`), retracting unilaterally would undo someone else's agreement, so that case is not offered. A 3-way reads its initiator from `participants[0]`. **§ defect 16 — A DENIAL CARRIES A REASON.** Deny was one click writing a bare status, so the requester saw a flat "❌ denied" and had to come and ask why. **The machinery already existed on the APPROVE path** — an override reason captured and written to the audit log — it had simply never been wired to deny. Implemented as an INLINE OPTIONAL field, not a modal, for two reasons stated in the code: a blocking `prompt()` would stall the 21 Playwright suites that drive this page, and forcing a reason would slow an admin working a queue. Empty behaves exactly as before. **The ordering is the load-bearing part:** the reason is read BEFORE the transaction, because the row re-renders the moment the status changes — reading afterwards would find an input that no longer exists and silently record nothing. Bounded to 140 characters. Shown to the requester, to the admin's decided list, and in the audit line; the machine-written `autoDenied` reason for a lost claim race is now surfaced too, having previously been stored and never displayed. A withdrawn SWAP also joins the admin's decided list — it never reaches admin, so without that it would vanish from every view instead of being recorded. Gates: NEW `tests/sched/build80-test.mjs` **26/26**, EXECUTING the real `withdrawRequest`/`withdrawSwap` and capturing what they hand the transaction — **stubbing `txUpdateItem` to "just succeed" would have proved the button exists and nothing about whether it is safe.** The race case is asserted explicitly: an already-approved item is refused. Honesty `--pre` vs the pushed admin **79** / staff **36**, fixtures from the EXPLICIT SHA `3e816d1`: **1 passed, 23 FAILED, exit 1**. **Schedule battery all 32 suites green with ZERO skipped**; isolation 27/27; **full auction battery per §2: 54 suites / 2,049 assertions, zero skips.** `node --check` clean on both pages. No rules change. ⚠️ **Two of this suite's own assertions were wrong first** — one tested a COMMENT (`!/prompt\(/` failed because the comment explains why prompt was avoided) and one used too small a search window and reported a FAIL on correct code. The suite now strips comments before textual assertions. Third time this session; the fix is now in the file rather than in a resolution. |
| **79** (staff unchanged) | `3e816d1` · tests `38f4ce5` · pushed 24 Aug | 24 Aug 2026 | **THE USERS PAGE — kept as its own build ON PURPOSE.** It is the single sanctioned exception to the cardinal rule: the one place the schedule writes the roster the LIVE auction reads. Build 78 carried the grid work so that if anything here misbehaves, the blast radius is identifiable. **§ S-hygiene — the KP address is normalised on save.** The LOGIN (Google) address has been trimmed and lower-cased at every writer since [47], because `firestore.rules` matches it case-sensitively. The KP address — which shares `vacations/emails` with the auction and is read back by BOTH sites — had `.trim()` and nothing else, **not even an "@" check**. The auction has normalised it since ITS build 270 and lower-cases at READ time, so the divergence was MASKED rather than absent: this page could write a mixed-case or malformed address into a document the auction believes is clean. `_normKpEmail` is ported and is **byte-for-byte identical to the auction's copy — verified by extracting both function bodies and comparing, not asserted**. That identity is the point: two sites writing one document must agree on what it may contain, and a "slightly better" copy here would be the very divergence this closes. Scope unchanged from the owner's 17 Aug ruling — trim, lower-case, refuse five unarguable shapes, NO domain policy. Wired at all three LIVE writers, each degrading the way that field deserves: `addSchedUser` still ADDS the person and refuses the address out loud (failing a whole add over a secondary e-mail would be worse than the thing it prevents); `saveSchedField('em')` refuses instead of storing; `saveAllSchedUsers` fails the BATCH, exactly as a bad login e-mail or FTE always has — it was the one field on that page with no validation at all. There is no fourth call site: `saveSchedUser` was deleted in 77, so the helper was not written into dead code. **§ defect 29 — `schedFteLoaded` is finally READ.** It had been set since the FTE listener was written and consulted by nothing, while `saveAllSchedUsers` turns a blank FTE box into `deleteField()`. Before that snapshot arrives every box is blank, so **one click on Save All in the cold-load window would have wiped every FTE in `dailysched/fteMap`** — the [48] shape, on the one flag already shaped for the job. Now gating both FTE writers alongside its three siblings. Gates: NEW `tests/sched/build79-test.mjs` **30/30**, EXECUTING the real extracted `_normKpEmail` over trim/case, missing-@, internal space, double-@, dotless domain, leading/trailing dots — and over two cases that guard the SCOPE rather than the code: a gmail address must survive (no domain policy) and an apostrophe/plus address must not be refused for being unusual. Honesty `--pre` vs the pushed **78**, fixture from the EXPLICIT SHA `e141d13`: **11 of 20 FAIL, exit 1** (20 not 30 because the executed block reports a single legible failure when the helper is absent, rather than throwing). **Schedule battery all 31 suites green, ZERO skipped** (browser suites RUN, not skipped). Isolation guard 27/27 — the cardinal rule re-proved after a build that touches roster writers. **Full auction battery per §2: 54 suites / 2,049 assertions, zero skips.** `node --check` clean. Staff bytes unchanged. No rules change. |
| **78** (staff unchanged) | `e141d13` · tests `36c2a53` · pushed 24 Aug | 24 Aug 2026 | **AUTO-POPULATE FINALLY ASKS THE DEMAND RULES — plus a stats read-storm guard and one more dead listener.** Three items, all read/compute paths; nothing here writes the roster (that is build 79's blast radius, kept separate on purpose). **§ defect 5:** `autoPopulate` and `autoPopulateYear` used the static per-shift `capacity` for EVERY day of the month and never called `demandOn()` — which the coverage report has used since build 58. So a weekdays-only shift was filled on Saturdays and Sundays; a shift with NO demand rules — §9's *"not being asked for at all"*, which is not the same as "nobody needed" — still had somebody assigned every single day, and then appeared in the `unfillable` warnings for slots nobody had ever requested. **The filler and the report were working from two different definitions of what a day needs, and only one of them was the owner's.** Both now take their target from a new `autoTargetFor()`: `null`→nobody, `0`→nobody, `n`→n. **Deliberate fallback, and it is a product decision worth knowing:** if NOT ONE shift in the catalog carries demand rules, the old `capacity` behaviour stands — otherwise auto-populate would silently do nothing on a catalog nobody has configured yet. The condition is the same `shiftsWithDemand` count `coverageRows()` uses for `demanded`, so the two cannot drift. Both confirm dialogs now say which of the two behaviours will run (§84's carve-out: wording that causes a wrong action is a defect), and both completion alerts NAME the shift-days skipped as not-asked-for, so "complete — 12 assignments" can never read as a bug. **§ defect 13:** `renderStatsYear` had no in-flight guard — `statsYearLoadedFor` is set only after the 12-month `Promise.all` resolves, so a second click, or any of the ~9 listeners that call `renderAll()` while the panel is open, started another twelve reads. The existing staleness check catches a fetch for a DIFFERENT year and structurally cannot see a duplicate of the SAME one. Now one fetch at a time, released in a `finally`; a deliberate Refresh still forces a re-fetch, so the guard blocks only accidental re-entry. **§ defect 25:** `dailysched/callTotals` and its listener deleted — a legacy fairness counter superseded by `windowShiftCounts()`, still subscribed on every page load, feeding a variable with ZERO read sites. Same shape as the passcodes listener removed in staff 35. One fewer Firestore read per admin page load; the document itself is untouched. Gates: NEW `tests/sched/build78-test.mjs` **26/26** — defect 5 proved by EXECUTING the real `demandOn`/`autoTargetFor` chain over weekday / weekend / holiday / explicit-zero / stacked-rule / no-rules-anywhere cases, and defect 13 proved by EXECUTING `renderStatsYear` three times concurrently and counting **12 reads, not 36**. Honesty `--pre` vs the pushed **77**, fixture from the EXPLICIT SHA `a796d02`: **0 passed, 16 FAILED, exit 1** — and the suite was changed to report a missing build-78 function as a legible FAILURE rather than throwing, because a crash and a regression are indistinguishable from an exit code. **Schedule battery: all 30 suites green with ZERO skipped** — the 21 Playwright suites were made to RUN (cloud, module installed), so this build was gated by the browser suites and not by watching them skip. Isolation guard 27/27. **Full auction battery re-run per §2: 54 suites / 2,049 assertions, zero skips.** *(That re-run was delayed by a stale desktop sign-in; the COST GATE was applied — the owner was told rather than a workaround being ground out — and in the meantime the three auction suites that actually read the schedule admin page were run on the device: 84/84, 338/338, 93/93. The full battery followed once he re-authenticated.)* `node --check` clean on all three inline blocks. Staff bytes unchanged. No rules change. |
| **tests only** | `bc751aa` (tests) · pushed 24 Aug | 24 Aug 2026 | **THE SCHEDULE BATTERY STOPS LYING ABOUT WHAT IT RAN.** ⚠️ **Its commit message does not mention this change** — the message was written before the fix and pushed unrevised, so this row is the record (same ruling as `cbd4a5b`/`427cab8` below: the CODE is right, and rewriting a pushed message is a bigger risk than an inaccurate label). `sched/run-all.mjs` decided whether to skip its 21 Playwright suites by testing for a chromium **BINARY** — but every browser suite's first line is `import { chromium } from 'playwright'`, and `playwright` is not a dependency of this repo. On a machine with the binary and no module all 21 RAN and died on `ERR_MODULE_NOT_FOUND` ("21 of 27 failed"); on the owner's Mac, which has neither, they skipped cleanly. **Same battery, same code, opposite report — which is how two rows in this very file came to read "27/27 with ZERO skipped" and "N green + 21 skipped".** It now asks the two questions the suites themselves ask, in their order, and NAMES WHICH ONE FAILED: can `playwright` be imported, and can a browser actually **launch**. The launch is real, not a path check — `executablePath()` names the full chrome build while `chromium.launch()` will use the headless shell, so the first attempt at this fix skipped a run that would have worked. **Substituting a cheaper proxy for the real question is exactly how the old gate lied.** The summary now prints `⚠ N of 28 suite(s) DID NOT RUN — this is NOT full coverage` beside the green tick, and each skip reads `SKIPPED, not passed (<reason>)`. Proven BOTH ways: on the Mac it names the missing module and skips 21 loudly, exit 0; in the cloud with playwright installed it reports `ENABLED — playwright launched a chromium successfully` and runs **all 28 green, zero skipped** — where the old code reported 21 FAILURES. Tests only — no page bytes, no build number. |
| **77** (staff unchanged) | `a796d02` · pushed 24 Aug | 24 Aug 2026 | **THE CALL-FAIRNESS BASELINE CAN NO LONGER BE ERASED BY A CLICK — plus one dead roster writer removed.** `saveBaseline` REPLACES `dailysched/callBaseline` (correct: the table is the full source of truth and a blank box means zero — so `{merge:true}` is NOT the fix, it would make blanking impossible). But `renderBaseline()` emits **no inputs at all** when the roster is empty or the catalog holds no call shifts, and the Save button stayed live either way — so one click during a slow, empty or permission-denied load wrote an empty map and destroyed every baseline. That document is the **one input that cannot be recomputed from the schedule**; it exists to record call worked BEFORE the system. 77 counts the boxes it expects using the SAME id derivation the writer uses, and refuses the whole write if even one is missing — a partial table is never saved over a complete document. Unchanged since admin build 30. **Second change: `window.saveSchedUser` DELETED (defect 26).** No call site since July 2026, when the Users grid moved to per-field saves — yet it was one of the **six sanctioned handlers** in `tests/sched/isolation-test.mjs`, the cardinal-rule allowlist for writing the roster the LIVE auction reads, and still carried real roster writes, the [47] duplicate guard and the [48] loaded-gate on unreachable code. **The sanctioned set is now FIVE and equals the reachable write surface — a stricter guard, not a looser one.** Three test pins moved with it and NONE is a weakening: the isolation allowlist shrinks; `build75-test`'s sibling COUNT goes 3→2 while its direct assertion (the gate inside `addSchedUser` itself) is untouched; and `test-audit-fixes`'s pin on the deleted function is removed — the other three roster writers keep theirs, one of them EXECUTED. Gates: NEW `tests/sched/build77-test.mjs` **23/23**, executing the real extracted `saveBaseline` against a DOM stub shaped like the real render (a box exists only where the table drew one — a stub that always returned a box would have hidden the defect, which is the [304 · FAST-1] lesson). Honesty `--pre` vs the pushed **76**, fixture from the EXPLICIT SHA `6d31b3e`: **9 of 23 FAIL, exit 1** — a real failure, not a skip. Schedule battery **all 28 suites green with ZERO skipped** — the 21 Playwright suites were made to RUN by installing playwright in the cloud, so for the first time in this site's history a schedule build was gated by the browser suites instead of watching them skip. Isolation guard green. Full auction battery re-run per §2: **54 suites / 2,049 assertions, zero skips** (2,050 − the one pin removed above — reconciles exactly). `node --check` clean on all three inline blocks. Staff bytes unchanged. No rules change. No new Firestore reads or writes. |
| **auction admin 299** (staff unchanged) | `2cd6a55` · pushed 21 Aug (one commit with 298) | 21 Aug 2026 | **§79 — approvals and denials are BLOCKED while bidding is open, not merely warned about.** Owner: *"we currently have a warning not to do that, but blocking is better."* Confirmed first that `adApprove` and `adDeny` consulted NOTHING about bidding state — the readiness warning rendered in the dialog body but the click was never stopped. **ONE definition of open:** the expression was LIFTED out of `approvalReadiness` into a single named `biddingOpenOnServer()` that both it and the new guard call, so they cannot drift — SERVER truth only (the biddingClosed flag, or an ENABLED timer past its deadline), because a merely switched-OFF timer once read as "closed" while every bidder could still write from devtools. **Scope, deliberate:** the five CREATING surfaces are gated (Approve, Deny, Approve All Current Winners, Deny All Losers, confirmApprove); the two REVOKES are NOT — a revoke only ever removes a decision, and blocking correction would trap the admin with a stale decision and no way out (§72: a false refusal is its own incident). The refusal names what is blocked, why, and which button clears it, and in Phase 4 it names the ROUND rather than the phase. **Gates:** NEW `test-admin-299-bidding-open.mjs` **18/18**, executing the real extracted handlers against real server states — including the historical timer-OFF trap, both genuinely-closed states, the revokes staying available, and a structural pin that readiness DELEGATES rather than keeping a copy · honesty `--pre` vs the explicit SHA `6bbb4af` (admin 297 — **298 was filed but never pushed, so per §4 the baseline is the last PUSHED build and this says so**) fails **12 of 17**, and the 5 that pass are exactly the must-keep-working ones · full auction battery **48 suites / 1,881 green** · schedule isolation guard 27/27. **Four suites went red and each was fixed at the cause, not the symptom:** `test-delta-fixes` and `test-high-fixes` pinned the readiness expression by its literal text, so the pins were RE-POINTED to its new home and gained an assertion that readiness delegates — moved, never weakened; `test-c4-phase-identity` got the real guard plus a CLOSED server state (the only state a decision is legitimate in); `test-admin-296` opens the gate for its §72 floor scenario under the file's existing convention, naming the suite that pins it. **Schedule battery RE-RUN after the owner re-authenticated: 27/27 in-cloud against admin 299, ZERO skipped.** (It was blocked mid-session — staging refused with `untrusted_device` — and the §6 cost gate was obeyed: the owner was told the 30-second fix instead of a workaround being attempted.) **Live verified after the push: `versions.json` = admin 299 / staff 161 / mobile 18, fetched twice.** The SCHEDULE repo's commit `fe168c5` carries GitHub's default subject "Update BUILD-LOG.md" rather than the prepared summary — **the owner was asked and is fine with it** (21 Aug: *"I'm ok with the brief uninformative commit summary"*), so it is NOT to be re-raised. Noted only because it is the fourth time and it costs nothing: the CONTENTS are correct, and these rows are the record precisely so a thin commit subject never matters. |
| **auction admin 298** (staff unchanged) | `2cd6a55` · pushed 21 Aug (one commit with 299; suites in tests `985e9b4`) | 21 Aug 2026 | **§77 LANDS — the projection freezes, and §82d is fixed before it ever shipped.** The §70 apparatus is gone (`_natFilter`, `_natReqs`, `_natContested`, `_blockFloor`, the demotion loop); `computeApprovals` is computed from BIDS ALONE, so an approval or denial can no longer re-deal a week — the defect the owner had SEEN as the projected list rearranging under him. `decidedWinners(wk)` and `weekLedger(wk)` answer "who is getting this week" and "how full is it"; four consumers repointed. **The pre-commit check earned its place:** a differential harness ran BOTH real engines (297 and the draft 298) over ~4,000 random week states and found that `weekLedger.committedFte` never subtracted the DENIED — under §77 the projection still lists them, so denied bids counted as filling a week and 2,864 of 3,958 states disagreed with 297, worst case a capacity-3 week reading 0.2 remaining when every bidder on it was denied. One line fixed it, and the same harness proved the fix: differences fell to 852, exactly the intended re-deal cases, matching the three consumers that were already correct. **Suites (§80):** `test-admin-294-engine.mjs` ARCHIVED (it guarded deleted machinery); `test-priority-inversion`, `test-engine-fuzz` and `test-never-events` had their denied-is-never-a-winner assertions REPLACED — not rewritten to agree with the new code — by the §77 INVARIANCE (run the real engine twice, with and without decisions, demand identical output); NEW `test-admin-298-frozen` 17/17 and NEW `test-admin-298-readouts` 4/4, the latter proving its own teeth by putting the §82d defect back. `test-c4-phase-identity`'s sandbox updated — it went red because `adApprove` gained a real ledger dependency, which is exactly what a battery is for. **Gates:** full auction battery **47 suites / 1,861 assertions green**, run with PRE_ADMIN set so the in-suite honesty checks executed (priority-inversion reports 12 assertions, not 11) · `test-admin-298-frozen --pre` vs the explicit SHA `6bbb4af` FAILS 11 of 12 · never-events 5a and the readouts mutation both fire · full schedule battery **27/27 green in-cloud with ZERO skipped** (first complete run — 21 browser suites had been skipping on the owner's Mac) · isolation green · CRNA restamped 298 · `node --check` clean. One archaeological honesty check (the pre-239 mutation) now says loudly that it did NOT run rather than passing silently — its baseline predates the standing fixture rule. |
| **36 (staff) · 76 (admin)** | `6d31b3e` · pushed 20 Aug | 20 Aug 2026 | **RA-1 fixes.** Staff 36 · RA-H12: Phase-4 vacation weeks finally PAINT — a completed Phase 4 shows its winners once results are e-mailed (currentPhase never advances past 4, so the old ph>=curPh skip hid a whole year of awarded weeks), and ANNOUNCED Phase-4 round wins paint all year under the same released-results embargo. Admin 76 · RA-M7: a failed bid-security-map sync can no longer hide behind the caller's green "Saved" — persistent audit-log row + the warning re-issued after the success toast. Gates: `sched/ra-fixes-36-76-test.mjs` 10/10 executed, honesty vs `dc6b8cc` FAILED exit 1; registered in sched run-all; isolation 27/27; auction battery re-run green (standing rule). |
| **75** (staff unchanged) | `dc6b8cc` · pushed 20 Aug | 20 Aug 2026 | **AUDIT LOW — addSchedUser gains the build-48 loaded-gate.** Its duplicate-login-email refusal consulted a possibly UNLOADED loginEmailsData mirror — vacuous in the cold-load window, so a duplicate address slipped past ENTRY (the fail-closed sync backstop still blocked NE-6, but entry refusal is the never-event’s named first guard). Same gate + wording as its three siblings. Cardinal rule: adds NO reads or writes. Gates: NEW `tests/sched/build75-test.mjs` 4/4 (executed, non-Playwright) · honesty `--pre` vs pushed `4f2e7ef` fails 4/4, exit 1 · schedule battery 26 suites green · isolation 27/27 · full auction battery 34 suites / 1,638 green (re-run after a schedule build per §2) · node --check clean. |
| **tests only** | tests `1d9e8da` · pushed 19 Aug | 20 Aug 2026 | **PW-1 — the Playwright harness learns App Check.** Since 71/31 both pages import a FOURTH gstatic module, `firebase-app-check.js`, which the 21 browser suites’ swap lists never gained — on any machine that cannot reach gstatic (the cloud containers) every page-load suite died at boot; 20 of 25 failed in-cloud on 20 Aug. Fix: the fourth URL added to every suite’s SWAPS and an inert `fake/firebase-app-check.js` (no-op `initializeAppCheck`, stub `ReCaptchaV3Provider`). Harness only — no page bytes change, no build bump. Honesty for a harness change: `staff30-test.mjs` crashed at page load pre-fix, 20/20 post-fix. Gates: schedule battery **25/25 suites green in-cloud** (first time since App Check shipped) · full auction battery 23 suites / 1,495 green · isolation 27/27 · `node --check` clean on all 21. |
| **staff 35** (admin unchanged) | `3fda419` · pushed 19 Aug | 19 Aug 2026 | **FB-5 PREP — the retired passcodes listener is deleted** (auction staff 150). Identical defect and identical fix: a live listener on the auction's `vacations/passcodes`, written into a variable nothing reads, with `_schedSignInDataReady()` blocking this site's sign-in card on it. Removing it also removes one of only four things this login screen waits for. The schedule ADMIN page never referenced the document at all, so nothing there changes. Cardinal rule respected — this REMOVES a read of a `vacations/*` document, it adds none, and the isolation guard is green. Gates: shared `test-appcheck-login.mjs` 72/72 with honesty at `0d84ece` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,482 green · node --check clean. |
| **74 · staff 34** | `0d84ece` · pushed 19 Aug | 19 Aug 2026 | **FB-6 — the security box now shows on BOTH sign-in panels** (auction 284/149). Same defect and same fix: "Remember me" is checked by default, so returning users got the WELCOME BACK panel and never saw the box — which since 73/33 carries Google's required reCAPTCHA attribution. These pages use their own panel ids (`signPicker`/`signWelcome` on staff, `authGateCard`/`authGateWelcome` on admin), which is exactly why the fix was written against each page's real structure rather than copied from the auction. Gates: shared `test-appcheck-login.mjs` 64/64 with its own honesty baseline at `fb42ed7` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,470 green · node --check clean. |
| **73 · staff 33** | `fb42ed7` · pushed 19 Aug | 19 Aug 2026 | **HIDE THE BADGE AGAIN — schedule half** (auction 283/148; DECISIONS §66). Owner reversed §65 after seeing the badge live. Un-hide rule removed; the sign-in security box gains Google's required reCAPTCHA attribution with both links, because hiding the badge makes that mandatory. Same paired invariant enforced by the shared suite. FB-6 (the box is invisible to returning users) applies to these pages too and is deliberately deferred. Gates: shared `test-appcheck-login.mjs` 52/52 · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,458 green · node --check clean. |
| **72 · staff 32** | `4fa726d` · pushed 19 Aug | 19 Aug 2026 | **SHOW THE reCAPTCHA BADGE — the schedule half** (auction 282/147; DECISIONS §65). Same single prefix-selector CSS rule, same reason: Firebase App Check hides the badge inside its own `display:none` container, and the owner wants it visible. Prefix selector matters more here than anywhere — these two pages use app names `sched-user-site` and `sched-admin`, so a rule copied from the auction with a hardcoded id would have silently done nothing on both. No other bytes change. Gates: covered by `tests/test-appcheck-login.mjs` (52 assertions across all six pages) with its own honesty baseline at `324c797` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,458 green (re-run after a schedule build per §2) · `node --check` clean. |
| **71 · staff 31** | `324c797` · pushed 19 Aug | 19 Aug 2026 | **APP CHECK — the schedule half of the same build** (auction 281/146; DECISIONS §63). The schedule shares the `vacation-25e8e` Firebase project with the auction, so it shares ONE App Check registration and one site key — and, as FB-3 already records, one quota and one bill. Both pages initialise App Check before `getFirestore`, with auto-refresh on. Staff page: the same 1500 → 3000 ms sign-in readiness fallback as the auction, for the same reason (App Check adds a reCAPTCHA script load and a token fetch in front of the first Firestore request; the old guard could unlock the sign-in card with an empty person list), and the warning now names which document was missing. Both pages take the replaced login security box (DECISIONS §64). **Enforcement is NOT enabled** — console step, staged after monitoring. Cardinal rule respected: no auction bytes are touched by this half, and the auction battery was re-run after it anyway (§2 — it has broken three times). Gates: `tests/test-appcheck-login.mjs` covers both schedule pages with the same structural checks and the same executed init block · honesty from explicit SHA `34f19b7` · schedule battery green · isolation 27/27 · full auction battery 23 suites / 1,443 green · `node --check` clean. |
| **70** | `2c97296` · pushed 19 Aug | 19 Aug 2026 | S5b (UX-PLAN S5, part 2 of 3) — THE GRID FLIPS. A View switch beside the S5a Density switch: **People** is the original grid, one row per person; **Shifts** turns it on its side — one row per shift in catalog order, each with its family-colour swatch, and cells naming who is on it. A cell under its demand gets an inset ring — amber at half or better, red below — and `box-shadow` was chosen over a background so the ring STACKS with the weekend and today tints instead of fighting them. A shift with no demand is never marked short (§9: silence is not a gap). Density applies to both views: Compact shows filled-over-needed instead of name chips. Choice remembered in `localStorage['dsGridView']`, read in a try/catch like S5a. **Read-only by design, stated:** a shift-view cell can hold several people, so there is no single obvious thing a click should edit; editing stays in the People view rather than inventing a second editor. Worth revisiting if the owner wants it. Admin page only; staff bytes unchanged; no rules change; no new Firestore reads or writes. Gates: NEW `tests/sched/build70-test.mjs` 26/26 executing the real `gridShiftRows`/`renderGrid` against a DOM shim (non-Playwright, so it RUNS) · honesty `--pre` vs the pushed 68 fails 13/13 · **build69-test.mjs's shim updated** — `renderGrid` gained a second axis and the S5a suite went red until it declared `gridView`, which is exactly what a battery is for · schedule battery 4 suites green + 21 skipped · isolation 27/27 · auction battery 22 suites / 1,403 green · `node --check` clean. |
| **69** | `2c97296` (one commit with 70) · pushed 19 Aug | 19 Aug 2026 | **MESSAGE MIS-PASTE, recorded not rewritten (same as `7fcd3f1`/`594778e` on 18 Aug):** commit `2c97296` carries the TESTS repo's summary, and an older over-length draft of it at that. The CONTENTS are correct and verified — `admin/index.html` at BUILD 70, `versions.json` admin:70, this log's two rows — and live is serving 70/30. The real record is these rows, which is exactly why they exist. S5a (UX-PLAN S5, part 1 of 3) — THE GRID FINALLY SHOWS WHERE THE HOLES ARE. **Coverage strip:** a second sticky header row under the dates, one cell per day, green when that day's demand is fully filled, amber under half short, red otherwise, showing the shortfall as `-n`. It reads the SAME `coverageRows()` the coverage report uses — build 69 adds a per-day roll-up (`byDay`) INSIDE the existing loop rather than recomputing, so the strip and the report cannot drift apart; a suite assertion pins that the per-day totals sum to the report totals. Hidden entirely when no demand is configured anywhere (`demanded`), so a department that never sets demand sees no empty strip. **Density toggle:** Detail keeps the text chips; Compact swaps each for one family-coloured 9px block, capped at three with a `+n` so a busy cell cannot blow the row height — labels move to tooltips, nothing is lost. Choice remembered in `localStorage['dsGridDensity']`, read inside a try/catch so a browser refusing storage still boots on Detail. **Scope, stated:** S5 as specced is FOUR features; this is two of them. The flipped shifts-as-rows view is S5b. The filter bar is S5c and its role/site half is BLOCKED — people carry no role or site attribute today (only `usernamesData` and `fteMap`; MD/CRNA and site live on SHIFTS), so row grouping waits for Stage 4. Admin page only; staff bytes unchanged; no rules change; no new Firestore reads or writes. Gates: NEW `tests/sched/build69-test.mjs` 35/35, executing the real `coverageRows`/`renderGrid`/`cellBlocksHtml` against a DOM shim — deliberately NOT Playwright so it RUNS in the battery instead of skipping · honesty `--pre` vs the pushed 68 fails 19 of 20 (coverageRows is loaded separately so the byDay assertions genuinely run and fail there, rather than the suite bailing early) · schedule battery 3 suites green + 21 Playwright-skipped · isolation guard 27/27 · auction battery 22 suites / 1,403 green · `node --check` clean on all three inline blocks. |
| **68 · staff 30** | `7fcd3f1` · pushed 18 Aug (one commit with 67) | 18 Aug 2026 | S4 (UX-PLAN) — THE LAST {list:[]} DOCS GO PER-ITEM. requests, swaps and openShifts were the only single-doc arrays left: every submit/decide transacted on the WHOLE list (publish-day contention at 80 users) and the arrays grew without bound. Same cure as the audit log in 63: one doc per item in dailysched/{requests|swaps|openShifts}/items/{id}. Readers merge legacy + items (item wins by id; _gone = tombstone); ALL writes go to item docs; the three legacy docs are frozen forever; a legacy item migrates itself the first time it is touched — no conversion step. Claims get structural dedup: doc id = claim_{openId}_{user}, with the legacy array still checked in the same transaction and decided history archived before re-apply. BOTH pages must deploy together. ⚠️ firestore.rules gained the dailysched subcollection match (also needed by 59 days/ and 63 entries/ — the repo file never had it); publish to the console BEFORE pushing these pages, and first COMPARE the console’s current rules against the repo file. Gates: build68-test 24/24 ×3 · staff30-test 20/20 ×3 · honesty vs 67 = 21 fails, vs staff 29 = 15 fails · ALL 23 schedule suites green (isolation/50/61/staff29 probes re-anchored to read both places; fake deny gained RegExp) · auction battery 15/1,101 green. |
| **67** | `7fcd3f1` · pushed 18 Aug (one commit with 68) | 18 Aug 2026 | S3 (UX-PLAN) — THE QUICK-WINS VISUAL BATCH. Five hard-to-read sections got the Families treatment. GRID: real thead + sticky header and name column in a scrolling wrap, today highlighted, federal holidays dotted. ELIGIBILITY: opens on the FIRST family instead of the 90-column wall — "All" is one click (latch guarded against the boot listener storm, which briefly ran it with an empty catalog). STATS: default month view is one column per FAMILY (family colors in the headers) + Call total/Total/OFF/VAC + a Group-total row; "Every shift" toggle keeps the full matrix, now with sticky name column. REPORTS: run cards first; the two config lists moved below them into an OPEN ⚙️ Settings fold; overnight-call chips grouped by family; comparison pool split FTE-set / no-FTE. CATALOG: sticky column-label bar over the shift list in its own scroll wrap. Gates: build67-test 28/28 ×3 · honesty vs 66 = 21 fails · ALL 21 schedule suites green (elig-test now clicks "All" before asserting the whole grid — entry point changed, no assertion weakened) · auction battery 15/1,101 green. |
| **staff 29** | `106addb` · pushed 17 Aug | 17 Aug 2026 | §55 — USERS CANNOT REQUEST INELIGIBLE SHIFTS (defect 4's staff half, closing defect 4 entirely). The staff page had subscribed to the eligibility grid forever and never read it. Now: the request dropdown offers only the signed-in user's eligible shifts; the submit handler refuses an ineligible shift even against a manipulated page; late-arriving eligibility refreshes the list; a no-ticks user gets an honest empty state. Hard block by owner ruling — a deliberate exception to §51, which still governs admin overrides. Stored request shape unchanged. Gates: staff29-test 19/19 ×3 · honesty --pre vs 28 = 7 pass/12 fail on exactly the defect · isolation 27/27 · elig 33/33 · build64 re-run green · auction battery RAN GREEN in-session 17 Aug (14 suites / 1,074, honesty skipped). |
| **66** | `623804e` · pushed 18 Aug | 18 Aug 2026 | S2 (UX-PLAN) — DIRTY-FLAG RENDERING. Every snapshot used to rebuild ~20 panels including hidden ones — the disease behind every typing-eaten defect. Now listeners mark panels dirty; one animation-frame flush renders the chrome (badges, banners, cell modal) plus only the VISIBLE panel; hidden panels render fresh on entry; boot still paints everything once (gated on roster+catalog arrival, not on flush count — a first-flush race the suite caught). The [64] typing guards unchanged and re-proven. Suites 59/62 now navigate before asserting hidden panels, as a user would. Gates: build66-test 14/14 ×3 · honesty vs 65 = 5 fails · ALL 20 schedule suites green · auction battery 15/1,101 green. |
| **65** | `aab97a2` · pushed 18 Aug | 18 Aug 2026 | S1 (UX-PLAN) — THE SECTION MAP. §S00 table of contents + 21 greppable §S banners, each naming owned state and writes; the Users banner carries the live-auction ⛔; auth-gate CSS pointer added. NO behavior change — proven by the ENTIRE battery green on 65. FINDING recorded: 8 suites had been red at full-sweep since build 63 (audit probes read the legacy array); anchors updated, no assertion weakened; full sweep is now 19 suites green and the claim is executed, not recalled. Auction battery 15 suites / 1,101 green against this page. |
| **—** (no build: admin 64 / staff 29) | `b8c078e` · pushed 18 Aug | 18 Aug 2026 | **THE OVERNIGHT REVIEW — `design/UX-PLAN.md`.** Commissioned by the owner on going to bed, answers expected in the morning: a per-section visual diagnosis with fixes, industry patterns, missing-feature gaps, a code-organization verdict and a proposed build sequence. **No code and no build number changed.** It is load-bearing rather than a one-off document: builds **65 (S1, the section map)**, 66-68 and 70 cite it by section as the source of what they shipped. **⚠ ROW WRITTEN 25 Aug, LATE.** The artefact had no row and no ruling, so the thing several builds were built FROM was unrecorded — found by a subagent survey. The owner's verbatim brief is in `anesthesia-kp.github.io/TODO.md` §1. |
| **64** | `13db983` · pushed 17 Aug | 17 Aug 2026 | Defect 12 — TYPING SURVIVES. renderAll() rebuilt the Users grid and the baseline grid on every snapshot, replacing every input mid-edit; unsaved typing died. Build 55's catalog guard (whose comment named defect 12 as the same shape) now covers both grids: rows are left alone while focus is inside; one rebuild with fresh data on focus-leave (250ms settle). Same accepted trade as [55]. Gates: build64-test 19/19 ×3 · honesty --pre vs 63 = 9 pass/10 fail on exactly the defect · isolation 27/27 · elig 33/33 · auction battery RAN GREEN in-session 17 Aug (14 suites / 1,074 assertions, honesty blocks skipped — old baselines unavailable in-cloud). |
| **63** | `c0021e4` — pushed, live | 16 Aug 2026 | The AUDIT LOG stops forgetting. It was never a size problem — it trimmed itself at 400 entries and kept 300, silently discarding the rest, including build 61's override records. Now one document per entry in `auditLog/entries`, never trimmed, read newest-300 with a real query. Pre-63 entries still shown, merged, with no conversion step (append-only, so unlike §47 it cannot split-brain). Also fixes `isolation-test`, which had stopped completing at all — `startsRegex()` re-scanned the whole emitted buffer per `/`, which went quadratic on a 318 KB page and hung the CARDINAL-RULE suite with no output. |
| **62** | `b2e0458` — pushed, live. ⚠️ carries builds 61 AND 62 in one commit, and its subject describes the TEST SUITES, not the build | 16 Aug 2026 | §49a — BOTH the real holiday and the federal observed day count, for all holidays (Independence Day 2026 = Sat 04 Jul AND Fri 03 Jul), including the cross-year case where 1 Jan on a Saturday is observed 31 Dec of the year before. Plus §52 — the Shift Catalog grouped by family with headings, and sortable by family / catalog order / A–Z. |
| **61** | inside `b2e0458` (with 62) — pushed, live; no commit of its own | 16 Aug 2026 | Defect 1 — ONE definition of "is this assignment a problem?". Request and swap approval now run the same eligibility / capacity / vacation / collision checks the cell editor runs; warns and records an override, never blocks (§51). Eligibility checked on every path for the first time, closing half of defect 4. |
| **60** | `15822e3` — pushed, live | 16 Aug 2026 | The config locks open by default (§50) — Shift Catalog, Report settings, Simulator. The Users page and the unconverted-month guard deliberately stay closed (§50a), and both refusals are asserted so a later session cannot "finish the job". |
| **59** | `b7dd372` | 16 Aug 2026 12:48 | §47 — a month became one document PER DAY (`sched_YYYY-MM/days/{DD}`). Unconverted months are shown in full but read-only behind a banner; converting copies in one atomic batch and keeps the old record as a backup. Plus the repo housekeeping and the `_archive/` folder. ⚠️ **commit message lost** — a test file was pasted instead. |
| **58** | `9f8a8b1` | 16 Aug 2026 11:25 | Stage 2 — COVERAGE against demand, closing defect 6. "Nothing has been asked for" and "nothing has been filled" are reported as different states. |
| **57** | `1e4e39c` | 16 Aug 2026 11:02 | Renaming a shift: the label changes, the internal id never does. |
| **56** | `3e2b6cc` | 16 Aug 2026 10:53 | Stage 1 — BULK ENTRY: duplicate-an-existing, and paste-a-list with a preview. |
| **55** | `427cab8` | 16 Aug 2026 10:39 | Stage 1 — DEMAND rules (stacking, last match wins), the holiday calendar, and tab-through time entry. ⚠️ **its commit message says "Build 54"** — the code is build 55. |
| **54** | `8763e18` | 16 Aug 2026 08:01 | Stage 1 — shift TIMES and SITES in the Shift Catalog, with the 24-hour parser and midnight-aware durations. |
| **53** | `e3cfef3` | 16 Aug 2026 07:46 | The Testing section — auto-populate and Clear Month moved behind rehearsal mode (§45). |
| **52** | `cbd4a5b` | 16 Aug 2026 07:20 | THE ASSIGNMENT MODEL — a list per person per day, closing defect 2 (the silent overwrite). ⚠️ **its commit message says "Build 51"** — the code is build 52. |
| **51** | `0027a5e` | 16 Aug 2026 07:12 | Reports — per-doctor shift counts, dated comparisons, FTE-adjusted fairness, and the overnight-call tag. |
| **50** | `ebf7a27` | 16 Aug 2026 | Small-fix batch — stale-build gate, Quick View month boundary, staff error surface, Users lock, audit log, sticky name column. Closes defects 11, 14, 19, 30. |
| **49** | `9dbbd9b` | 15 Aug 2026 | Eligibility rebuild; demo banner on both pages. |

## Commits whose message names the wrong build

Recorded here rather than rewritten. Rewriting pushed history to correct a message is a far
bigger risk than an inaccurate label, and the CODE in both commits is correct.

- `e260f4f` (this repo), `0832ebf` (tests) and `ae11548` (the docs repo) all say **82**; every
  one of them contains **83**. The owner pushed while build 83 was being written, so the tree
  moved on under a message prepared for 82. Both builds are real, both are described above, and
  the 83 row names this. Nothing is missing and nothing is rewritten.
- `cbd4a5b` contains **build 52**; its message says "Build 51".
- `427cab8` contains **build 55**; its message says "Build 54".
- `7fcd3f1` (this repo) and `594778e` (tests) carry the combined COMMIT-MESSAGES file
  pasted whole, so their SUBJECT line is a section divider ("═══ schedule ═══"). The
  bodies contain the right messages. Recorded, not rewritten — same ruling as above.
  The combined file now opens with a "copy ONE section per repo" banner.

## The tests repo

Seven of its recent commits carry a test file's contents as the message
(`6cee6a6`, `c097b38`, `0a88e0a`, `f8d6565`, `60c9942`, `0c208b9`, `8ba3348`). Each one is
the battery for the schedule build of the same era, and the schedule row above describes
what it covers. Nothing is missing from the test files themselves — every suite opens with
a header comment stating what it proves and what its honesty check is.

## How to not lose another one

The summary for the build you are about to push is written to **`COMMIT-MESSAGE.txt`** in
each repo — deliberately NOT a dotfile (the old `.claude-commit-msg.txt` was invisible in
Finder because of the leading dot, which is exactly how messages got lost). It is
gitignored but visible; open it and paste the whole thing into the commit box. It is also
delivered to the chat outputs column every build. Copy from either — never from a source or
test file.
